# OpenAI 账号会话数上限：按会话分账号，放开单会话并发

**状态**：已实现；账号未设置会话上限时不生效。仓库组织与发布流程见 `FORK-NOTES.md`。

本文只涉及 OpenAI OAuth（ChatGPT 订阅）账号，是通用设计说明。各账号的上限与并发取值属于部署参数，
不在本文。旧版选号的整体流程见 `DESIGN-openai-account-pace.md` §0。

## 0. 背景：并发数管不住"一个账号服务多少会话"

账号的并发上限现在同时承担两件事：限制一个账号同时服务多少个用户会话，以及限制一个会话能同时发出多少
请求。codex 的一个会话会同时发出多个请求，两件事因此互相牵制。

**codex 的会话会并发**（codex 源码，2026-09-23 的 main）：

- 会话标识是请求头 `session-id`，取值是根线程的 id；body 的 `prompt_cache_key` 同值。
- 子代理（`spawn_agent`，默认开启）、`/side` 分叉、review 与父会话共用这两个标识，只有 `thread-id` 不同。
- 单个线程内的模型请求严格串行（并行工具调用只让工具并行，下一次采样要等全部工具结束）。所以一个会话
  同时在途的请求数，就是它同时在跑的线程数：典型为 1；默认配置用子代理时最多约 7 个（根线程 1 + 子代理 6）；
  用户调高 `agents.max_threads` 则没有硬上限。
- 标题 / recap、记忆整合等后台请求用新的会话标识，是独立的短会话。

**线上数据**（一个部署实例近 3 天，OAuth 账号上带显式会话标识的 403 个会话、21,537 个请求）：

| 观测 | 值 |
|---|---|
| 出现过同一时刻多个请求在途的会话 | 143 个（35%），峰值 10 |
| 会话在途时间中同时 ≥2 / ≥3 个请求的占比 | 14.6% / 9.3% |
| 同一会话落在多个账号上 | 10 个 |

峰值 10 超过了账号的并发上限（5 或 8），多出的请求在网关排队。**并发上限实际在节流大会话的子代理**：
调低它，大会话变慢；调高它，一个账号又可能被堆上很多会话。

本功能把两件事拆开：用**会话数上限**限制一个账号服务多少个会话，并发上限放宽到能容纳单个会话的展开。

## 1. 目标与约束

- **G1 按会话分账号**：账号设了上限时，新会话优先落到活跃会话数未满的账号。
- **G2 不打断已有会话**：已绑定在账号上的会话照常回到该账号，不因上限迁移、不等待会话名额。约束与节奏组件
  一致——粘性优先于平衡。
- **G3 可用性优先**：会话上限不新增拒绝与等待——有余量的账号都用不了时，新会话落到满额账号（溢出），只做记录。
- **G4 账号级设置，默认不限制**：上限是每个账号的一个可空整数，空即不限制，和并发数在同一处配置与显示。

由此，上限是**软上限**：它约束的是新会话的去向，活跃会话数可能暂时超过它（空闲过久的会话回来、溢出，
见 §3）。

不做：

- 高级调度器路径，以及旧版选号在 `gateway.scheduling.load_batch_enabled=false` 时的简化分支（它绕过下文的
  三层，默认开启批量加载时不会走到）。两者都与节奏组件的覆盖范围一致。
- 分组级或套餐级的上限。
- Anthropic 账号上游已有的会话限制保持原样。

## 2. 会话与活跃的口径

**会话**：网关的路由会话，与粘性绑定用同一个会话哈希。显式标识按 `session-id` / `session_id` /
`conversation_id` 等请求头、body 的 `prompt_cache_key` 的顺序取；都没有时 HTTP 由内容推导，WebSocket 建连
另有按分组、用户与 API Key 生成的后备标识。不同来源的稳定性不同：

| 来源 | 保证 |
|---|---|
| 显式标识（codex 的 `session-id`） | 一个 codex 会话（含其子代理）一个标识，跨请求稳定 |
| 内容推导 | 近似：相同开场内容的独立调用可能合并，内容变化也可能拆开 |
| WebSocket 后备标识 | 一条连接内稳定 |

计数只以网关路由会话为准，不追求与上游看到的会话身份逐一对应（出站身份另含 API Key 与账号的命名空间）。
哈希为空的请求不计数、不受约束。

**计数身份与粘性身份分开**：计数永远用请求自己的会话哈希。选号过程中某些路径为保护已有绑定而不带哈希进入
旧版选号（guardian 回退，§3.1），此时计数身份经上下文带入，照常计数，但不改绑定。

**活跃**：会话在该账号上最近一次登记后 T 分钟内。每次选号登记一次（即请求开始时），WebSocket 每轮续期（§3.5）。
登记只会刷新或自然过期，不主动删除（§3.4）。

**T 取 15 分钟**，存在账号 `extra.session_idle_timeout_minutes`：

- 登记只在请求开始时刷新，T 必须覆盖单个请求的时长。线上请求时长 p99 为 3 分钟、p99.9 为 6.5 分钟、
  最长 13 分钟，3 天里有 62 个请求超过 5 分钟、没有超过 15 分钟的。上游 Anthropic 的默认值 5 分钟会让
  这些长请求在进行中就被判为会话结束。
- 会话内相邻两次请求的间隔 p99 为 32 秒；3 天里超过 5 分钟的间隔 66 次、超过 60 分钟的为 0。15 分钟能
  覆盖绝大多数停顿，又不至于把已经结束的会话挂太久。
- 界面设置上限时，若 extra 里还没有 T，一并写入 15（§5）。直接改库只写上限时，T 按上游访问器的默认值
  5 分钟计。
- 账号的 T 是登记、计数与展示的共同依据，三处都取这一个值。

**存储**：复用上游的 `SessionLimitCache`（Redis 有序集合，键 `session_limit:account:{id}`，成员是会话哈希，
分数是登记时刻）。它的"清理过期 + 查重 + 计数 + 登记"在一段 Lua 里原子完成，过期按调用方传入的 T 惰性清理。
账号 ID 在各平台间唯一，与 Anthropic 账号共用键空间不冲突。它的单成员活跃判定用组件全局的默认窗口、不读账号 T，
本功能不使用它。

**配置键**：`extra.max_sessions`（正整数；缺省即不限制）与 `extra.session_idle_timeout_minutes`。上游 Anthropic
用的是同两个键，OpenAI 账号上游不读它们。

## 3. 选号规则

只挂在旧版选号的三层上（范围见 §1）。以下动作都只对设了上限的账号生效，未设上限的账号不登记、不查询。

基本动作：

- **放行登记**：不查上限，把会话登记到账号上（没有则加入，有则刷新时刻）。上游的登记操作在上限 ≤ 0 时直接放行、
  什么也不写，所以放行登记要以一个足够大的正数作为上限调用它。
- **确认登记**：带上限的原子登记。会话已在集合里则刷新并通过；不在且未满则加入并通过；不在且已满则不通过。
- **有余量**：账号未设上限，或活跃会话数小于上限。

**分段**：新落点的候选按有无余量分成两段，**有余量段在前、满额段在后**，段内保持原有顺序（优先级、负载、
节奏重排等）。满额段不是被剔除，而是只在有余量段用不上时才用，用它即溢出（§3.3）。

### 3.1 各路径

| 路径 | 行为 |
|---|---|
| `previous_response_id` 续接 | 放行登记 |
| guardian 父会话亲和（codex 自动审查子代理跟随父会话的账号） | 放行登记 |
| guardian 回退（父会话账号用不了，不带哈希进入旧版选号以保住父绑定） | 按新落点分段选号与登记；计数身份经上下文带入，不改绑定 |
| 第 1 层粘性命中（抢到槽，或在绑定账号上排队） | 放行登记 |
| 绑定账号队满后的单次借用 | 按新落点处理（第 2、3 层） |
| 第 2 层新会话（含负载读取失败时的降级分支） | 分段抢槽与确认（§3.2） |
| 第 3 层兜底排队 | 分段排队与确认（§3.2） |

放行登记会让活跃数超过上限：会话空闲超过 T 后回到绑定账号时，它已不在集合里，重新登记即多出一个。按 G2
这是有意的；溢出同理。

**借用后的绑定**：第 2 层直接抢到槽的借用不改绑定；借用走到第 3 层排队、或 WebSocket 建连落在借用账号上时，
handler 在排队成功或建连后会把绑定改到借用账号（未启用利润门控、也不是 guardian 保护时）。这是现有行为，
本功能不改。对计数而言两种情况都成立：借用账号登记了这个会话；改绑后会话此后按粘性回到新账号，原账号上的
登记到期退出。

### 3.2 第 2、3 层：分段、确认、兜底

**第 2 层**：

1. 一次批量查询取各候选的活跃会话数，分出有余量段与满额段。
2. 有余量段非空时，只在有余量段内走原逻辑：节奏在段内重排，按最终顺序逐个复核、抢槽。抢到槽后做确认登记：
   不通过（两个新会话争同一账号的最后一个名额，后到者）则释放刚抢到的槽，把该账号记为满额，继续试下一个。确认
   通过之后才绑定粘性会话、交回节奏的"抢到"。都没成则照原逻辑用刷新后的负载再试一轮、再转入第 3 层。
3. 有余量段为空时（一开始就为空，或段内账号都在确认时被抢满），满额段即全部候选，按原逻辑抢槽，抢到后放行登记
   并记溢出（§3.3）。
4. 负载读取失败的降级分支按同样的分段与确认执行。

有余量段非空但段内账号的槽都被占满时，新会话转入第 3 层，在有余量的账号上排队，而不是立刻溢出到满额账号的
空闲槽。这是 G1 的取舍；并发放宽后槽满应当少见。

**第 3 层**：按原有兜底顺序，先有余量段、再满额段逐个复核：

- 有余量段的账号：确认登记通过即返回排队计划；不通过则把它记为满额，试下一个。
- 有余量段用尽（复核不过或确认不通过）后进入满额段：复核通过即放行登记、记溢出、返回排队计划。
- 最后把设了上限的有余量候选再排一遍：确认时才被抢满的账号此时已记为满额，第二次遇到按溢出放行。第一次复核就
  不可用的账号，第二次照样过不了复核。

原来的全部候选都在这个顺序里，确认没通过的账号还会作为溢出再出现一次，所以原逻辑能服务的请求，本功能下仍能
服务——名额竞争也不会让请求落空（G3）。登记发生在返回排队计划时；
排队随后失败的，登记留到 T 过期（§3.4）。

### 3.3 溢出

溢出 = 新落点用了满额的账号（分段时已满的，或确认时才被抢满的）。只在有余量段用不上时发生：第 2 层是有余量段
为空，第 3 层是有余量段逐个用尽。
每次溢出记一条 WARN 并计数（§6）。溢出持续出现说明账号不够或上限设得过低。

不选"拒绝"与"排队等待会话名额"：拒绝会让 codex 在重试后报错；会话名额要等空闲超时才释放，等待是分钟级的。

### 3.4 失败与换号：不主动注销

登记只会刷新或按 T 过期，任何失败都不删除登记。

一个会话会同时有多个请求在途。其中一个请求在账号 A 上失败、换号重试时，同一会话的其他请求可能仍在 A 上执行；
此时从 A 删除这个会话，A 的计数就会少掉一个仍在服务的会话，新会话随即得到虚假的余量。账号上的登记只有一个成员，
分不出是哪个请求留下的，所以不做主动注销。

代价是：选号登记之后没能完成的尝试（换号前的失败、排队失败、最终失败）会让登记留到 T 过期，计数偏高最多 T 分钟。
换号本身少见，这个偏差可以接受；它与单次借用、单请求短会话留下的登记性质相同。

### 3.5 WebSocket

WebSocket 连接只在建连时选号（此时登记），之后的轮次不经过选号。长连接上的会话必须每轮续期，否则 T 分钟后就会
被当成不活跃。续期只刷新时刻、不查上限——连接已经在这个账号上。账号的上限与空闲超时每轮重新读取（调度快照）：
连接活得很久，期间配置可能已经改了，沿用建连时的账号对象会让新设的上限对这条连接不生效，旧的空闲超时还会按错的
窗口清理整个账号的登记；读不到时沿用连接上的账号并计一次错误。三条路各在自己的每轮入口续期：

| 路径 | 续期位置 |
|---|---|
| ctx_pool、HTTP bridge | ingress 的每轮请求解析（两者共用，票据的 WS 准入所在处） |
| passthrough | 首帧在 relay 启动前单独写上游，逐帧过滤器处理后续帧；两处都续期（与票据的首帧、逐帧准入同处） |

续期用的是建连选号时的会话哈希：handler 在转发前把它放进上下文，续期处从上下文取。ingress 每轮算出的
`sessionHash` 会被替换成线程级的执行作用域（用来区分父线程与子代理的连接状态），不能拿来计数，否则父子线程
会被当成多个会话。

### 3.6 同一新会话的并发首请求

一个还没有绑定的会话同时发出多个请求（例如刚启动就展开子代理）时，它们各自选号，可能落到不同账号，最后写入的
绑定生效。这是现状，本功能不协调首次落点：每个实际接纳了它的账号各自登记，按口径各计一次。首轮之后会话按粘性
收敛到一个账号，其余账号上的登记到期退出。

### 3.7 故障

Redis 出错时失败开放：按不限制处理，记 WARN（按间隔节流）。与上游 Anthropic 的做法一致。批量计数里单个账号查询失败时，
上游的实现不报错、只是结果里没有它；缺项不能当成 0（那样满额账号会被当成有余量），同样按查询失败处理。

## 4. 与并发、节奏组件的关系

**并发怎么配**：会话上限生效后，并发上限只需容纳单个会话的展开。并发 ≥ 会话上限 × 单会话峰值时子代理不会被
节流；codex 默认的单会话峰值约 7，线上观测到过 10。具体取值还要结合对"单账号同时请求数"的风控判断。

**节奏组件**：第 2 层先分段，节奏只在有余量段内重排（有余量段为空时在满额段内重排）。决策记录的每一轮增加满额段
的账号与溢出标记（§6）。节奏的并发占用调整（节奏设计 §2.5）在并发调大后很少触发；节奏另有会话占用调整
（节奏设计 §2.6），按本次分段查得的活跃会话数算会话占用，在有余量的账号之间让占用低的多接新会话；确认登记
没通过、被记为满额的账号，之后的轮次按占满计。两项各自有开关。

## 5. 配置与管理界面

**编辑账号**：并发数所在的那一行，为 OpenAI OAuth 账号加一个"最大会话数"输入框，留空即不限制。

- 打开时取账号的 `max_sessions`。
- 保存时在提交前的最后一步合并进 extra：有值则写 `max_sessions`，且 extra 里没有 `session_idle_timeout_minutes`
  时写入 15；清空则删除这两个键。
- 创建账号与批量编辑不加：新账号默认不限制，创建后在编辑里设置。

**账号列表**：容量列里并发徽标下方的会话徽标（活跃数 / 上限）是上游现成的，只对 Anthropic 显示；显示条件放宽到
OpenAI OAuth。上游的提示文案是"新会话需等待"，与本设计的行为不符，OpenAI 账号换一条文案：满额时新会话优先
落到其他账号。

**后端**：账号 DTO 对 OpenAI OAuth 也导出 `max_sessions` 与 `session_idle_timeout_minutes`；管理列表与账号详情
对 OpenAI OAuth 也查活跃会话数（按账号 T）。分组容量页上游本就不分平台，设了上限的账号自动计入。

## 6. 可观测性

- **周期日志**（一行 INFO，10 分钟）：新落点里有余量段非空与为空的次数、确认登记不通过的次数、溢出次数、
  Redis 错误次数。
- **节奏决策记录**：`Rounds` 的每一轮增加 `session_full`（满额段的账号）与 `session_overflow`。它是 JSON 列，
  不需要迁移。
- **账号列表**：容量列的会话徽标即时显示活跃数与上限。

## 7. 接入点与改动面

| 位置 | 性质 | 内容 |
|---|---|---|
| `service/kong_openai_session_limit.go` | 新增 | 放行登记、确认登记、分段、WS 续期、计数身份的上下文、周期计数；setter 注入 `SessionLimitCache` |
| `service/openai_gateway_scheduling.go` | 上游文件，单行挂钩 | 第 1 层两处返回前放行登记；第 2 层分段；两处抢槽后确认；第 3 层分段与确认 |
| `service/openai_account_scheduler.go` | 上游文件，单行挂钩 | `previous_response_id` 续接与 guardian 亲和选中后放行登记；guardian 回退进入旧版选号前带入计数身份 |
| `service/openai_ws_forwarder_ingress.go` | 上游文件，单行挂钩 | ctx_pool 与 HTTP bridge 每轮续期 |
| `service/openai_ws_v2_passthrough_adapter.go` | 上游文件，单行挂钩 | passthrough 首帧与每轮续期 |
| `handler/openai_gateway_handler.go` | 上游文件，单行挂钩 | WS 转发前把建连选号的会话哈希放进上下文 |
| `service/openai_gateway_service.go` | 上游文件，追加字段 | 可选依赖字段 |
| `service/kong_openai_account_pace_select.go` | fork 文件 | 决策记录增加两个字段 |
| `cmd/server/wire_gen.go` | 手写装配 | 注入 `SessionLimitCache` |
| `handler/dto/mappers.go`、`handler/admin/account_handler.go` | 上游文件，单行挂钩 | DTO 导出；列表与详情的活跃会话数（逻辑在同包的 `kong_openai_session_limit.go`） |
| `frontend/src/features/openai-session-limit/` | 新增 | 输入框组件、extra 合并与账号判定的工具函数、单测 |
| `EditAccountModal.vue` | 上游文件，追加 | import、输入框、打开时取值、保存时合并 |
| `AccountCapacityCell.vue` | 上游文件，替换 | 会话徽标的显示条件与提示文案 |
| `i18n/locales/{en,zh}/admin/accounts.ts` | 上游文件，追加 | 输入框的占位与提示、OpenAI 版徽标提示 |
| 根 `Makefile` | 追加 | 前端单测加入 `FRONTEND_CRITICAL_VITEST` |

## 8. 边界情况

| 情况 | 结果 |
|---|---|
| 账号未设上限 | 不登记、不过滤、不确认，与没有本功能时相同 |
| 刚给账号设上限 | 已在它上面的会话在下一次请求时登记（活跃会话的请求间隔通常只有几秒），计数很快追上实际 |
| 上限调低到低于当前活跃数 | 已有会话照常服务，新会话不再优先落到该账号，直到活跃数降下来 |
| 会话空闲超过 T 后回到绑定账号 | 放行，重新登记，可能暂时超过上限 |
| 粘性绑定超过 1 小时失效后回来 | 当作新会话，按规则选号 |
| 绑定账号被限流或不可调度，粘性被清除 | 会话按新落点迁到别的账号；原账号上的登记 T 分钟后过期 |
| 请求在账号上失败、换号 | 原账号上的登记不删，T 分钟后过期（§3.4） |
| 有余量的账号在快照里、复核时已不可用 | 第 3 层用尽有余量段后进入满额段溢出，不因会话上限拒绝 |
| 两个新会话争同一账号的最后一个名额 | 确认登记决定先后，后到者试下一个账号；没有别的有余量账号时按溢出接纳，不拒绝 |
| WebSocket 连接期间改了上限或空闲超时 | 下一轮续期按新配置 |
| 同一新会话的多个首请求并发选号 | 可能分落多个账号，各自登记（§3.6） |
| 请求没有会话标识 | 不计数、不受约束 |
| Redis 不可用 | 失败开放，按不限制处理 |

## 9. 已知局限

- **软上限**：空闲回来的会话与溢出都会让活跃数超过上限。
- **计数偏高最多 T 分钟**：失败的尝试、单次借用、单请求的短会话（标题生成、脚本探测等每次都是新会话标识）都会
  留下登记直到过期。线上 3 天里单请求会话 63 个，其中 54 个来自脚本。
- **以请求开始为活动时刻**：单个请求超过 T 且期间同一会话没有其他请求时会被漏计。T=15 覆盖了线上全部请求时长；
  要精确到"仍在服务"需要跟踪每个请求的开始与结束，本功能不做。
- **口径是网关路由会话**：内容推导的会话只是近似；不同 API Key 携带相同原始标识会被合并计数。
- **覆盖范围**：只覆盖旧版选号的三层（§1）。
- **Redis 单独重启后失效到网关重启**：上游在网关启动时预加载计数脚本，批量查询在管道里执行、遇到脚本缺失不会回退重载。
  Redis 重启而网关没有重启时，批量查询持续缺项，会话上限一直失败开放（每分钟一条告警），重启网关后恢复。
- **批量短会话先撞线**：`codex exec` 每次运行都是新会话，成批执行时最先占满名额。
