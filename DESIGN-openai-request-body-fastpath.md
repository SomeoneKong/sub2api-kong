# OpenAI 请求体快速路径：跳过必然不改写的处理，复用顶层查找

**状态**：已实现，待代码 review 与发布；只用于本 fork，不向上游提 PR。开关 `KONG_OPENAI_BODY_FASTPATH`，默认开启。仓库组织与发布流程见
`FORK-NOTES.md`。

本文是通用设计说明，§0 的剖析数据来自一个部署实例。

## 0. 背景：大半 CPU 花在"处理完发现什么都不用改"上

这个部署实例的主要流量是 codex CLI 走 HTTP `POST /v1/responses`，流式，落在 OpenAI OAuth 账号上并走透传
（`forwardOpenAIPassthrough`）。每轮请求都带完整的对话历史，请求体平均约 1.1 MB，每个请求约耗 0.44 CPU 秒。

请求有两种形态，由客户端所用模型目录里的 `use_responses_lite` 决定，不能只看模型名推断。本部署客户端用的目录见
`codex-model-catalog.md`：

| 形态 | 模型 | 请求体特征 |
|---|---|---|
| 普通 | gpt-6-sol（约八成） | 顶层有 `instructions`、`tools`，`parallel_tool_calls` 随配置 |
| lite | gpt-6-astra（约两成） | 请求头 `X-OpenAI-Internal-Codex-Responses-Lite: true`。没有 `tools` 和 `instructions`，工具与基础指令放在 input 开头的 `additional_tools` 项和一条消息里；`parallel_tool_calls=false`，`reasoning.context=all_turns` |

codex 的顶层字段顺序是 `model, instructions, input, tools, …, stream, include, prompt_cache_key, text, client_metadata`。
除 `model`、`instructions` 外，查任何顶层字段都要先跳过约 1.1 MB 的 input；查一个不存在的字段，更要扫完整个顶层。

从 handler 到构造出上游请求，请求体要经过三十多道检查与改写。对上面两种请求，几乎每一道都不改写，却都要付出开销。
按剖析里"最深一层 sub2api 函数"归并，这些开销分三类：

| 类别 | 例子 | 占 sub2api CPU |
|---|---|---|
| ① 把整个请求体解码成 map 之后才发现无事可做 | handler 的两道 bootstrap（逐 token 查重键加完整解码，同一请求体共 4 遍）、lite 工具规范化、孤儿 output 清理、compaction trigger 排序、python 工具别名 | 约 28% |
| ② 同一件事做了多遍 | 工具 schema 清洗调用 2 次，每次对全文解析 2 遍；请求体 sha256 算 3 次，其中 2 次的结果用不上 | 约 15% |
| ③ 顶层查找的全文扫描 | 约 70 次查尾部或缺失的顶层字段（每次约 0.2%）；约 4 次顶层遍历；约 5 次 `gjson.ValidBytes`；sjson 每次改写前也要用 gjson 定位一遍；`gjson.GetBytes(body, "input")` 复制 1.1 MB 约 8 次 | 约 17% |

按叶子所在的包看，gjson 占 19.3%，Go 运行时占 16.2%。后者包括内存分配与 GC 扫描，其中 6.8% 是栈上没有
sub2api 帧的后台 GC；其余运行时开销已经计入上表各业务函数里。

## 1. 目标与约束

- 降低上述负载下每个请求的 CPU。偏向性能，但不为零点几个百分点增加复杂度和上游改动面。
- **发往上游的请求与现状逐字节一致**，请求头与请求体都一致，每一次重试、每一次换号都算。
- **对客户端的可见行为一致**：状态码、错误响应、流式输出，以及畸形或宽松 JSON 的处理结果都与现状相同。
- **观测面一致**：用量记录（含 `kong_request_features`）、ops 抓取、日志内容都不变。不新增逐请求日志。
- 其他路径（`/v1/alpha/search`、WS、Anthropic、grok 等）的行为不变，开销不变差。
- 同负载长时间运行时，峰值 RSS 不高于原版本。
- 这一轮只升级一次，所有改动一起上线，不依赖上线后再剖析、再迭代。所以正确性与收益都要在离线阶段拿到证据；
  上线后万一出问题，也要能只关掉出问题的那一部分。

## 2. 做法

两件事：

1. 在昂贵的处理前面加**判否门**，只在可证明原函数不改写时跳过（§3.1–3.6、§3.9–3.11）；
2. 让同一份请求体上的**顶层查找只扫一遍**（§3.7、§3.8）。

两者都不改任何一道处理的改写逻辑。

- **字节一致由结构保证**。
  - 门只在能证明原函数一定原样返回（请求体不变、`changed=false`、没有错误）时才跳过，判断不了就照常进入原函数。
    会改写的请求、会报错的请求走的仍是原代码，所以键序、转义、数字格式、错误文本、日志都不变。
  - 顶层查找的加速只改变"怎么找到"，返回的结果与原生 gjson 逐字段相同，包括 `Index`：sjson 靠它拼接字节。
    原生语义不能简单概括的情况（重复的顶层键、非简单路径、对标量值继续取子路径）一律交还原生逻辑，见 §3.8。
- **判定尽量调用上游自己的函数**，不在 fork 里复制规则。例如：
  - bootstrap 门把 input 项的投影交给上游的 `isCodexAutomationCandidate` / `isCodexDelegationCandidate`；
  - 孤儿 output 门在投影上跑原来的 `sanitizeOpenAIResponsesOrphanToolOutputs`；
  - lite 门在去掉 input 的顶层骨架上跑原来的 `normalizeOpenAIResponsesLiteTools`。

  上游改了规则，门会跟着变。门依赖的前提（"判定只读这几个字段"之类）各配一条守护测试，上游一改就会失败，见 §7。
- **门里的字符串照原函数的取值方式取值**。
  - §3.1–3.5 的原函数看到的字符串是 encoding/json 解码出来的，而 gjson 的反转义在边角上和它不同：
    - 孤立的代理项，例如 `"\ud800\u0061"`，encoding/json 解出 `U+FFFD` 加 `a`，gjson 只得到一个 `U+FFFD`；
    - 对非法 UTF-8，encoding/json 替换成 `U+FFFD`，gjson 原样保留。
  - 所以这几道门只借 gjson 或自己的扫描定位结构边界（请求体已确认是合法 JSON，边界没有歧义）。凡是要比较或交给
    上游判定的字符串，包括键名，按以下规则取值：
    - 原文里没有反斜杠、又是合法 UTF-8 的，直接用原文，这时它与解码结果逐字节相同；
    - 否则用 encoding/json 解码这一个字符串 token。
  - §3.9、§3.10 的原函数本身用 gjson 取值，门就按 gjson 的语义：原文没有反斜杠时，gjson 的取值就是原文；有反斜杠
    的一律回落，不在 fork 里复刻 gjson 的反转义。
  - §3.8 服务的是 gjson 自己的调用方，按 gjson 的语义。同一道门里两套语义不会混用。
- **跳过不能改变错误行为**。原函数遇到解码失败会返回错误的，门还要求请求体**可解码**才跳过，判据是
  `json.Valid(body)` 且顶层是对象。
  - Go 1.27 的 `encoding/json` 由 v2 实现，`Valid` 和 `Decoder` 都按 v1 的宽松语义（允许重复键、允许非法 UTF-8），
    嵌套深度上限也相同。
  - 所以这个判据恰好等于 `decodeOpenAIJSONUseNumber(body, &map)` 成功。不可解码的请求体会照常进入原函数、照常报错。
- **重复键一律回落**。解码成 map 时同名键取最后一个，gjson 取第一个。门扫描时发现相关位置有重复键，就判"要进原函数"；
  §3.8 发现顶层有重复键，就对这份请求体整体不加速。
- **不变量：请求体不被原地修改**。流水线上每道改写都返回新的切片：sjson 默认不原地改，仓库里也没有用它的原地替换
  选项；`json.Marshal` 生成新缓冲。仓库里现有的零拷贝视图 `parseRawJSONView` 已经依赖这一点，§3.7–3.12 也依赖它。
  端到端测试用 `PrereadBody` 把请求体原样交给 handler（读取时不复制），跑完核对这份切片没有变化；测试构建里
  登记表在注销时还会核对每个登记版本的校验和，发现原地修改就 panic。

## 3. 各项设计

判据里的"项"指 input 数组的元素；"字符串字段"指值的 JSON 类型是 string，取值规则见 §2。

### 3.1 handler 的两道 bootstrap

**现状**：`normalizeCodexCallOutputBootstrap`（`handler/openai_gateway_handler.go`）被 automation 和 delegation 各调用一次。
每次先用 `hasUniqueJSONMembers` 逐 token 遍历全文查重键，再用 `Decoder` 完整解码，然后才检查有没有候选项。
对普通 codex 请求，结果总是不改写。这两道约占 11.8%。

**跳过条件**：input 不是数组；或者 input 里没有任何一项同时满足下面三条：
- `type` 是字符串 `function_call_output`；
- `namespace` 是字符串；
- 用 `{type, namespace, name, output}` 的投影调用这一次传入的 `isCandidate`，结果为真。

**依据**：
- 原函数只有在至少一项 `isCandidate` 为真时才改写，出错一律静默返回原体，所以门判不了时交还原函数即可。门要求
  请求体可解码，只是为了让结构扫描可靠。
- 两个候选判定都要求 `namespace` 是特定的非空字符串。门据此只给带 `namespace` 的项取 `output`，其余项不必复制
  那些很长的工具输出。
- 重复键：原函数遇到任何层级的重复键都不改写，门怎么判都一致。
- 其余情况下，投影里各字段的取值与完整解码一致（§2）。

**接入**：`normalizeCodexCallOutputBootstrap` 函数头 3 行。

**守护测试**：两个候选判定对"去掉 `namespace` 的候选项"都返回 false。

### 3.2 lite 工具规范化

**现状**：lite 请求在 `Forward` 里进入 `normalizeOpenAIResponsesLiteToolsPayload`，先完整解码，再交给
`normalizeOpenAIResponsesLiteTools`。codex 的 lite 请求本来就满足 lite 形态，所以总是不改写。这一步约占 4%，
全部落在 astra 那两成请求上。

**跳过条件**：请求体可解码，顶层没有重复键，且满足以下之一：
- 顶层没有 `tools`；
- `tools` 是数组，但其中没有 `type`（按原函数的规则去掉首尾空白后）为 `namespace` 的项。

然后把**除 `input` 之外的顶层成员**逐个解码，组成骨架 map（这些成员都很小），在骨架上调用原来的
`normalizeOpenAIResponsesLiteTools`，返回"不改写且没有错误"时才跳过。

**依据**：
- `normalizeOpenAIResponsesLiteTools` 只有在 `tools` 里有 namespace 工具时才读 `input`（经
  `appendOpenAIResponsesLiteAdditionalTools`）；其余分支只读 `parallel_tool_calls`、`reasoning`、`tools`。
- 所以在骨架上的判定结果与在完整 map 上相同。
- 返回错误的情况回落原函数，由原函数给出同样的错误。
- 即使 namespace 的判断漏了某种写法，骨架上的原函数也会走到 namespace 分支。这个分支必然返回"改写"（或错误），
  门照样回落。按原函数的规则去空白，只是为了少走这一趟。

**接入**：`normalizeOpenAIResponsesLiteToolsPayload` 函数头 3 行。

**守护测试**：用一组 lite 请求体，分别在完整 map 和骨架上调用 `normalizeOpenAIResponsesLiteTools`，结果（含错误）相同。

### 3.3 孤儿 output 清理

**现状**：`normalizeOpenAIResponsesWebSocketCompatibilityBody`（`service/openai_gateway_request_body.go`）里，OAuth 账号
只要 input 是数组，就完整解码一次，然后调用 `sanitizeOpenAIResponsesOrphanToolOutputs`。典型请求没有孤儿 output，
所以不改写。这一步约占 3.9%。

**跳过条件**：
- 请求体可解码；
- 顶层 `input`、`previous_response_id` 都不重复；
- 每一项里 `type`、`id`、`call_id`、`name` 都不重复；
- 在由这四个字段构成的投影 input 上调用原来的 `sanitizeOpenAIResponsesOrphanToolOutputs`，结果为 false。
  - 字符串字段按 §2 取值；其他类型放一个非字符串占位值；非对象的项保持非对象。
  - `hasPreviousResponseID` 按原逻辑算。

**依据**：
- 这个函数只经 `firstNonEmptyString` 读这四个字段和 `previous_response_id`，所以投影上的结论与完整 map 相同。
- 投影里的字符串取值与完整解码一致（§2）。反例：`call_id` 分别为 `"\ud800\u0061"` 和 `"\ufffd"`，gjson
  会把两者解成同一个值；按 §2 的规则取值就不会。这组输入列入差分语料。
- `truncateOpenAIResponsesInputText` 与 `openAIResponsesInputMayNeedTruncation` 目前恒为 false。门只收窄
  `needsOrphanCleanup`，不碰截断这一支；上游以后启用截断，照样会解码。

**接入**：`needsOrphanCleanup` 赋值之后一行：`needsOrphanCleanup = needsOrphanCleanup && kong…(normalized)`。

**守护测试**：用一组含各种调用、输出、引用组合的请求体，分别在完整项和投影项上调用该函数，结果相同。

### 3.4 compaction trigger 排序

**现状**：`NormalizeCompactionTriggerInputOrder`（`service/openai_compact_body_signal.go`）在兼容处理的最后**无条件**
完整解码，只为找 `type == "compaction_trigger"` 的项。非压缩回合不改写，约占 4.2%。

**跳过条件**：
- 请求体可解码；
- 顶层 `input` 不重复，各项的 `type` 不重复；
- 且满足以下之一：input 不是数组或为空；没有 `type` 恰为字符串 `compaction_trigger` 的项；恰好一个，并且是最后一项。

**依据**：这就是原函数返回 `(body, false, nil)` 的全部条件。它用 `item["type"] == "compaction_trigger"` 比较，只有
字符串值才可能相等；门按 §2 取值后，做同样的精确比较。

**接入**：函数头 3 行。这个函数还被 handler 的 compact 分支和 WS 路径调用，门对所有调用方都等价。

### 3.5 python 工具别名

**现状**：`aliasOpenAIOAuthReservedToolNamesBody`（`service/openai_codex_tool_names.go`）只要全文出现（忽略 ASCII 大小写）
`python` 字样就完整解码，然后收集所有工具名，看有没有名字需要改成别名。编程对话几乎必然出现这个词，而真叫
python 的工具极少。这一步约占 4%。

**跳过条件**（在原有的字面量门之后再加一道）：
- 请求体可解码；
- 全文所有 JSON 字符串（键和值都算），解码后都不满足 `aliasOpenAIOAuthReservedToolName(s) != s`。

**依据**：
- 现有逻辑只有在某个工具名需要改成别名时才会改写或报错：名字冲突的错误也要以改成别名为前提。没有任何字符串
  满足这个条件时，必然返回 `(body, nil, false, nil)`。
- 门检查的是全部字符串，与工具名出现在哪个位置无关。上游以后新增工具名的位置，门也不会漏判。
- 原有的字面量门保留不动：它现在会放过转义拼写的名字（如 `"\u0070ython"`），去掉它行为就变了。

**实现**：一趟线性扫描。用 `bytes.IndexByte` 跳到字符串边界，每个字符串交给一个不分配内存的匹配器：
1. 按 encoding/json 的规则边读边解码：处理转义与代理对，孤立的代理项和非法 UTF-8 记为 `U+FFFD`；
2. 跳过开头的 `unicode.IsSpace`；
3. 接下来的字符按 `strings.EqualFold` 的规则逐个比对保留名常量 `codexReservedPythonToolName`；
4. 其后只允许空白。

遇到第一个不符的字符就结束。绝大多数字符串在头一两个字符就结束比对。

**接入**：函数头原有检查之后 3 行。

**守护测试**：对随机字符串（含各种转义、代理项、非法 UTF-8、Unicode 空白、大小写变体），匹配器的结论与
"用 encoding/json 解码后调用 `aliasOpenAIOAuthReservedToolName`"一致。

### 3.6 工具 schema 清洗的第二遍

**现状**：`sanitizeOpenAIResponsesToolSchemasForPlatform`（`service/openai_responses_tool_schema.go`）在 openai 平台上
先跑参数类型修复，再跑 lookaround pattern 删除。
- 两遍各自对全文做 `utf8.Valid`、手写解析（input 每项解析两次）和 `json.Valid`。
- 第二遍的预判只看全文有没有 `(`，codex 请求几乎必然有。
- 整条链路调用它两次（`Forward` 一次，透传的兼容处理里一次），共 4 遍全文解析，约占 9.7%。

**跳过第二遍的条件**：
- 平台两遍都要跑（即 openai）；
- 第一遍没有编辑（`changed == false`）；
- 请求体小于 8 MiB；
- 全文不含下列任何一种子串：
  - `\u0028`
  - `\u003f`
  - `\u003F`
  - 紧跟 `=`、`!`、`<`、`\` 之一的 `(?`

**依据**：
1. 选项只影响生成哪些编辑，不影响解析路径与校验。第一遍无编辑，说明它对同一份字节要么已经做完全部校验，
   要么在同一位置撞到了深度上限。
   - 两遍的解析路径相同，所以第二遍不会报错；撞深度上限时也同样原样返回。
   - 编辑数上限（1<<20）会让第一遍提前停下、跳过后面的校验。每次编辑至少要消耗约 11 字节原文，
     8 MiB 以下的请求体够不着这个上限。
2. 第二遍只删除解码后含 `(?=`、`(?!`、`(?<=`、`(?<!` 的 pattern。这几个字符在原文里要么是字面量，要么是
   `\u` 转义（JSON 只允许小写 `u`；`(` 的码点 `0028` 不含字母，`?` 的码点 `003f` 大小写两种都要列）。
   - `(` 或 `?` 被转义的，由前三条子串命中。
   - 两者都是字面量的，原文里必有 `(?`，后面紧跟第三个字符：字面的 `=`、`!`、`<`，或者转义开头的 `\`。
   - 所以上述判据是精确的超集。
3. 常见的非捕获组 `(?:` 不会命中。Go 生成的 JSON 里常见的 `\u003c`，只有紧跟在 `(?` 后面才会命中。
   这段会话的历史里写过 lookaround 或命名分组（`(?<name>`）时，门回落，行为与现在相同。

第一遍承担整体校验，不能跳过，保持原样。

**接入**：两遍之间 3 行。

**收益**：每次调用少一遍全文解析，两次调用合计约 4.5%，前提是会话历史里没有上述写法。

### 3.7 请求体登记表

§3.2–3.5 的门要用到几类事实，§3.8 的顶层查找加速也要用到。它们都要求"确定是同一份字节"，由一张登记表统一管理。

**登记点**：只登记流水线自己持有、生命期明确的请求体版本，而且只登记不短于 64 KiB 的。
- **入站版本**：handler `Responses` 读完请求体、判空之后，一行 `defer service.KongRegisterRequestBody(body)()`。
  - 所有经 Responses 入口的请求都会登记，不论最后落到哪个平台。
  - 对其他平台的影响只有两处：登记本身是常数开销；第一次查询时建一次索引，成本约等于一次顶层扫描，之后的查询
    一样变快。
- **出站前的版本**：`forwardOpenAIPassthrough` 在账号身份改写与指纹收敛（`applyCodexAccountIdentityClientMetadataRaw`、
  `applyCodexFingerprintClientMetadataRaw`）都做完之后、进入兼容处理之前登记。
  - 发送循环结束、开始转发响应之前就注销，另用 `defer` 兜底异常退出，共 3 行。
  - 所以这份请求体不会因为登记而在整个流式期间被保活。

**两个登记点之间会改写请求体的处理**：以下处理一旦改写，新版本就不再命中登记，照原来的方式处理，只是不加速。
- handler：reasoning effort 策略（分组配置）、bootstrap、渠道模型映射；
- `Forward`：lite 规范化、namespace 摊平（账号配置）、input namespace 清理、reasoning 回放、item id 清洗、按账号策略
  剥离生图工具；
- 透传：补默认 instructions（仅模型名含 codex）、OAuth 规范化。

对本部署的典型请求，它们都不改写，所以两个登记点覆盖了绝大多数查询。实际命中率由离线统计给出（§7），不假定全部
约 70 次查询都能加速。

**身份与生命期**：
- 以底层数组起始地址加长度识别。
- 登记期间表里持有这个切片的引用，这份内存不会被回收、也就不会被别的对象复用；请求体又不会被原地修改（§2），
  所以地址加长度唯一确定一份字节。
- 同一版本可以被登记多次（按引用计数），注销函数可重复调用。注销之后，注销函数本身不再引用条目：它常挂在
  `defer` 上，提前注销就结束了登记带来的保活。
- 注销之后还有 goroutine 在读这份请求体也没关系，它们走原来的路径。
- 入站版本本来就被 handler 引用到请求结束，登记不额外延长它的寿命。

**表里存什么**（都在首次用到时计算。除 §3.1 的候选投影外，不复制请求体里的任何内容，索引的键名直接引用请求体本身
的字节）：
- `json.Valid` 的结果。它的嵌套上限是 10000 层，递归深度有界；
- 可解码，即 `json.Valid` 为真且顶层是对象，供 §3.1–3.5；
- gjson 有效性，供 §3.8–3.10。只在 `json.Valid` 为真时才计算：gjson 的校验递归没有深度上限，而 compact 请求的原文，
  原代码只提取白名单字段、不做递归校验，快速路径不能先于原代码对嵌套极深的原文做这件事。`json.Valid` 为假时这一项
  记为"不判断"，§3.8 的钩子交还原生实现，§3.9、§3.10 的门回落；
- 顶层索引，供 §3.8：每个顶层键第一次出现的值的起止偏移。有重复键、或键名带转义时不建；
- input 判定：§3.4 的"trigger 已就位"与 §3.3 的"无孤儿 output"，一趟零拷贝扫描同时算出，只存两个布尔值；
- §3.1 的候选投影：input 里 `type` 为 `function_call_output`、`namespace` 为字符串的项，供 automation 与 delegation
  两次调用共用。codex 的普通请求里一项也没有。

**上限**：顶层成员超过 256 个时不建索引，这份请求体只是不加速。索引按成员逐个追加，超过上限就停止并丢弃，不会先
构造出一份无界的缓存。每个条目的大小与请求体长度无关，条目数与在途请求数成正比。

**并发**：表锁只用于查找和增删条目，计算事实与执行原生解析时都不持锁。每类事实各有一个 `sync.Once`。

门遇到未登记的请求体时，自己当场计算所需的事实，结论一样，只是不共用。§3.9、§3.10 的门例外：它们只对登记过的
请求体生效，未登记时直接回落。

### 3.8 gjson 顶层查找加速

**现状**：顶层查找每次都从头扫起。每个请求约有 70 次查尾部或缺失的顶层字段、约 5 次 `ValidBytes`，sjson 每次改写前
还要再定位一遍，合计约占 13%。散布在几十个调用点，逐个去改不划算。

**做法**：fork 自带一份 gjson v1.18.0，放在 `backend/third_party/gjson`，由 `go.mod` 的 `replace` 引用。
- 与原版的差别只有一组查询钩子，挂在两个入口：`Get` 和 `Valid` / `ValidBytes`。
- `GetBytes`、`GetMany`、`GetManyBytes`、`Result.Get` 最终都经过 `Get`，sjson 定位也调用 `Get`，所以都被覆盖。
- 副本同时提供不经过钩子的原生入口，供钩子实现内部使用。钩子实现不调用公开入口，所以不会重入自己，也不需要
  临时关掉钩子。
- 钩子由 fork 在启动时装上，逻辑在 `service/kong_*.go` 里。

**钩子只在以下情况全部成立时接管**，否则交还原生逻辑：
- 输入字符串恰好是一份已登记请求体的全文，起始地址与长度都相同；
- `json.Valid` 为真（嵌套不超过 10000 层），并且它是 gjson 意义上合法的对象，顶层没有重复键、键名都不带转义，
  索引已建成（没有超过上限）；
- 对 `Get`：路径由一段或几段用 `.` 连接，每段非空，且只含字母、数字、`_`、`-`。含通配、转义、查询、修饰符、管道的
  路径都不接管。

**接管时怎么算**：
- **`Valid`**：返回表里记下的结果。
- **`Get`**：用索引找第一段对应的顶层值。
  - 键不存在：返回空结果，与原生相同。
  - 只有一段：用原生解析重建这个值的结果，`Index` 按原生 `fillIndex` 的规则取它相对全文的偏移。
  - 还有后续段，且值是对象或数组：对这个值的子串调用原生 `Get` 取剩余路径。结果存在、并且落在全文之内时，把
    `Index` 换算成相对全文的偏移；否则 `Index` 为 0。
  - 还有后续段，但值是标量：返回空结果。原生 gjson 不会沿字符串值继续解析；而对子串直接查询，会把字符串里的
    `[`、`{` 当成结构。
  - `Indexes` 在这些路径下保持原生的 nil。

**等价性**：
- 原生 `Get` 对单段路径取第一个同名成员；对嵌套路径，第一个同名成员里没命中时，会继续查后面的同名成员。
  钩子只在没有重复顶层键时接管，这时两种情况都只有一个候选成员，所以结果一致。
- 已登记的请求体不可变，`Get`、`Valid` 的结果都只由字节和路径决定。
- 顶层遍历（`Result.ForEach`）不接管。它的结果还取决于接收者 `Result` 的 `Index`、`Indexes` 等字段，不只由字节决定；
  要回放，就得保存解码后的字符串，收益又只有约 0.8%。
- 逐字段等价由差分测试保证（`Type`、`Raw`、`Str`、`Num`、`Index`、`Indexes`），见 §7。

**不受影响的调用**：未登记的输入（响应流的事件、小 JSON）只多一次长度比较。输入不短于 64 KiB 时，再多一次表查找。

**收益**：每个已登记的版本只剩一次建索引的顶层扫描和一次 `Valid`，其余顶层查找都是常数时间。估计约 12–14%，
以离线统计的命中率为准。

### 3.9 input 项的元数据字段

**现状**：透传的 OAuth 兼容处理 `normalizeOpenAIOAuthResponsesCompatibilityBody` 最后一步，对 input 的每一项用 gjson
查 `internal_chat_message_metadata_passthrough`，有就删掉。codex 请求不带这个字段，于是每一项都要扫到尾才知道没有；
`input.Array()` 还要先为全部项建一份结果切片。透传段调用这个函数两次。

**跳过条件**：请求体已登记、在 gjson 意义上合法，input 是数组，并且没有任何对象项的顶层键等于这个字段名。键名带
反斜杠的，一律当作可能等于。

**依据**：
- 原逻辑 `item.Get(字段名)` 按 gjson 的键名语义比较：先反转义再比较。键名原文没有反斜杠时，反转义的结果就是原文，
  逐字节比较即可；带反斜杠的交还原逻辑。
- 路径只有一段，所以只看项的顶层键。嵌在 content 里的同名字段原逻辑不删，门也不看。
- 非对象的项原逻辑跳过，门也跳过。
- 只对登记过、gjson 合法的请求体生效，这时结构扫描的边界才可靠；其余请求体照原逻辑走。

**接入**：取出 input、确认是数组之后 3 行。开关名 `metadata`。

### 3.10 item id 清洗

**现状**：`sanitizeOpenAIResponsesInputItemIDs`（`service/openai_responses_item_id.go`）对每一项分别用 gjson 查 `type`、
`id`、`call_id`，缺失的字段每查一次就扫一遍该项；然后交给 `shouldStripOpenAIResponsesInputItemID` 与
`shouldStripOpenAIResponsesNonPairCallID` 决定删不删。codex 回放的 id 都合规，所以不改写。

**跳过条件**：请求体已登记、在 gjson 意义上合法；input 不是数组，或者每个对象项都通过下面的检查：
- 在一次遍历里取 `type`、`id` 第一次出现的值，记下有没有 `call_id`；
- 键名带反斜杠，`type` 存在但不是字符串或带反斜杠，`id` 是带反斜杠的字符串，都回落；
- 有 `call_id`，且 `shouldStripOpenAIResponsesNonPairCallID(去掉首尾空白的 type)` 为真，回落；
- `id` 是字符串，且 `shouldStripOpenAIResponsesInputItemID(去掉首尾空白的 type, id)` 为真，回落。

**依据**：
- 原函数用 gjson 取值，同名键取第一个；门也取第一个，判定交给原来的两个函数。
- 字符串原文没有反斜杠时，gjson 的取值就是原文。缺失的 `type` 取空串，与原函数的 `String()` 相同。
- `type` 不是字符串时，`String()` 会给出数字原文之类的值，门不去模拟，直接回落。
- `id` 不是字符串时，原函数不删 id，门也不看。
- 原函数只在至少一项要删时才改写，所以全部项都通过检查时，它必然原样返回。

§4 原先把"单项内合并查找"列为候选。实施时改成了这道门，省下的不只是重复查找，还有整个 `ForEach` 与每项的结果构造。

**接入**：函数头 3 行。开关名 `item_ids`。

### 3.11 空 base64 图片检查的预判

**现状**：`openAIRequestBodyMayContainEmptyBase64InputImage` 先用 `openAIRequestBodyMayContainInputImageToken` 预判：原文有
字面的 `input_image` 就进入完整扫描；没有的话，只要原文有 `\u` 也进入，因为任何字符都可能写成 `\u` 转义。工具
输出里的终端控制字符会被写成 `\u001b` 之类的转义，这样的请求每次都要做完整扫描：用 gjson 取出 input，再逐项、逐个
content 查找。

**改为**：原文没有字面的 `input_image` 时，只在出现 `\u005f`、`\u005F`，或以 `\u006`、`\u007` 开头的转义时才进入
完整扫描。

**依据**：
- 完整扫描找的是解码后 `type` 等于 `input_image` 的项。原文没有这个字面量却能解码出它，说明其中至少一个字符写成了
  `\u` 转义。
- `input_image` 的字符都是小写字母或下划线，码点是 0x5F 或落在 0x61–0x7A，转义写法必然是上面几种之一。
- 检查时逐个看反斜杠后面的字节，所以被转义的反斜杠后面跟着的 `u006…` 这类字面文本也会命中。只会多判，不会漏判。

**接入**：原预判里、字面量检查之后 3 行。开关名 `image`。

### 3.12 其余机械修复

以下几项的等价性都能直接看出来。改动的上游代码行都带 `// [kong]` 标记：

1. **handler 的请求体哈希只算一次**。`Responses` 在每次尝试时都把 `HashUsageRequestPayload(body)` 作为
   `recordCyberPolicyIfMarked` 的实参，提前求值；没有 cyber 标记时这个结果直接丢弃。结束时 `submitResponsesUsage`
   又算一次。
   - 这两处用的是同一个 `body`，循环里不会重新赋值。
   - 改成在循环前算一次，两处共用。
2. **不再复制请求体**。热路径上有 7 处复制了整个请求体或整个 input，分两类改：
   - 6 处 `gjson.GetBytes(body, "input")`，改为对请求体的只读字符串视图直接调用 `gjson.Get`：
     - `HasCompactionTriggerInInput`：handler 调用两次；
     - `normalizeOpenAIOAuthResponsesCompatibilityBody`：透传段调用两次；
     - 孤儿清理前的 `IsArray` 判断；
     - input 形态检查；
     - `openAIRequestBodyMayContainEmptyBase64InputImage`；
     - `stripOpenAIResponsesInputNamespaces`。

     `GetBytes` 本来就是先这样调用 `Get`，再把结果复制一份，所以去掉复制后结果完全相同。**不能换成
     `parseRawJSONView(body).Get(...)`**：它先做 `Parse`，遇到前面带垃圾字节的输入会返回空，而 `Get` 仍能找到
     后面的对象。handler 的 compact 信号检测在 `ValidBytes` 之前运行，正会遇到这种输入。
   - `applyCodexAccountIdentityClientMetadataRaw` 里只为判断"是不是对象"而做的 `gjson.ParseBytes`，改为
     `parseRawJSONView`。`ParseBytes` 就是先复制再 `Parse`，两者只差这一次复制。

   两类改动取到的值都与原来相同，只是 `Raw` 与请求体共用内存，依赖 §2 的不变量。
3. **宽松 JSON 归一化先做预判**。`NormalizeLenientJSONRequestBody` 逐字节走状态机，只会把字符串里的原始控制字节
   （0x00–0x1F、0x7F）改成 `\u00XX`。
   - 状态机之前还有三步：确定默认大小上限、去掉 UTF-8 BOM、检查去 BOM 后的长度（超限返回 `http.MaxBytesError`）。
     这三步保持原样。
   - 预判放在这三步之后，只替代状态机：去 BOM 后的正文里一个控制字节都没有时，状态机必然原样返回，于是直接返回这份
     已去 BOM、已检查长度的切片；有控制字节就照原逻辑走。
   - codex 发的是紧凑 JSON，字符串里的控制字符都已转义，通常一个都没有。

4. **schema 清洗的字符串扫描成块跳过**。手写解析器的 `parseString` 逐字节走分支，普通字节只做一次 `p.pos++`。改为
   遇到普通字节时，一次跳过后面连续的普通字节，停在第一个引号、反斜杠或控制字符（小于 0x20）上，由原来的分支照原样
   处理。
   - 普通字节在原循环里只是前进一位，所以停下的位置就是逐字节前进时下一次进入其他分支的位置，报错位置与文本都不变。
   - 每次看 8 个字节，用位运算判断其中有没有这三类字节。这个判断作为"有没有"是精确的，有就退回逐字节。
   - §4 原先把它列为候选；它对 §3.6 跳不过去的第一遍同样有效。

前三项合计约 4%：sha256 从 3 次减到 2 次，约 1.1%；少复制 8 次大块内存；宽松归一化约 1.2%。第 4 项与各项的实测见 §8。

### 3.13 zstd 读出站请求体时预分配

`kongZstdReadBody` 经 `GetBody` 读明文时用 `io.ReadAll`，会从小缓冲一路倍增扩容。改为用请求的 `ContentLength` 作为
预分配提示：此时请求尚未压缩，这就是明文长度。
- 这个长度只用来预分配，不限制读取量：偏小会继续扩容，未知则不预分配，读取失败时照旧按明文发送。
- 压缩参数与输出字节不变。

### 3.14 本轮不做：流式响应合并刷写

**现状**：透传流式转发在每个 SSE 事件的边界都 `Flush` 一次（`handleStreamingResponsePassthrough`）。codex 的响应由大量
很小的增量事件组成，每次 `Flush` 都是一次写出和一次系统调用。剖析里 `opsCaptureWriter.Flush` 占 2.4%，它只是转手
调用底层的 `Flush`；内核的约 5% 里也有一部分来自这里。本机的 xray 要逐个转发这些小块，同样多耗 CPU。

**做法**：合并刷写。
- 首个可见输出、终止事件（`response.completed`、`response.failed`、`error`、`[DONE]`）照旧立即 `Flush`。
- 其余事件写入后，在不超过一个短窗口（例如 20 ms）内合并成一次 `Flush`。
- 心跳与首输出前 keepalive 的逻辑不变。

**影响**：客户端收到的字节序列不变，只是中间事件最多晚一个窗口到达。首 token 时间照原来记录，因为首个可见输出
仍然立即刷出。

**收益**：粗估 sub2api 降 1–4%，外加 xray 的一部分。事件数分布没有实测数据，所以区间很宽。

这一项改变了流式输出的时序，不在"逐字节一致"约束的字面范围内，但属于客户端可感知的行为。需求方决定本轮不做，
这里留作以后的选项，不计入本轮的实施与收益。

## 4. 不做的与理由

| 方案 | 不做的理由 |
|---|---|
| 只解析一次，全流水线共用一份 map，最后统一序列化 | 会改写的请求字节会变（键序、HTML 转义、数字格式），还要改几十个上游函数 |
| 逐个改调用点，把顶层查找换成索引 | 要改几十个调用点。§3.8 在 gjson 入口统一接管，调用点一个不改 |
| 记录"第一轮已无须处理"的证明，第二轮核对依赖的字段没变就跳过（OAuth 兼容、reasoning 回放、item id、reasoning mode 等） | 两轮之间确有相关改写（`prompt_cache_key` 换成账号派生值、lite 工具迁移、input 形态转换），证明要逐组记录依赖的字段与账号、模型条件，上游一改就可能失效。这些处理的开销主要是顶层查找，已由 §3.8 拿到；剩下的逐项扫描约 1–2%，不值得追踪依赖 |
| input 各项的键也建索引 | 逐项的查找本来就要扫一遍该项；建索引同样要扫一遍，只有同一项被查多次时才省。剩余约 3% 的逐项扫描里能省下的不多，内存却与项数成正比 |
| 跳过透传兼容处理里的第二次 schema 清洗（第一遍） | 它对整个请求体做校验。要跳过，就得证明两个版本之间只改了工具以外的部分、而且改后仍然合法，证明依赖上游各步的写法 |
| schema 清洗第一遍加预判 | 它承担整体校验，报错语义依赖它 |
| 换 JSON 库 | 边界情况的语义和报错都不同，内存也会涨 |
| 降低 zstd 压缩级别或不压缩 | 出站字节会变；压缩参数是按官方客户端的行为定的 |
| 响应流里逐事件的其余变换（命名空间还原、工具名还原、参数规范化等） | 没有清点过，收益不明。§3.14 只动刷写 |
| 防重试死循环的 sha256 推迟到第一次需要时再算 | 只省一次整份请求体的 sha256，折合基线约 0.5%。推迟就得在重试状态里留着初始请求体的引用；compact 换模型回退会直接替换请求体、不经过重试状态，旧版本会被这份引用保活到这次尝试结束，连同流式转发期间。要避免，就得在 HTTP 的六处回退点逐一补释放，WS 的构造路径还要另外分开，改动面与收益不相称 |
| 票据观测读请求体时预分配 | 只在响应带 turn-state 时才读，路径少见；而且读时请求长度可能已是压缩后的长度，拿不到可靠的提示 |

**实施阶段比较过的候选**：取舍办法是在完整生命周期的离线负载（§7）上测边际收益，不少于基线 1% 的才纳入；纳入的同样
要有差分测试与开关归属。

| 候选 | 结论 |
|---|---|
| `sanitizeOpenAIResponsesInputItemIDs` 单项内合并查找 | 改成了更彻底的判否门，见 §3.10；实测边际见 §8 |
| schema 清洗手写解析器的字符串扫描成块跳过 | 纳入，见 §3.12 第 4 项。它没有开关，与其余机械修复一样由三方对照保障 |
| `stripOpenAIResponsesInputNamespaces` 延迟构造输出 | 不纳入。codex 请求没有 `"namespace"` 字面量时，原有的字面量预判就直接返回；新版本的剖析里它约占 1.3%，折合基线不到 0.5% |
| 响应流里的生图状态规范化 | 不纳入。新版本的剖析里低于采样阈值 |
| 合并各道门对 input 的逐项扫描（bootstrap 候选、input 判定、元数据、item id 各扫一遍） | 不纳入。四趟合计约占新版本的 8%，合并成一趟最多省下其中一大半，折合基线约 1%；却要把四道门的判定揉进同一趟扫描，各自的开关也就分不开了 |

## 5. 开关与应急关闭

| 变量 | 取值 | 行为 |
|---|---|---|
| `KONG_OPENAI_BODY_FASTPATH` | 未设置，或 `strconv.ParseBool` 认作真的值 | §3.1–3.11 全部开启 |
| | `strconv.ParseBool` 认作假的值 | 全部关闭，走原路径 |
| | 空值或其它值 | 记一条错误日志，全部关闭 |
| `KONG_OPENAI_BODY_FASTPATH_OFF` | 逗号分隔的名字：`bootstrap`、`lite`、`orphan`、`trigger`、`alias`、`lookaround`、`index`、`metadata`、`item_ids`、`image` | 只关掉列出的项；未知名字记一条错误日志后忽略 |

- `index` 指 §3.8 的 gjson 钩子。钩子在启动时总是装上；关掉后它不接管任何查询，只多一次开关判断。登记表仍供各道门
  使用。全部项都关掉时，请求体也不再登记。
- 两个变量都用 `sync.OnceValue` 在首次使用时读一次。首次使用时（即第一个请求到来时）打一行 info，列出实际生效的项。每个请求不打日志。
- **改开关要重建容器**：改 compose 的环境变量，然后重建容器，不需要换镜像。重建会中断在途的流式请求，所以要选低峰
  操作。操作后看第一个请求之后的日志里生效的项，确认改对了。
- **原因不明时直接全关**，不要求先定位是哪一项。定位得到时，才只关那一项，其余收益保留到下一次升级。
- §3.12、§3.13 没有开关，全关后仍然生效。它们的保障是 §7 与原版本的对照。

## 6. 切分

### 6.1 提交

不给上游提 PR，所以不单列"可提给上游的修复"，全部按 fork 的提交次序：

| 提交 | 内容 | 位置 |
|---|---|---|
| fork 适配 | `backend/third_party/gjson`：原样引入 v1.18.0，并把查询钩子与原生入口作为单独一段改动；`go.mod` 的 `replace`；根目录与 `backend/` 两个 Dockerfile 在 `go mod download` 之前先复制 `third_party/`（本地 `replace` 的模块在下载依赖时就要能读到） | 适配提交之后 |
| zstd 的 fixup | §3.13，`--fixup` 并回 zstd 压缩的后端提交 | 该提交所在位置 |
| 功能后端 | `service/kong_openai_body_fastpath.go`（开关）、`kong_openai_body_registry.go`（登记表与 gjson 钩子实现）、`kong_openai_body_gates.go`（§3.1–3.6、§3.9–3.11 的门；§3.1 的判定也在这里，由 handler 调用）、`kong_testhooks_unit.go`（只在测试构建里编译的开关替换）、`pkg/httputil/kong_body.go`（§3.12-3 的预判）、`pkg/kongcorpus`（合成语料）、各处接入、§3.12 的修复、对照测试基建与对照基准、差分与守护测试、基准 | 后端提交之后 |
| 文档 | 本文，以及 `FORK-NOTES.md` 的维护约定（含 gjson 副本的升级方法） | 最末 |

每个提交都要能独立编译、通过已有测试。gjson 副本单独成一个提交，这样功能提交的 diff 里只剩 fork 自己的逻辑，
以后升级 gjson 时也只需重做这一个提交。副本是嵌套的 module，父模块的 `go test ./...` 不会跑到它，它自己的测试要在
本地闸门里单独跑。

### 6.2 实施顺序

1. **先搭测试基建，改代码之前就在原代码上跑通**：
   - 按 codex-rs 的 `ResponsesApiRequest` 结构写合成语料生成器，固定所依据的模型目录；
   - 端到端对照测试：在原代码上录下**对照基准**（§7 列的全部输出）；
   - 基准测试：在原代码上记下构造阶段的 ns/op、B/op、allocs/op，以及完整生命周期负载的每请求进程 CPU（§7）。
2. 引入 gjson 副本，钩子留空。全部已有测试、副本自身的测试与对照基准都必须不变；源码 Docker 镜像与 GoReleaser
   snapshot 都要能构建。
3. zstd fixup 与 §3.12 的修复，逐字节对照原版本基准。
4. 登记表，然后 §3.8 的钩子，带差分测试。
5. 各道门逐个加入，每道门带自己的差分测试与守护测试。
6. 全量对照：原版本基准、开关关、开关开三方比较；再做基准对比与长时间运行的内存对比。
7. 代码 review 并修复。
8. 按 §8 的发布判定检查。
9. **升级前**在线上测一次基线：按需求文档附录的方法，连续至少 1 小时的每请求 CPU、RSS，再剖析一次。
10. 按 `FORK-REPO.md` §7 发布，完成这一次升级。
11. 升级后同样测至少 1 小时并剖析，对照 §8 的预估。观察 §8 列出的异常信号，按 §5 处置。

## 7. 验证

**差分测试与 fuzz（每道门）**：断言"门判定跳过 ⟹ 原函数返回 `(body, false, nil)`"。语料覆盖：
- 两种请求形态的真实结构，大小、input 项数、工具输出与工具定义都按分布生成；
- 重复键；转义写法的键和值（如 `"typ\u0065"`）；孤立代理项、代理项后接普通 `\u` 转义、非法 UTF-8；input
  不是数组；嵌套超过一万层；字段类型不对；
- 真会改写的样例：
  - automation 与 delegation 的输入；
  - 孤儿 output，含 §3.3 的代理项反例；
  - trigger 不在末尾或有多个；
  - 名为 python，或带空白与大小写变体的工具；
  - 带空白的 `" namespace "` 工具类型；
  - 需要修复的参数 schema；
  - 四种 lookaround 的每个字符分别取字面量或 `\u` 转义的全部组合，以及普通括号、`(?:`、命名分组。

**gjson 钩子的差分测试与 fuzz**：
- 对登记过的请求体，比较钩子开、关两种情况下每个入口的返回值，逐字段比较 `Type`、`Raw`、`Str`、`Num`、`Index`、
  `Indexes`。
- 覆盖的入口：`Get`、`GetBytes`、`GetMany`、`GetManyBytes`、`Result.Get`、`Valid`、`ValidBytes`。`Indexes` 要区分
  nil 与空切片。
- 路径取流水线里实际出现的全部路径，加上随机生成的路径：含通配、转义、查询、修饰符、数组下标、空段、不存在的键。
- 请求体覆盖以下情况：
  - 重复键，包括因转义写法而重复的键；
  - 同名成员里第一个缺子路径、后一个命中（如两个 `reasoning`，只有后一个带 `effort`）；
  - 第一段命中 `null`、数字、字符串；
  - 字符串里含有数组或对象样子的文本（如 `{"a":"[123]"}` 取 `a.0`）；
  - 非对象的顶层；gjson 判为不合法的输入；顶层成员超过上限。
- sjson 的 `SetBytes` / `DeleteBytes` 在登记过的请求体上，结果与钩子关闭时逐字节相同。按默认调用和仓库里实际
  用到的写法各测一遍。
- 钩子实现内部走原生入口，不重入自己：在回调与嵌套查询里再调用 `Get`、`Valid`，不死锁、结果不变。

**登记表**：
- 生命期：注销之后同一地址上的新内容不会命中；同一版本多次登记、交错注销、重复调用注销函数，结果都正确。
- 覆盖率：离线统计各阶段的登记命中、未命中与建索引次数。配置组合包括：指纹收敛关闭及各开启模式、渠道模型映射、
  lite、换号、字段重试。§8 的收益按这组数据重算。
- 内存：长时间并发流式、登记后又被改写、巨大的字符串 input、超多顶层键、取消与换号。除峰值 RSS 外，还要看流式
  等待期间与请求结束后的存活堆。
- 并发：首次计算与注销交错、同一版本并发首次访问，跑定向的 race 测试。

**§3.12 的机械修复**：
- 零拷贝取值：前导垃圾字节、多个顶层值、前导空白、截断输入，以及前导垃圾字节加 compaction trigger 的 compact
  场景。比较取值、走的路径、错误、日志与是否向上游发送。
- 宽松归一化：只有 BOM、BOM 加紧凑 JSON、没有控制字节但超限、去 BOM 前后恰好跨过上限、默认上限。比较返回的
  字节、错误类型与 `Limit` 字段。

**守护测试**：见 §3.1–3.5，锁住各道门对上游判定函数的前提。

**端到端对照（三方）**：从 handler 进入，经 `Forward`、透传，到 httptest 上游。
- 同一批语料跑三遍：原版本基准（§6.2 第 1 步录下）、开关关、开关开。
- 逐项比较以下输出：
  - 是否发送、发送几次；
  - 每一次出站请求的请求头、明文请求体、压缩后的字节；
  - 客户端收到的状态码与响应体；
  - 用量记录的各字段（含请求体哈希与 `kong_request_features`）；
  - ops 抓取的内容；
  - 业务日志条目。
- 覆盖的路径：
  - 成功；
  - 上游 400 触发字段重试；
  - failover 换号；
  - zstd 被拒后明文重发；
  - 响应不带票；
  - 在发出上游请求之前就失败的请求（校验失败、解码失败）。
- 逐次随机的值，优先固定随机源。实在要屏蔽的字段逐项列出，并继续核对它的格式与派生关系。
- 流水线跑完后，核对入站请求体与各中间版本没有被原地修改（§2 的不变量）。

**性能测量**：分两层，都在原版本、开关关、开关开三个版本上各测一遍。只比较开关关与开关开，会漏掉 §3.12、§3.13 的
收益。
- **构造阶段微基准**：从 Responses handler 到构造出上游请求。语料在计时之外生成，每次迭代用不同的请求体，模拟
  对话持续追加历史。报告 ns/op、B/op、allocs/op，用于定位各项的贡献。
- **完整生命周期负载**：这是发布判定用的口径。
  - 包括出站 zstd 压缩、合成 SSE 响应的转发、用量任务完成与 GC。
  - 用进程 CPU 时间（或 cgroup 的 CPU 增量）除以完成的请求数。
  - 两种形态分别测出每请求 CPU 时间，再按八二的请求比例加权原始时间，不直接平均百分比。
- **敏感性**：历史里有无 lookaround、指纹收敛的各配置、不同请求体长度、不同并发度分别报告。没有真实频率数据的
  组合，结果标为假设。
- **回落路径**：正文只是提到 python 一词；真有名为 python 的工具；历史里有 lookaround。
- **其他路径**：WS、API key 账号、其他平台各跑一组，确认不劣化。
- **内存**：长时间持续负载下，对比原版本与新版本的峰值 RSS。

**本地闸门**：`FORK-REPO.md` §7.1。离线验证不使用线上请求体。

### 7.1 实施后的覆盖情况

| 验证项 | 落在哪里 | 结果 |
|---|---|---|
| 三方对照 | `handler/kong_openai_body_fastpath_e2e_test.go`，53 个用例：两种形态、不同大小、各类边界片段、嵌套上万层的未知字段、字段重试、换号、zstd 被拒、带票、发送前就失败；对照基准在原代码上录制 | 快速路径关、开两遍都与基准零差异 |
| 入站请求体不被原地修改 | 同一文件的 `TestKongOpenAIBodyFastpathInboundNotMutated`：全部用例以 `PrereadBody` 交给 handler，快速路径关、开各跑一遍，跑完逐字节比较 | 通过；临时加一处原地修改，快速路径关闭时即被抓出 |
| 各道门、gjson 钩子与 sjson 的差分 | `service/kong_openai_body_fastpath_test.go` 的 `TestKongOpenAIBodyFastpathMatchesOriginal`：400 份请求体，逐一在快速路径开、关下跑挂了门的各个原函数，以及 41 条路径的 gjson 查找、10 种 sjson 改写，结果逐项相同；同时核对每道门判"跳过"时，原函数确实原样返回 | 零差异。每道门在语料里都既判过跳过、也判过回落 |
| fuzz 与随机变异 | `FuzzKongOpenAIBodyFastpath` 把任意片段放进登记大小的请求体与小请求体，做上一行的全部检查；`TestKongOpenAIBodyFastpathRandomMutations` 用固定种子对语料片段做 300 次随机插入、删除、复制，检查相同 | 种子与 300 次变异都通过；另用不插桩的方式跑过 5000 次随机变异，未发现差异。本机上带覆盖率插桩的原生 fuzz 每秒只有几次执行（5 分钟约 530 次），更长时间的 fuzz 可以放到 Linux 上跑 |
| bootstrap 门 | `handler/kong_openai_body_fastpath_bootstrap_test.go`：automation 与 delegation 在开、关下结果相同；语料里既有被跳过的，也有真会改写的 | 零差异 |
| 守护与性质测试 | 候选判定要求 `namespace` 是字符串；投影上的孤儿清理结论与完整解码逐一相等；lite 骨架上的结论由差分覆盖；别名匹配器与"解码后调用原判定"一致（随机 2 万组）；lookaround 与图片预判从不漏判（各随机 5 万组，另加四种 lookaround 的全部转义拼法）；8 字节扫描与逐字节扫描一致；gjson 钩子在重复键、转义键名、标量取子路径、字符串里像数组的文本、成员上限前后、遍历回调里再查询时都与原生一致 | 通过 |
| 登记表 | 生命期、同址不同长度、全关不登记、原地修改检测、注销后注销函数不再保活条目（weak 指针加 GC）、16 个 goroutine 的并发登记与查询 | 通过。本机没有 cgo 工具链，`-race` 没有跑，可以用 `kong-race.yml` 在 Linux 上补跑 |
| 嵌套极深的请求体 | `TestKongDeepBodyLeftToNative`：子进程里把栈上限压到 16 MiB，对嵌套 20 万层的登记请求体走查询、钩子与各道门 | 通过；去掉 `json.Valid` 前提、或 bootstrap 门改回 gjson 校验时，子进程都以栈溢出退出 |
| 变异检查 | 逐个把门改成恒判跳过、去掉转义回落、改成取最后一次出现的值、放宽 trigger 的位置判断、改错 `Index` 等，确认上面的测试会失败 | 非等价的变异全部被抓出。没抓出的两个（钩子里"标量不再下钻"、lite 门的 namespace 前置判断）经分析是等价变异，属于防御性写法 |

离线没有做的：
- 登记命中率的逐阶段统计：由 §8 的整体测量代替。§7 列的其他配置组合（指纹收敛的各模式、渠道模型映射、字段重试、
  换号）只在三方对照里验证了正确性，性能没有单独测。
- 长时间负载下的峰值 RSS：离线只测了每请求的分配（§8，降到原来的约七分之一），RSS 在升级前后的线上测量里对比
  （§6.2 第 9、11 步）。
- WS、API key 账号、其他平台的负载对比：这些路径上的请求体不登记，门遇到未登记的请求体时自己计算所需的事实。回落时
  多出一次 `json.Valid` 与一趟结构扫描，比跳过时省下的完整解码便宜得多，没有单独测。

## 8. 预估收益、发布判定与风险

**预估**（按"最深一层 sub2api 函数"口径，已含各自的分配开销，不再另加一份 GC 收益）：

| 项 | 降幅 |
|---|---|
| §3.1 bootstrap | ~11% |
| §3.2 lite | ~3.5% |
| §3.3 孤儿 output | ~3.7% |
| §3.4 trigger | ~4% |
| §3.5 别名 | ~3.5% |
| §3.6 schema 第二遍 | ~4.5% |
| §3.7、§3.8 登记表与 gjson 顶层查找 | ~12–14%，按离线统计的命中率重算 |
| §3.12 机械修复 | ~4% |
| §3.13 | <1% |
| 登记表、索引与门自身的开销 | −1.5% |

- 合计约 42–47%。另外，后台 GC 的 6.8% 会随分配减少而下降，可能再多几个百分点。
- 每请求约 0.23–0.26 CPU 秒。
- 这个区间还依赖三件没有实测的事：登记命中率、各道门的命中率、新增扫描的成本。以完整生命周期负载的测量为准，
  预估不是承诺。
- §3.14 与 §4 的候选都未计入。

**实测**：基准 `BenchmarkKongOpenAIBodyFastpath` 走完整生命周期：handler → `Forward` → 透传 → zstd 压缩 → 假上游 →
转发 SSE → 写用量。
- 请求体是约 1.1 MB 的合成请求，8 份轮换；`-cpu=2`，每项 40 次迭代，3 轮取中位数。
- CPU 按 runtime/metrics 的用户代码加 GC 统计。每次迭代复制请求体的开销也算在内，三个版本相同。

| 场景 | 原版本 | 开关全关 | 开关全开 | 降幅 | 每请求分配（原版本 → 全开） |
|---|---|---|---|---|---|
| sol，响应只有终止事件 | 88.2 ms | 82.9 ms | 21.3 ms | 76% | 57.1 MB → 7.9 MB |
| lite，响应只有终止事件 | 100.1 ms | 82.7 ms | 23.1 ms | 77% | 63.6 MB → 8.0 MB |
| sol，400 个增量事件 | 100.1 ms | 85.9 ms | 21.3 ms | 79% | 57.2 MB → 8.3 MB |
| lite，400 个增量事件 | 97.0 ms | 82.4 ms | 22.8 ms | 76% | 64.0 MB → 8.7 MB |
| sol，正文提到 python | 98.9 ms | 82.9 ms | 20.6 ms | 79% | 56.8 MB → 7.7 MB |
| sol，真有名为 python 的工具（别名门回落，请求体被改写） | 104.4 ms | 88.8 ms | 30.7 ms | 71% | 63.3 MB → 17.5 MB |
| sol，历史里写过 lookaround（schema 第二遍回落） | 100.5 ms | 85.2 ms | 24.0 ms | 76% | 56.8 MB → 7.8 MB |

- 按八二加权（取 400 个增量事件那两行），每请求约 99.5 → 21.6 ms，降约 78%，远高于预估的 42–47%。预估按"最深一层
  函数"归并，没有计入跳过完整解码后连带省下的分配与 GC；实测分配降到原来的约七分之一。
- 开关全关比原版本低一成左右，这是 §3.12、§3.13 不带开关的部分。各场景之间差得较多（sol 无增量事件那一行的原版本
  偏低），单次测量的噪声不小，只能看量级。
- 逐项关闭的边际，即相对全开增加的毫秒数（sol / lite）：

  | 项 | sol | lite |
  |---|---|---|
  | `index` | +23.5 | +24.9 |
  | `bootstrap` | +13.8 | +15.4 |
  | `alias` | +6.7 | +10.8 |
  | `trigger` | +4.6 | +8.8 |
  | `lookaround` | +3.2 | +8.2 |
  | `metadata` | +5.6 | +6.8 |
  | `orphan` | +4.9 | +6.6 |
  | `item_ids` | +2.4 | +5.6 |
  | `lite` | 不适用 | +6.4 |
  | `image` | 噪声以内 | +2.9 |

  各项相加不等于总降幅：关掉一项，后面的处理可能拿到改写过的请求体或重新解码，项与项之间有交互。单次测量的噪声约
  ±1.5 ms。
- 剩下的两大块是 zstd 压缩（约占全开版本的 16%）与 schema 清洗第一遍（约 14%），都按 §4 的理由保持原样。
- 这是离线的合成负载，线上的比例以升级前后的测量为准（§6.2 第 9、11 步）。

**对容量的意义**：按需求文档 §1.5 的粗算口径：
- 现状：整机每个请求约 0.5 CPU 秒，其中 sub2api 约 0.44；扣掉空载后，基线内可持续约 800 个请求/小时。
- 升级后：整机约 0.29–0.32 CPU 秒，可持续约 1,250–1,380 个请求/小时。
- 日均负载约 1,380 个请求/小时。要在日均负载下不再消耗突发额度，sub2api 的降幅需要达到约 47%。预估区间的上端
  刚好够到这条线：降到 42% 时还差一些，降到 47% 时刚好够。
- 高峰 2,400 个请求/小时，不论哪一端都高于基线，靠突发额度吸收。
- 按离线实测的约 78% 外推（未经线上验证）：整机约 0.16 CPU 秒/请求，可持续约 2,500 个请求/小时，高峰也在基线以内。

**发布判定**（需求方已定：正确性零差异，性能有明显优化即可发布，不设固定降幅门槛）：
- 三方对照零差异，覆盖全部语料与路径：离线已满足（§7.1）；
- gjson 钩子差分零差异：离线已满足；
- 完整生命周期负载下，两种形态加权的每请求 CPU 明显下降：离线已满足，约 78%；
- 其他路径不劣化：离线没有单独测，理由见 §7.1；
- 长时间负载下峰值 RSS 不高于原版本：离线只测了分配（降到约七分之一），RSS 待升级前后的线上测量对比。

约 47% 的容量线只作参考，不是发布条件。

**风险**：
- **门误跳过**：会漏掉一次本该做的规范化。上游可能拒绝请求，也可能照常返回 200，只是处理的历史或约束与原来
  不同，线上未必看得出来。所以主要防线在离线：
  - 门只在可证明时跳过，由差分测试、fuzz 与三方对照验证；
  - 对上游判定函数的前提有守护测试。

  线上观察以下异常信号：上游 4xx 的数量与错误文本分布、客户端错误率、用量记录异常、内存。发现异常按 §5 处置。
- **gjson 钩子返回了与原生不同的结果**：影响面比单道门大，因为所有读请求体顶层字段的地方、sjson 的改写都经过它。
  防线有两道：
  - 只接管最简单的路径，其余交还原逻辑；
  - 差分测试与 fuzz 逐字段比较。

  线上可以单独关掉 `index`。
- **gjson 副本的维护**：上游 sub2api 升级 gjson 版本时，要换成新版本的副本，并重新打上钩子这一段改动。
  `FORK-NOTES.md` 写明步骤，钩子差分测试用来验证。
- **上游 rebase 改了被跳过的函数**：门可能与新逻辑不再等价。守护测试会失败的改动能被发现；守护测试没覆盖到的，
  要靠 rebase 时复核，复核点列入 `FORK-NOTES.md`。
- **内存**：
  - 登记表的条目只存偏移与布尔值，超过成员上限就不建索引，随请求结束而移出。
  - 出站前的版本在转发响应之前就注销，不因登记而在流式期间保活；入站版本本来就被 handler 引用到请求结束。
  - 完整解码减少后，峰值 RSS 预计下降，以长时间负载对比为准。
- **上线后可选**：分配大减之后，可以试着把 `GOGC` 调到 150–200，由 `GOMEMLIMIT` 兜底，用内存换 CPU。这同样只需
  改环境变量、重建容器。

## 9. 改动面

| 文件 | 改动 |
|---|---|
| `handler/openai_gateway_handler.go` | §3.1 函数头 3 行；§3.7 登记 1 行；§3.12-1 增 1 行、改 2 行 |
| `service/openai_gateway_passthrough.go` | §3.7 登记、提前注销与兜底，3 行 |
| `service/openai_responses_lite_tools.go` | §3.2，3 行 |
| `service/openai_gateway_request_body.go` | §3.3，1 行；§3.9，3 行；§3.11，3 行；§3.12-2，改 4 行 |
| `service/openai_compact_body_signal.go` | §3.4，3 行；§3.12-2，改 1 行 |
| `service/openai_codex_tool_names.go` | §3.5，3 行 |
| `service/openai_responses_tool_schema.go` | §3.6，3 行；§3.12-4，改 1 行 |
| `service/openai_responses_item_id.go` | §3.10，3 行 |
| `service/openai_responses_namespace.go` | §3.12-2，改 1 行 |
| `service/openai_codex_account_identity.go` | §3.12-2，改 1 行 |
| `pkg/httputil/body.go` | §3.12-3，3 行 |
| `backend/go.mod` | `replace` 1 行，外加 1 行注释 |
| `Dockerfile`、`backend/Dockerfile` | 在 `go mod download` 之前复制 `third_party/`，各 1 行，外加 1 行注释 |
| `backend/third_party/gjson` | 原版副本，外加钩子与原生入口约 30 行 |

- 上游业务文件共 11 个，增 42 行、改 10 行，都是函数头的提前返回、原有赋值之后追加一行，或者同一行里的替换。
- 另有两个 Dockerfile 各 1 行，以及一份 gjson 副本。
- rebase 时要复核：
  - 被门跳过的 9 个函数（含 §3.9 的循环、§3.11 的预判）的改写条件与读取的字段有没有变化；
  - 两个登记点之间，上游有没有新增会改写请求体的处理；
  - 流水线上有没有新增原地修改请求体的写法。
