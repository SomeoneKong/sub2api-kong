# OpenAI 账号消耗节奏：让额度消耗进度跟上时间进度

**状态**：已实现；数据目录下没有配置文件时不生效。仓库组织与发布流程见 `FORK-NOTES.md`。

本文只涉及 OpenAI OAuth（ChatGPT 订阅）账号的**新会话落点**，是通用设计说明。具体部署实例的参数取值、
数据依据与上线步骤不在本文，由各实例单独记录。额度计量本身的事实（套餐之间的额度比例、读数噪声、窗口规则）
以工作区 `codex-quota-analysis/quota-analysis.md` 为准。

## 0. 背景：旧版选号不看额度

高级调度器关闭时（`openai_advanced_scheduler_enabled=false`），OpenAI 请求走旧版路径
`selectAccountWithLoadAwareness`：

1. **会话粘性**：会话绑定的账号优先，TTL 1 小时，每次命中续期。绑定账号的槽位满了，先在该账号上排队
   （上游默认最多 3 个、最长 120 秒），队满后单次借用其他账号。第 2 层直接抢到槽的借用不改绑定；借用走到第 3 层
   排队、或 WebSocket 建连落在借用账号上时，handler 在排队成功或建连后会把绑定改到借用账号（未启用利润门控时）。
2. **新会话**（无绑定、或绑定账号队满后的单次借用、或请求没有会话标识）：过滤掉负载率 ≥ 100 的账号，
   按 `accounts.priority` → 负载率 → 最久未用 排序，完全相同的再随机打散，依次抢并发槽。
3. 都抢不到时，按优先级与最久未用选一个账号排队。

与本设计相关的事实：

- **整条路径不看额度余量与期限**。唯一与额度有关的是按已用百分比停用的阈值。
- **负载率** =（在途 + 排队）× 100 ÷ `load_factor`，`load_factor` 为空时用并发上限。它只影响过滤与排序，
  真正的槽位上限始终是账号的并发上限。`load_factor` 大于并发上限时，槽满的账号负载率也不到 100，
  过滤形同虚设；多数选号时刻各账号在途为 0，负载率相同，排序退化成轮流。
- **分组内优先级 `account_groups.priority` 不参与 OpenAI 选号**：候选账号带着这个值，但排序只用账号级的
  `accounts.priority`（跨分组共用）。
- **套餐之间的额度差很大**（prolite 的周额度是 pro 的 1/4，quota-analysis §4.7）。按账号轮流分配时，小额度
  账号的百分比涨得快得多，先用完。

## 1. 目标与约束

**目标**

- **G1 按容量分配**：大额度账号承担更多，小额度账号不被按账号平均用穿。
- **G2 先用快作废的额度**：额度在截止时刻作废。截止时刻是 **7d 窗口重置**与**订阅到期**两者中较早的一个。
- **G3 平滑短时消耗**：同时观测过去 **6h** 与 **24h**，在明显有其他账号可用时，不把少数账号短时间用得
  太快。目的是降低风控概率。
- **G4 条件接近时先用 pro**：各账号进度差接近时，pro 比按容量应得的份额再多接一些。
- **G5 不挤占已有会话的并发**：一个会话可能同时占用多个槽，新会话不应挤进槽位已紧张的账号。
- **G6 会话在账号间摊开**：账号设了会话上限（`DESIGN-openai-session-limit.md`）时，新会话优先落到会话占用
  （活跃会话数 ÷ 上限）低的账号，而不是把一个账号先填到上限。这是其他条件接近时的软偏好，不压过 G1、G2（2.6）。

**做法：让每个账号的额度消耗进度跟上它自己的时间进度。** 落后于时间进度的账号多接新会话，超前的少接。
G2 由此提前实现：快到期的账号在整个窗口里就被均匀地多用，而不是到最后才面临"赶着用完"和"短时用太快"
的两难。**不预测需求**：服务负载在时间上随机性很大，本设计只比较各账号当前状态与各自的时间进度，
不假设未来的消耗速度。

**约束**

- **只做软约束**：只调整尝试顺序，不排除账号，没有其他账号可用时照常服务。本功能**永不导致拒服**。
- **会话粘性优先于节奏平衡**：粘性本身也是降低风控特征的手段。本功能只决定新会话落在哪里，不因节奏
  迁移已绑定的会话。迁移只由现有机制触发（账号被限流、不可调度等）。平衡状态下即使某个账号超出份额，
  它通常仍有余量让当前会话跑完。
- **作用于所有 OpenAI OAuth 分组**。节奏状态按账号计算，各分组共用。
- **不设观察期**：直接上线，按运行情况调参。参数放在配置文件里，改后一分钟内生效，不用重启（第 6 节）。
- **与 codex 票据不做耦合**：票据功能长期会下线，本功能不读、不写票据的任何状态。
- **状态落 Redis**：节奏状态与短时消耗的历史要能被外部观察服务只读获取，并在进程重启后保留。

## 2. 模型

### 2.1 每个账号的量

| 量 | 定义 | 来源 |
|---|---|---|
| Uᵢ | 当前窗口内**已接受读数**的最高值 | `accounts.extra.codex_7d_used_percent`，经 4.3 的读数判定 |
| Lᵢ | 窗口长度 | `extra.codex_7d_window_minutes`（pro / prolite 为 10080） |
| Sᵢ | 窗口起点 = 当前窗口的重置时刻 − Lᵢ | `extra.codex_7d_reset_at`，经 4.3 的读数判定 |
| Dᵢ | 截止时刻 = min(7d 重置时刻, 订阅到期时刻)；订阅到期只取晚于 Sᵢ 的未来值 | 另加 `credentials.subscription_expires_at` |
| Kᵢ | 容量系数，按套餐配置（第 6 节）；表外套餐或未知按默认套餐 | `credentials.plan_type` |

**权重与短时消耗用同一份读数判定**（4.3）：被判为回滚的读数（上游短暂回报被取代的旧窗口）既不进短时
消耗，也不改变 Uᵢ、Sᵢ——这类读数与真实值可以相差几十个百分点，且在没有新流量时会一直停在那里。

**窗口是否在跑**：上游的 7d 窗口从重置后的第一次请求开始计时。当前窗口的重置时刻已过（上游还没回报新
窗口），或窗口长度为 0（上游侧尚未激活）时，该账号处于**空闲**：Uᵢ 记 0，没有 Sᵢ。空闲公式里的 Lᵢ 此时取标准窗口
长度（7 天）。还没有读数（额度字段缺失）、或刚换套餐还没有新套餐的读数（4.3）时，该账号按**进度正常**处理：
Δ = 0，不罚。已有窗口之后的读数缺了字段不算新读数，沿用上一份已接受的读数——网关不会删除已有账号的这些字段，
只在复制账号时剥掉它们，复制出来的是没有读数的新账号。

### 2.2 进度差

窗口在跑的账号：

```
时间进度 τᵢ = (now − Sᵢ) ÷ (Dᵢ − Sᵢ)            0 → 1，到截止时刻为 1
进度差   Δᵢ = 100 × τᵢ − Uᵢ                       单位：百分点；正 = 落后于时间进度
```

空闲账号的窗口还没开始计时，额度本身不随时间流失；但订阅到期会截掉它下一个窗口的一部分：

```
空闲账号 Δᵢ = 100 × max(0, 1 − (订阅到期 − now) ÷ Lᵢ)      订阅到期缺失、已过或在一个窗口长度之外时为 0
```

即"现在开窗，一个完整窗口已经有多少用不上"。订阅还有 2 天到期的空闲账号 Δ ≈ 71，会很快分到新会话。
开窗后改按上面在跑的公式计算，Δ 回到 0 附近、再随时间按截断后的窗口上升——这一跳是有意的：开窗之后
它的节奏由那个截断窗口决定。

- **订阅到期早于重置时**，分母是这个窗口实际可用的时长，时间进度走得更快。例如窗口开始后 91 小时订阅
  到期：窗口一开始 Δ = 0，之后每小时若不用就落后约 1.1 个百分点（完整的 7 天窗口约 0.6），于是整个窗口里
  持续多接新会话，而不是到期前集中补用。
- **额度用尽的账号**（Uᵢ = 100）Δᵢ ≤ 0，通常也已被限流、在候选过滤阶段排除。
- **额度来源不限于本服务**：Uᵢ 是上游读数，账号在别处的使用也包含在内。

### 2.3 权重

```
wᵢ = Kᵢ × 2^((min(Δᵢ, G) − Oᵢ − Qᵢ − Cᵢ − Mᵢ) ÷ H)     H：每落后 H 个百分点，权重翻倍
                                                       G：追赶上限（百分点），只截进度差
                                                       Oᵢ：套餐让位量（百分点），按套餐配置
                                                       Qᵢ：短时惩罚（2.4），0 到 Qmax 个百分点
                                                       Cᵢ：并发占用调整（2.5），空闲时为负（加分）
                                                       Mᵢ：会话占用调整（2.6），0 到 Mmax 个百分点
```

G、Oᵢ、Qᵢ、Cᵢ、Mᵢ 都折成"视同超前 / 落后多少个百分点"，与 Δᵢ 同一单位。权重在**选号时**用当时的生效配置现算
（4.1）：刷新协程只维护额度事实，参数改动不必等下一次刷新。

- 不计让位量时，进度相同的账号按容量分配（K 之比），各自的百分比以相同速度上涨，进度保持一致（G1）。
- **让位量实现 G4**：小套餐要比 pro 多落后 Oᵢ 个百分点，才与 pro 同等对待。进度差接近时 pro 优先
  （O = H 时，同样的 Δ 下权重再减半，pro : prolite 从 4 : 1 变成 8 : 1）；小套餐明显落后时照常被用上。稳定后
  它的 Δ 比 pro 高约 Oᵢ，与负载高低无关。代价是需求不足时，它在窗口结束时比 pro 多剩约 Oᵢ 个百分点。
- 偏离进度的账号按指数被拉回。只有账号之间的**进度差之差**影响分配比例；负载高低让所有账号一起领先或一起
  落后，彼此的比例不变（各账号都在上限 G 以下时成立）。这就是"不预测需求"的含义。
- **追赶上限 G 限制集中**：落后很多的账号（刚加入、刚上线本功能、订阅截短的窗口）若不设上限，会以几百倍
  的权重拿走几乎全部新会话，在额度读数反馈回来之前就绑定一批会话——正是 G3 要避免的短时集中。进度因素最多
  让一个账号是同套餐、进度正常账号的 2^(G/H) 倍。上限只截进度差，超前一侧不设下限（该避让就避让）；让位量、
  短时惩罚与两项占用调整都在上限之后扣，所以追赶中的账号照样被它们压住，套餐偏好也不会被上限吞掉。代价：
  落后超过 G 的账号之间不再按期限紧迫程度区分，明显落后的账号追得慢一些；需求长期不足、所有账号都超过 G
  时，分配退化为按容量与让位量。
- Δᵢ、Oᵢ、Qᵢ、Cᵢ、Mᵢ 都有界，权重不会出现 0 或无穷。

### 2.4 短时消耗与软线

**inc6ᵢ / inc24ᵢ**：过去 6h / 24h 里账号消耗的周额度百分点，由 Uᵢ 的历史读数累计得到（4.3）。

每个窗口、每个套餐各有两条线：**起罚线 start** 与 **软线 full**，单位是占本账号周额度的百分点。按一个
窗口的消耗 x 分三段：

| 区间 | 处理 |
|---|---|
| x < start | 不罚 |
| start ≤ x < full | 线性罚：q = Qmax × (x − start) ÷ (full − start) |
| x ≥ full | **到线**：q = Qmax，并排到同一段的后面（第 3 节）——只有同段未到线的账号都抢不到并发槽时才轮到它们；全部到线时退化为按权重排序 |

Qᵢ = max(q₆, q₂₄)。**取较大者，不相加**：24h 的消耗已经包含这 6h，相加等于重复计罚。

- **第一段用线性而不是阶跃**：读数是整数，窗口又在滑动（旧消耗滑出后 inc 会回落），账号在线附近会一拍
  24、一拍 25 地来回；阶跃会让它的权重跟着跳、新会话去向跟着摇摆。线性下相差 1 个点只带来很小的变化，
  与"每落后 H 个点翻倍"的连续模型一致。
- **软线处的跳变是有意的**：那是最后一道线，到那时权重已被压到 2^(−Qmax/H)。
- **负载普遍高时比例不失真**：所有账号一起进入罚区、罚得相近时，彼此的比例基本不变。惩罚真正起作用的是
  "某几个账号比别的账号用得明显快"——即"明显有其他方案时不把少数账号用得太快"。
- 例：Qmax = 20、start 25、full 50，某账号近 6h 用了 40%：q₆ = 20 × 15 ÷ 25 = 12，H = 10 时权重约为原来的 0.44。

软线用于兜底进度差控制不住的突发，例如大会话集中、或某账号远落后于进度时的追赶。风控的真实判据未知，
取值应参照所部署账号已安全经历过的短时消耗峰值；full 不应超过 100（超过 100 的历史峰值只可能来自窗口
重开，一个窗口本身提供不了更多）。

### 2.5 并发占用

**为什么要看并发**：一个会话同时占用的并发不止 1。它展开的 subagent 与它共用同一个粘性标识，全部落在它
绑定的账号上，一个会展开 subagent 的会话单独就可能占满一个账号的槽位。绑定账号的槽满了，这个会话的请求先
排队，队满后单次借用别的账号——同一个会话标识出现在多个账号上，借到的账号也没有它的 prompt 缓存。所以给
一个账号新增会话，挤占的是它上面已有会话展开 subagent 所需的槽位（G5）。

**占用** = 近 N 分钟内该账号"在途 + 排队"的峰值 ÷ 并发上限（上限取账号的并发上限，不取 `load_factor`）。
用峰值而不用此刻的瞬时值：已绑定的会话在两轮之间（客户端在本地执行工具）在途为 0，subagent 也是一阵一阵
展开的，瞬时值会让一个挂着几个活跃会话的账号恰好在选号那一刻显得空闲。峰值靠高频采样得到（4.1，每 5 秒
一次，覆盖粘性会话的请求）；短于采样间隔的突发可能漏掉，所以"空闲"另要求账号的最近使用时刻也在 N 分钟以前，
两个条件都满足才加分。

| 占用 | Cᵢ | 以示例值、H = 10 计 | 例：上限 8 的峰值 | 例：上限 5 的峰值 |
|---|---|---|---|---|
| 0，且最近使用在 N 分钟以前 | −idle_bonus（加分） | −10，×2 | 0 | 0 |
| 不超过 busy_ratio | 0 | ×1 | 1–5 | 1–3 |
| 超过 busy_ratio，还剩 2 个以上槽 | free_2_plus | +10，×1/2 | 6 | — |
| 只剩 1 个槽 | free_1 | +30，×1/8 | 7 | 4 |
| 满（含排队） | full | +50，×1/32 | ≥ 8 | ≥ 5 |

- **软性权重而不是分档排序**：与节奏权重相乘，两者同时起作用。分档排序会让并发整体压过节奏，例如进度大幅
  超前的账号只要空闲就会排到最前；相乘之下空闲只让它的权重翻倍，照样基本不接新会话。
- **预留余量**：占用超过 busy_ratio（示例 2/3，即预留 1/3）开始罚，给已有会话的 subagent 留出余地。超过之后
  按剩余槽数分三档，而不是按比例分：槽位少的账号只剩 1 个槽时也应该重罚，按比例它可能只落在 80%。
- **free_1 宜取与追赶上限 G 相同的值**：一个正在追进度的账号（进度因素最多 2^(G/H) 倍）只剩 1 个槽时，不会比
  同套餐、进度正常、槽位宽裕的账号更优先——对应"粘性优先于短时平衡"。不同套餐之间还有容量系数与让位量的差，
  这条只在同套餐之间成立。
- **满的账号仍参与加权随机**：抢槽失败会顺延到下一个，排在前面的代价只是一次失败的尝试。
- **可单独关闭**：`concurrency.enabled: false` 时 Cᵢ 恒为 0（空闲加分也不给），峰值采样照常进行，状态与决策记录里
  仍能看到占用。

### 2.6 会话占用

**为什么要看会话**：会话上限只区分"满 / 没满"——满的账号在有余量的账号还在时不接新会话，没满的账号之间没有
先后，一个账号可能先被填到上限。风控上，同时挂在一个账号上的会话越少越好；并发上限放宽之后，2.5 的并发调整
又很少触发。所以在有余量的账号之间，按会话占用再分先后：占用低的多接新会话。

```
占用 rᵢ = min(1, 活跃会话数 ÷ 会话上限)      会话上限即 extra.max_sessions
Mᵢ     = Mmax × rᵢ                            Mmax：占满时的扣分（百分点）
```

活跃会话数就是会话上限在第 2 层入口那次批量查询的结果（`DESIGN-openai-session-limit.md` §3.2），由它交给本组件，
本组件不另查 Redis。不取峰值：活跃会话本身就是"最近 T 分钟内有活动"的会话，已经平滑。同一次选号里确认登记没
通过、被会话上限记为满额的账号（名额被别的新会话先占），之后的轮次按 rᵢ = 1 计。

**按占用比例，不按会话个数**：会话上限是逐账号设的，表达的是这个账号能挂几个会话；各账号上限相同时两者等价。

**线性、不分档、没有空闲加分**：并发调整分档，是为了给已有会话的 subagent 留槽位（2.5）。会话这一侧，满额账号
在分段时已被排除，名额竞争由确认登记处理，剩下的只是有余量账号之间的软偏好，一条随占用上升的直线就够。没有会话
的账号 Mᵢ = 0，已是设了上限的账号里最优的；不另给空闲加分，也就不会与并发的空闲加分叠加。

以 Mmax = 30、H = 10 计，其他条件相同时，权重相对于没有会话的账号：

| 活跃会话 | 上限 2 | 上限 3 | 上限 4 |
|---|---|---|---|
| 0 | ×1 | ×1 | ×1 |
| 1 | ×0.35 | ×0.5 | ×0.59 |
| 2 | 满 | ×0.25 | ×0.35 |
| 3 | — | 满 | ×0.21 |

- **只在其他条件接近时起作用**：G6 是次要目标，不应压过额度进度（G1、G2）。有余量的账号 rᵢ < 1，Mᵢ 小于 Mmax；
  Mmax 不大于 G 时，落后满 G 的账号即使只剩 1 个名额，也仍比进度正常、没有会话的同套餐账号优先。例如同套餐的
  A（1/2 个会话、Δ = 30）与 B（0/2、Δ = 0）：不计会话时 A 的首选概率约 89%，计入后约 74%。
- **满额时相互抵消**：所有候选都满时（会话上限的溢出），rᵢ 都是 1，Mᵢ 相同，先后由其他因素决定。
- **Mᵢ 取 0（中性）**：账号没设会话上限（占用无从计算）；请求没有会话标识、候选都没设上限、计数查询失败——这些
  情况下会话上限那一侧不建立分段，也就没有计数可用。
- **可单独关闭**：`sessions.enabled: false` 时 Mᵢ 恒为 0。会话上限的分段不受这个开关影响——分段是会话上限自己的
  硬性规则，本开关只管权重里的这一项。

## 3. 选号规则

**只作用于旧版选号的第 2 层（新会话落点）**。改动后的第 2 层：

1. 候选过滤不变（排除、资格、限流、负载率 ≥ 100 等）。
2. 先按原逻辑排序（优先级 → 负载率 → 最久未用 → 同组随机）。
3. 对每个 **`accounts.priority` 相同的连续段**：
   - 段内全部是 OpenAI OAuth 账号时，把段内账号分成"未到线"与"到线"（任一窗口 x ≥ full）两组，每组内
     按 wᵢ 做**不放回加权随机**，未到线组在前；
   - 段内混有其他类型的账号时，这一段保持原顺序。
   优先级之间的先后不变：手工设置的主力 / 后备分层继续有效。
4. 原有的后续处理照旧作用在新顺序上，依次是：开启"低倍率优先"时的倍率排序（稳定排序）、需要 compact
   能力时的分层（稳定分组）。
5. 按新顺序依次抢并发槽；抢到后按原逻辑绑定会话（溢出时保留原绑定的例外照旧）。

**与后续排序的先后是有意的**：compact 分层表达的是"这个账号能不能正确处理这个请求"，倍率排序表达的是运维
明确打开的成本偏好，两者都优先于节奏；节奏在"同一 compact 层、同一倍率"的账号之间生效。OAuth 账号使用统一
调度倍率时，倍率排序对它们不改变顺序；若给 OAuth 账号设了不同倍率并打开低倍率优先，节奏只在同倍率的账号
之间起作用，到线的低倍率账号会排在未到线的高倍率账号前面。

**用随机而不是"取权重最大"**：额度读数在请求结束后才更新、状态每分钟刷新一次，严格取最大会让一波新会话
在数据追上之前全落到同一个账号——这正是要避免的短时集中。

**同段内不再按负载率排序，改由并发占用调整 Cᵢ 进权重**（2.5）：负载率反映不了并发槽位的真实余量；负载率
≥ 100 的账号仍在过滤阶段排除，抢槽失败仍顺延到下一个。**会话上限的分段在重排之前**：有余量的账号还在时，
重排只看得到它们；会话占用调整 Mᵢ（2.6）在它们之间再分先后。

**部署要求**：

- **`load_factor` 留空**，让负载率的分母等于并发上限。这条过滤发生在节奏重排之前：`load_factor` 小于并发上限
  时，占用到它的账号会在进入节奏权重之前被硬性排除，2.5 里"只剩 1 个槽""满"两档用不上，全部都忙时还会落到
  不看额度的第 3 层；留空后过滤只剔除真正满槽的账号，占用交给软性权重处理。**清空要逐个账号改**：管理接口的
  批量修改把 0 当作"不修改"，清不掉；单账号更新传 0 才会清空。改完读回确认。
- **要互相平衡的账号设同一个 `accounts.priority`**：节奏分配只在同一优先级段内进行。例如 pro 与 prolite 分处
  两段时，后一段只在前一段都抢不到槽时才被用到，它的额度会大量作废。
- **只有一个网关实例写节奏状态**：多个实例各自采样，同一次消耗会按不同的读数序列记账，合并会重复计、覆盖会
  随最后写入者跳变。多实例部署需要先规定唯一的记账者，不在本设计范围内。

**分段只看 `accounts.priority`**，与原逻辑一致。账号能不能接某个分组的流量由分组成员关系决定；进了同一段，
就只按额度与并发状态分配，余量多、进度落后的账号多接。分组内优先级不参与，否则排在后面的账号只在前面抢
不到槽时才被使用，额度大多作废。

**不作用的路径**（保持原逻辑）：

- 第 1 层会话粘性：已绑定的会话照常回原账号（约束：粘性优先）。
- 带 `previous_response_id` 的请求路由到持有该响应的账号（上游逻辑）。
- 第 3 层（全部抢不到时排队）、负载查询失败时的降级分支、`load_batch_enabled=false` 的路径：
  都是少见情形，保持原逻辑以缩小改动面。
- 高级调度器：开启时本功能不生效（它有自己的打分，需要另行接入）。

## 4. 数据、刷新与存储

### 4.1 刷新与选号时的计算

**额度事实每分钟刷新**（后台协程，与请求无关）：

1. 检查配置文件（第 6 节），修改时间变了才重新解析，并立即替换内存里的生效配置。文件不存在时本拍到此为止。
2. 从数据库读全部 OpenAI OAuth 账号的 id、`plan_type`、`subscription_expires_at`、`codex_7d_*`、
   `codex_usage_updated_at` 与最近使用时刻。`credentials` 逐键取，不取整列（同一列里有 access / refresh token）。
3. 做读数判定、更新当前窗口与消耗记录（4.3），得到每个账号的**额度事实**：套餐、U、S、D 与截止来源、是否空闲、
   消耗记录、记录完整起点。这些事实与 H、G、O 等参数无关。
4. 整体替换内存里的事实快照并写 Redis（4.2）；另按当前生效配置算一份各账号的状态写进 state，供外部查看
   （空闲加分看的最近使用时刻取第 2 步读到的值）。

**并发峰值每 5 秒采样**：同一协程每 5 秒对全部账号读一次"在途 + 排队"（与网关批量读负载同一个接口），每次
第 2 层选号时网关本就要读的候选负载也计入；每个账号在内存里保存近 60 分钟（N 的上限）的逐分钟最大值，两个来源
按分钟合并，先后到达的顺序不影响结果。定时采样覆盖所有占槽
路径，包括不经过第 2 层的粘性会话请求。峰值不落 Redis：重启后 N 分钟内逐步补齐，期间占用可能偏低。

**选号时一次算出权重**：挂钩取当时的生效配置与事实快照，按第 2 节算 Δ（用当前时刻）、inc6 / inc24（用消耗记录）、
Q、C（占用取 N 分钟峰值与本次读到的值中较大者，最近使用时刻取候选自带的值）、M（活跃会话数由会话上限在本次
第 2 层入口查得后交来，上限取候选自带的 `max_sessions`；确认登记没通过的账号之后按占满计）与 w，再排序。账号数在几十的量级时
计算量可以忽略；选号时不查库、不访问 Redis、不读配置文件。决策记录保存的就是这次实际用到的参数与数值。

**退回原顺序**：生效配置 `enabled: false` 或文件已删除——在第 1 步就生效，不依赖后面查库成功；事实快照超过 5 分钟
未更新（刷新持续失败）——同时打 Warn。候选里有快照没有的账号（刚加入、还没到下一拍）时按进度正常处理：Δ = 0，
套餐取候选自带的 `plan_type`，不罚、不到线。

**为什么读库而不是用候选数据**：候选账号来自调度缓存，缓存保留的账号字段里没有订阅到期时间；而且
只在新会话出现时才能拿到候选，新会话稀疏时状态会一直是旧的。账号数在几十的量级时，每分钟读一次的代价
可以忽略。

### 4.2 Redis 键

| 键 | 类型 | 内容 | 过期 |
|---|---|---|---|
| `kong:pace:openai:account:<account_id>` | STRING | JSON：当前窗口（套餐、窗口长度、重置时刻、已接受读数的最高值与其读数时刻）、是否未激活、最近处理时刻、记录完整起点、25h 内的消耗记录（每条：记账时刻、上涨点数、读数时刻） | 有变化时整体改写；账号不再出现时删除 |
| `kong:pace:openai:state` | HASH | field = 账号 id，value = JSON：U、窗口是否在跑、S、D 与截止来源（重置 / 订阅）、K、O、τ、Δ、inc6、inc24、q₆、q₂₄、Q、是否到线、并发上限、占用峰值、最近使用时刻、C、w、记录完整起点（4.3）、本拍时刻；按本拍的生效配置计算，与选号时的实际值可有秒级差别。这里的 w 不含会话占用调整 M（按 0 计），是基础权重，选号时的实际权重 = w × 2^(−M ÷ H)，以决策记录为准 | 每拍续 10 分钟 |
| `kong:pace:openai:meta` | STRING | JSON：当前生效的配置（第 6 节全部字段）与版本、配置加载时刻、最近一次加载错误（没有则为空）、事实快照时刻、决策记录丢弃计数、本拍时刻 | 每拍续 10 分钟 |

- **一个账号的当前窗口与消耗记录放在同一个键里、整体改写**：Redis 在内存紧张时可能按 LRU 淘汰任意键，分开存会
  出现"窗口还在、记录没了"的半套状态，恢复后把丢掉的消耗当成没发生，还自称记录完整。放在一起时要么整套在、
  要么整套不在；不在就按"作为起点"重新开始（4.3），记录完整起点从此刻算。
- **键的有无只说明写入情况**：state 与 meta 不存在，意味着本功能没在运行（配置文件不存在），或者 Redis 写入
  持续失败（网关日志里有 Warn）——后一种情况下选号照常使用内存快照。`enabled: false` 时两个键照常更新，meta
  里能看到重排已关闭。改完配置文件后，在 meta 里核对新版本是否已加载、有没有加载错误。
- 进程启动时先从 Redis 载入各账号的键，此后每拍同时更新内存与 Redis。载入失败时这一拍不推进状态、下一拍
  重试，在此之前没有快照、保持原顺序——从空开始会用新起点覆盖 Redis 里仍在的历史。载入成功、只是某个账号
  没有键时，该账号从空开始。
- Redis 写失败时打 Warn，内存照常更新，选号不受影响；下一次写成功时整体写回。写成功之前进程退出，恢复的是
  较旧但自洽的一份：已接受最高值停在旧值，之后的新读数照常记出上涨，记账时刻按 4.3 的规则落在缺口开始处。
- 外部观察服务只读这些键；展示方式不在本文范围。

### 4.3 由读数历史得到短时消耗

上游只回报 7d 已用百分比（整数），短时消耗由读数的上涨累计而来。**过去 6h / 24h 的消耗跨过重置连续统计**：
窗口重置只是额度清零，重置前后的上涨都是这段时间里真实发生的消耗，一起计入。只有换了套餐，百分比的单位变了，
才从换套餐的时刻重新统计。

每个账号保存一份**当前窗口**（套餐、窗口长度、重置时刻、已接受读数的最高值、最近处理时刻），每拍拿数据库里的
读数和它比较。读数更新时刻没变的，是同一条读数，不算新观测。

**读数判定**（权重与消耗记录共用这一份结果）：

| 读数与当前窗口相比 | 判定 | 处理 |
|---|---|---|
| 同一套餐，重置时刻相差 1 小时以内 | 同一窗口 | 读数抖动是秒级到分钟级的 |
| 同一套餐，重置时刻晚 1 小时以上 | 重置 | 自然重置、重置卡重开、首次调用改写重置时刻，重置时刻都只会往后移。换成新窗口，消耗统计照常跨过它 |
| 同一套餐，重置时刻早 1 小时以上 | **回滚** | 上游短暂回报被取代的旧窗口（quota-analysis §4.5），读数整条忽略：不改当前窗口、不记消耗、不进权重 |
| 套餐变了（`plan_type` 或非零的窗口长度与当前窗口不同） | 换套餐 | 清空该账号的消耗记录，记录完整起点重置为此刻；等到读数时刻晚于发现这次变化那一刻的读数出现，再以它为新起点（不记上涨；此刻及以前的读数，包括稍后才落库的，可能还是旧套餐的），在此之前按进度正常处理（2.1）。套餐资料与额度读数分别更新，谁先到都按此处理。重置时刻往前移也不算回滚（套餐变更可能让窗口长度改变、重置时刻前移，quota-analysis §4.5）。窗口长度为 0 是未激活，按空闲处理，不算换套餐 |
| 窗口长度为 0 | 未激活 | 按空闲处理（2.1）。当前窗口与消耗记录照旧保留，重新激活时照常按上面几行判定；此前没有窗口、已知未激活的账号，激活后的第一个读数记为上涨（此前用量为 0） |
| 没有当前窗口（首次见到该账号，或 Redis 数据丢失） | 作为起点 | 只建当前窗口，不记消耗 |

**记消耗**：

- 同一窗口内读数超过已接受最高值：记一次上涨 = 读数 − 最高值，并更新最高值。读数回退 1 个点再恢复，按最高值
  比较不会重复计。
- 重置：记一次上涨 = 新窗口的读数（新窗口从 0 起）。旧窗口在最后一次读数之后、重置之前的那一点消耗看不到，
  少计。
- **记账时刻取"本组件上次处理该账号的时刻"与"读数时刻"中较早的一个**。本网关自己的请求在 30 秒内就会刷新
  读数，连续运行时误差在一拍以内。本组件中断过（重启、查库失败、配置文件缺失）时，缺口期间的上涨全部记在
  缺口开始处，只会少计。例：0 时读数 10，随后中断，1 时上游读数涨到 60，12 时恢复处理：上涨 50 记在 0 时，
  此刻 inc6 = 0、inc24 = 50，与事实一致。
- **例外会多计**：读数长时间没有刷新、其间账号在别处被使用（站外消耗）时，这部分上涨要等下一次读数才看得到，
  会记在看到它的那一刻，可能被算进最近 6h / 24h。上游只给当前读数，发生时刻无从得知；站外消耗同样是账号的
  真实用量，按风控口径把它算作"最近看到的消耗"可以接受。
- inc6 = 记账时刻落在过去 6 小时内的上涨之和；inc24 同理。
- **记录完整起点**：该账号第一次被处理、或最近一次换套餐的时刻（Redis 数据丢失后重新计）。它晚于 now − 24h 时
  inc24 可能少计，写进 state 供外部判断。

### 4.4 决策记录

每次进入第 2 层写一条决策记录（不论是否经过节奏重排），只用于事后分析，不进管理界面。

**存 Postgres 表 `kong_openai_pace_decisions`**：

- 与 `usage_logs` 同库，分析时直接联表。
- 量很小：第 2 层只在新会话、绑定账号队满后的单次借用、没有会话标识的请求时经过，条数与新会话数同一量级；
  每条带全部候选约 1–3 KB。
- 不用容器日志：会被轮转，分析要 grep 再解析；也不用 Redis：不适合长期保存。

| 内容 | 说明 |
|---|---|
| 决策编号、时间、用户 ID、分组 ID、模型 | |
| 本地请求 ID 与客户端请求 ID | 逐请求关联用：`usage_logs.request_id` 通常是 `client:<客户端请求 ID>` 或 `local:<本地请求 ID>`。WebSocket 等以上游请求 ID 记账的路径对不上，只能按账号、用户、时间关联 |
| 会话哈希 | 与粘性绑定同一取值（会话标识的 xxhash64），用于会话级分析。`usage_logs.session_id` 只保存请求头里的显式会话标识，粘性标识来自请求体或内容推导时对不上 |
| 进入第 2 层的原因 | 新会话 / 绑定账号队满后的单次借用 / 没有会话标识 |
| 是否实际重排及原因 | 至少有一个优先级段按节奏排了序才算实际重排。未重排的原因：`enabled: false`、快照缺失或过期、各轮的每个段都混有其他类型账号、没有可用候选、负载读取失败。能算出节奏顺序时照样算出并记录，可与实际结果对比 |
| 实际使用的参数 | 本次计算用到的配置内容（或配置版本，配置版本对应的内容另存一份，保留期不短于决策记录） |
| 每个候选 | 套餐、优先级段、所在段是否按节奏排序、调度倍率（倍率参与排序时）、U、Δ、O、q₆、q₂₄、Q、是否到线、占用峰值与上限、最近使用时刻、C、活跃会话数（没有计数时记为缺失，不记 0）与会话上限、M、w、首选概率（只对按节奏排序的段有意义） |
| 每一轮的顺序与结果 | 第 2 层可能用刷新后的负载再试一轮。每轮记：节奏给出的顺序、经倍率排序与 compact 分层后的最终尝试顺序、抢到的账号在最终顺序中的位置（排在它前面的，是复核未通过或抢槽失败的） |
| 结果 | 抢到的账号；或转入第 3 层排队、走降级分支 |

写入不阻塞选号：记录先进内存队列，后台每几秒批量写一次；队列满（数据库长时间不可用）时丢弃并计数，计数写进
meta 并打 Warn。保留期由配置决定，由刷新协程每天清理一次。

## 5. 接入点与改动面

| 文件 | 性质 | 内容 |
|---|---|---|
| `backend/internal/service/kong_openai_account_pace_config.go` | 新增 | 配置文件的解析、校验与热加载 |
| `backend/internal/service/kong_openai_account_pace_model.go` | 新增 | 读数判定与消耗记录、进度差、短时惩罚、并发调整、权重 |
| `backend/internal/service/kong_openai_account_pace.go` | 新增 | 每分钟一拍的刷新协程、并发采样、快照、Redis 状态的恢复与发布、决策记录的写入与清理 |
| `backend/internal/service/kong_openai_account_pace_select.go` | 新增 | 第 2 层的挂钩：按优先级段分层、加权无放回抽样、决策记录的组装 |
| `backend/internal/repository/kong_openai_account_pace_repo.go` | 新增 | 4.1 第 2 步的查询；决策记录的批量写入与清理；Redis 键的读写 |
| `backend/migrations/904_kong_openai_pace_decisions.sql` | 新增迁移 | 决策记录表与时间索引 |
| `backend/internal/service/openai_gateway_scheduling.go` | 上游文件，单行挂钩 | 进入第 2 层时开始一次决策（带入是否单次借用）；每轮在 `shuffleWithinSortGroups(available)` 之后重排 `available`；抢槽前交回最终尝试顺序，抢到时交回账号；负载读取失败分支标记不重排。第 2 层的每个出口（抢到、没有可用候选、负载读取失败、转入第 3 层）都经同一个 `defer` 写决策记录 |
| `backend/internal/service/openai_gateway_service.go` | 上游文件，1 个字段 | `OpenAIGatewayService` 上的可选依赖与 setter，nil 即不启用 |
| `backend/cmd/server/wire_gen.go` | 已有 fork 装配处 | 按上游数据目录规则（`setup.GetDataDir()`）定位配置文件，装配、注入、启动协程；在清理步骤里停止协程 |
| `FORK-NOTES.md` | 已有 fork 文档 | 定制边界加入本功能；记下挂钩位置与 rebase 时的复核点，以及配置走数据目录下的文件、不走环境变量这一例外及理由（第 6 节） |

协程总是装配并运行（它要能发现后来放进去的配置文件），配置文件不存在时每拍只检查一次文件。挂钩对"没有
可用快照"安全：直接返回原顺序。新增 1 张表（决策记录），不改已有表，不动前端。

## 6. 参数

**配置文件 `openai-account-pace.yaml`**，放在上游的数据目录（`DATA_DIR`；容器部署时通常是挂载进容器的
`/app/data`，放进去不用改编排）。

**文件名、Redis 键与代码文件都带平台名 `openai`**：将来其他平台的账号若要同类控制，额度结构与套餐名都
不同（套餐名还可能重名），各用各的文件与键，加载与出错互不影响。

**为什么不用环境变量**（本 fork 其余配置都走环境变量）：上线后要按运行情况调参，环境变量每改一次都要重建
容器，会掐断进行中的流式响应。配置文件每拍检查一次，改后一分钟内生效；`enabled: false` 同时是不用重启的
应急开关。按套餐的参数在文件里也能写成结构，而不是挤进一个字符串。

结构与示例值（各实例的取值另行确定）：

```yaml
enabled: true               # 是否重排新会话；false 时刷新与写 Redis 照常，第 2 层保持原顺序
gap_doubling_pp: 10         # H：每落后多少个百分点，权重翻倍。越小，拉回进度越急、新会话越集中
gap_cap_pp: 30              # G：追赶上限（百分点）；最多是进度正常账号的 2^(G/H) 倍
max_penalty_pp: 20          # Qmax：短时消耗到软线时的惩罚（百分点）；H = 10 时权重降到 1/4
plans:                      # 按 credentials.plan_type 匹配
  pro:
    capacity: 1             # 容量系数 K
    yield_pp: 0             # 让位量 O
    soft_line_percent:      # 占本账号周额度的百分点：start 起线性罚，到 full 罚满并排到后面
      6h:  { start: 25, full: 50 }
      24h: { start: 40, full: 80 }
  prolite:
    capacity: 0.25          # 额度比例见 quota-analysis §4.7
    yield_pp: 10
    soft_line_percent:
      6h:  { start: 25, full: 50 }
      24h: { start: 40, full: 80 }
default_plan: pro           # 表外套餐或套餐未知时套用哪一项
concurrency:                # 并发占用调整 C（2.5）
  enabled: true             # false 时 C 恒为 0（含空闲加分）
  peak_window_minutes: 5    # N：占用按近 N 分钟"在途 + 排队"的峰值计
  idle_bonus_pp: 10         # 窗口内没有请求：加分
  busy_ratio: 0.667         # 占用超过此比例开始罚
  penalty_pp:               # 超过 busy_ratio 后按剩余槽数
    free_2_plus: 10         # 还剩 2 个以上槽
    free_1: 30              # 只剩 1 个槽，宜与 G 相同
    full: 50                # 满，含排队
sessions:                   # 会话占用调整 M（2.6）
  enabled: true             # false 时 M 恒为 0；不影响会话上限的分段
  max_penalty_pp: 30        # Mmax：占用为 1 时的扣分，按占用比例线性；宜不大于 G
decision_log:               # 决策记录（4.4）
  enabled: true
  retention_days: 90
```

**全部字段必填，出现未知字段即非法，文件里只能有一个 YAML 文档**：半截的文件不会被悄悄补上默认值，拼错的键
也不会被静默忽略，`---` 之后的内容也不会躲过校验。取值范围：
H > 0；G > 0；Qmax ≥ 0；0 < capacity ≤ 100；yield_pp ≥ 0；每条线 0 ≤ start < full ≤ 100；`default_plan` 必须在
`plans` 里；1 ≤ peak_window_minutes ≤ 60；0 < busy_ratio < 1；idle_bonus_pp ≥ 0；0 ≤ free_2_plus ≤ free_1 ≤ full；
Mmax ≥ 0；retention_days ≥ 1；(G + idle_bonus_pp) ÷ H ≤ 64。最后一条与 capacity 的上限保证权重及其总和不会溢出成无穷大，
否则加权抽样会失效。

| 情形 | 处理 |
|---|---|
| 文件不存在 | 功能关闭：不刷新、不写 Redis，第 2 层保持原顺序 |
| 启动时文件非法 | 同上，并打 Error。**不阻止网关启动** |
| 运行中文件被改坏 | 沿用上一份有效配置，打 Warn，错误写进 `kong:pace:openai:meta` |
| 运行中文件被删除 | 视同不存在，功能关闭 |

**新增字段与升级顺序**：`concurrency.enabled` 与 `sessions` 段是必填的，旧文件在新版本上非法、新文件在旧版本上也
非法（未知字段）。所以镜像与配置文件成对切换，两份文件都留着：

1. 事先备好新文件，旧文件另存一份。
2. 停掉网关，换上新文件，再用新镜像启动；在 `kong:pace:openai:meta` 里核对配置版本与加载错误。
3. 失败或要回滚时，同样先停网关，换回旧文件，再用旧镜像启动。

不要在旧进程还在跑时先换文件：它读到非法文件时只是沿用内存里的配置，一旦在换镜像之前重启，就会因启动时文件
非法而关闭本功能。

**取值要点**：

- 软线参照所部署账号已安全经历过的短时消耗峰值（2.4），start 可取 full 的一半。按匀速用完一周算，6h 只需
  3.6%、24h 只需 14.3%，软线应明显高于这个水平，只拦极端时段。
- 让位量 O 取 H 时，进度差相同下小套餐的权重再减半；越大越偏向 pro，小套餐在需求不足时剩得越多。
- 并发调整的 free_1 与 G 取同值（2.5）。
- 会话调整的 Mmax 不大于 G（2.6），会话占用就不会压过明显的进度差。

**代码常量**：

| 常量 | 值 | 依据 |
|---|---|---|
| 刷新间隔 | 60 秒 | 额度读数在流量驱动下每账号最快 30 秒更新一次；配置文件也随每拍检查 |
| 并发采样间隔 | 5 秒 | 请求多在数秒到数分钟之间，5 秒能看到绝大多数突发；每次只读一次全部账号的计数 |
| 快照有效期 | 5 分钟 | 超过即退回原顺序 |
| 消耗记录保留 | 25 小时 | 覆盖 24h 窗口的起点 |
| 窗口身份容差 | 1 小时 | 与 quota-analysis 的切窗口径一致；重置时刻抖动在分钟级 |
| Redis 状态过期 | 10 分钟 | 外部据此判断状态是否仍在更新 |

## 7. 可观测性与成功条件

**观测**：

- Redis 里的 `kong:pace:openai:state`（4.2），外部观察服务按账号展示 Δ、inc6、inc24、Q、是否到线、占用、C、w。
- 决策记录（4.4）：逐次选号的全部输入与结果，用于事后分析，例如节奏顺序的首选账号与实际抢到的账号是否一致、
  单次借用的频率、并发调整是否把新会话从忙账号引开、会话调整是否把新会话摊到会话占用低的账号。会话占用只在
  决策记录里有（它来自选号时的查询）；state 与周期日志里的 w 不含 M，是基础权重（4.2）。
- 每 10 分钟打一行 Info 日志，逐账号给出与 state 相同的字段。
- 计数（进日志）：本周期内第 2 层选号的次数；其中**实际重排、同一优先级段、同一 compact 层与倍率里有未到线账号
  可选、却落到到线账号**的次数（倍率指倍率排序实际使用的调度倍率；倍率不参与排序时不按倍率区分）（只应出现在那些未到线账号都抢不到槽或复核不通过时）；保持原顺序的次数及原因；
  被判为回滚而忽略的读数条数；决策记录因队列满而丢弃的条数。

**成功条件**（上线后看，按需改配置文件里的参数）。都只在同一优先级段、同一 compact 层与倍率、实际重排的决策里
比较；记录完整起点晚于 24h 前、或快照过期的时段不评判。

| 目标 | 看什么 | 期望 |
|---|---|---|
| G1 | 同段账号的首选概率与额度消耗速度：Q、C、M 相近且都在 G 以下时，比较各账号 min(Δ, G) − O 之差 | 差值大多数时间在 ±2H 以内；小额度账号不再先于大额度账号用完。所有账号一起落后或一起超前是负载高低造成的，不算偏离 |
| G4 | 条件同上时，小套餐与 pro 的 Δ 之差 | 稳定在 O 附近（粘性会话与各账号窗口错位会带来波动） |
| G2 | 窗口结束（重置或订阅到期）时的剩余额度，扣除让位量 O 的影响后比较；对照该账号最后一段时间的首选概率与实际接纳 | 同段其他账号额度吃紧时它接近用完；需求不足时各账号剩余相近。窗口结束前它一直有可观的首选概率、也确实被选中接纳，却没有用掉，是新会话不足而非控制失效 |
| G3 | 各账号进入罚区、到线的时长占比，以及上面的计数 | 到线少；进入罚区的主要是明显比同段其他账号用得快的账号，且新会话确实被引到别处 |
| G5 | 决策记录里单次借用占第 2 层决策的比例；并发采样中各账号达到上限的时长 | 与上线前同口径的数据相比下降（上线前的借用次数可由 `usage_logs` 里同一会话跨账号的请求估计） |
| G6 | 决策记录里同段、有余量、设了会话上限的账号的会话占用，以及新会话实际落在哪个账号；只看会话调整生效（开关打开、有计数）的决策 | 新会话多落到占用低的账号；同段账号的占用差距小于不开会话调整时。G1、G2 仍按各自的判据评判，开启会话调整不应让它们超出期望 |

**控制力的边界**：本功能只影响新会话的**首选顺序**；实际落点还取决于后续排序、复核、抢槽与会话大小，账号的
消耗还包括已绑定会话。粘性会话一直活跃时（大会话持续超过 1 小时），账号的消耗由它决定。"Δ 偏离大但新会话
很少"说明控制力不足，而不是参数不对，此时调小 H 或软线没有用。

## 8. 边界情况

| 情形 | 处理 |
|---|---|
| 7d 已过重置点、上游未回报新窗口；窗口长度为 0 | 空闲：U = 0，Δ 按 2.2 的空闲公式（窗口长度取标准 7 天；订阅临期时为正，否则为 0） |
| 还没有读数（额度字段缺失） | 进度正常：Δ = 0，不罚（2.1） |
| 订阅到期时刻缺失、已过、或早于窗口起点 | 不计入截止时刻，只看 7d 重置。订阅是否真的失效由现有账号机制判断，本功能只少了 G2 的偏好 |
| 订阅续费后到期时刻后移 | 下一拍读到新值，Dᵢ 与 τᵢ 随之变化（τ 变小，账号显得超前一些） |
| 订阅到期早于 7d 重置 | 截止按订阅到期（2.2） |
| 读数回退 1 个点 | 按已接受最高值计算，不影响 Δ 与消耗记录 |
| 上游短暂回报旧窗口（回滚） | 读数整条忽略，Δ 与消耗记录都沿用当前窗口（4.3）；回滚读数在没有新流量时会停留数小时，期间同样忽略 |
| 套餐变更（`plan_type` 或非零窗口长度改变） | 从变更时刻重新统计短时消耗，等到变更之后的新读数再建起点，其间按进度正常处理；即使重置时刻往前移也不算回滚（4.3） |
| 本组件中断后恢复（重启、查库失败、配置文件缺失） | 缺口期间的上涨记在缺口开始处，只会少计（4.3） |
| 读数长时间未刷新、其间有站外消耗 | 上涨记在看到它的时刻，可能多计进最近 6h / 24h（4.3） |
| Redis 里某账号的键被淘汰或丢失 | 该账号按"作为起点"重新开始，记录完整起点从此刻算（4.2） |
| 同一段只有一个候选 | 不重排；仍写一条决策记录 |
| 读并发负载失败 | 这一拍不更新峰值；选号时本次读取失败的账号 C 按已有峰值算，没有峰值时为 0 |
| 账号没设会话上限；请求没有会话标识；会话计数查询失败 | M = 0（2.6） |
| 所有候选会话都满（会话上限溢出） | 占用都按 1 计，M 相同，先后由其他因素决定 |
| 有余量的账号在确认登记时被别的新会话抢满 | 该账号之后的轮次按占满计（2.6） |
| 进程重启 | 并发峰值从空开始，N 分钟内补齐，期间占用可能偏低 |
| 决策记录写库失败 | 留在内存队列里重试，队列满后丢弃并计数，不影响选号（4.4） |
| 账号跨多个分组 | 状态按账号计算，各分组看到同一个 w 与到线状态 |
| 段内混有 API Key 等其他类型的账号 | 这一段保持原顺序 |
| 刷新失败（查库失败） | 保留上一份快照；超过 5 分钟退回原顺序 |
| 配置文件缺失、非法或被删除 | 见第 6 节的加载规则 |
| 多个网关实例 | 不支持多个实例同时写节奏状态（第 3 节部署要求） |

## 9. 已知局限

- **粘性优先意味着大会话会持续压在原账号上**：本功能只能在它之后把新会话引开。这是已接受的取舍。
- **软线按本账号周额度的百分点计**：风控看的是绝对用量还是相对套餐未知。各套餐取同一组百分点时，按绝对用量
  折算，小套餐的线只相当于 pro 的一小部分。
- **容量系数是按套餐配置的**：同套餐账号之间的差异、新套餐的系数，需要改配置文件。
- **时间进度只是参照线，不是预测**：负载长期低于总容量时，所有账号一起落后、窗口结束时都有剩余，本功能
  只能让剩余分布均匀，不能创造需求。
- **只覆盖旧版选号的第 2 层**（第 3 节列出的其余路径不生效）。
- **读数判定只看套餐、窗口长度与重置时刻**：Redis 数据丢失后第一条读数恰好是回滚读数时，会被当作起点；之后
  真实窗口的读数按"重置"记一次上涨，短时消耗多计一次。需要两件小概率事件同时发生，接受。
- **追赶有上限**：进度因素最多让落后很多的账号拿到同套餐、进度正常账号 2^(G/H) 倍的权重，追回进度比不设上限慢；
  落后都超过 G 的账号之间不再按期限紧迫程度区分。
- **并发峰值靠采样**：短于采样间隔的突发可能漏掉；空闲加分另要求最近使用时刻足够早，以免把刚用过的账号当成空闲。
- **会话调整只对设了会话上限的账号起作用**：没设上限的账号 M = 0，与设了上限、已有会话的账号混在同一段时，
  相对地更容易被选中。
- **会话占用不进 state**：它来自选号时的查询，state 里的 w 是不含 M 的基础权重；外部观察只能看决策记录与账号
  列表的会话徽标。
- **站外消耗的记账时刻偏晚**：读数不刷新期间的站外消耗，要到下一次读数才计入短时消耗（4.3）。
- **只支持单个写入者**：多实例部署需要另行规定唯一的记账者。
- **不改粘性会话的排队与借用**：subagent 与父会话共用粘性标识、落在同一个账号上，这是上游行为；绑定账号满了
  以后的排队和单次借用也照旧。本功能只通过并发调整，减少把新会话压到忙账号上，从源头减少排队与借用。
