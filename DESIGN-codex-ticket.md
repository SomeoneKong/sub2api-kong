# Codex 票据：验证与注入设计

**状态**：已实现。仓库组织、版本号与发布流程见 `FORK-NOTES.md`。

本文的事实基础来自一批独立的协议实验（门票结构、A-B-A 因果性、时序常数、客户端源码行为）。
**那批原始观测记录不随本仓库发布**，所以文中凡是「实测」「已关闭的前提」这类说法，读者只能按本文
的转述看待；下面 §1 按可靠性把前提分成三类，正是为了让转述与推断不混在一起。

## 0. 为什么不直接用上游那套

上游 PR #7315 的方案（采票 → 按长度筛 → 注入）方向是对的，A-B-A 实验证明门票能决定实际
服务的模型档位。但它解决不了问题，有四处缺陷：

| 缺陷 | 后果 |
|---|---|
| **只按长度判定合格** | 292 与 312 相差正好一个 AES 块，292 桶的明文有 16 字节变化空间，**同为 292 的票档位可以不同**（实测同账号门票全程 292，模型却从 astra 变成 sol）。长度只能判"没掉到 luna" |
| 全局开关，无账号级豁免 | 一旦开启，任何拿不到合格票的账号在门控模型上直接被拒 |
| 时间参数会自锁 | 采票本身是出口上的活动，默认 6s 轮询让首次失败后的重试永久锁死在坏票 |
| 无档位可观测 | 拿到 292 就当成功，实际档位无从得知，降智完全静默 |

本设计的核心变化：**合格判定从"看长度"换成"看指纹"**，长度只用来做第一道廉价筛除。

## 1. 设计前提

**分三类，因为它们的可靠性差得很远。** 把条件性前提当成已证事实用，是这类系统最容易犯的错：
推理链会悄悄超出证据范围，而症状只是"怎么老拿不到好票"。

### 1.1 已实测的事实

1. **票是因不是果**。同一降智态下只改"是否注入"，注入 292 票即得 astra，撤掉立刻回落 luna。
2. **票的档位在其 TTL 内稳定**。注入同一张票可将 astra 维持 47 分钟、跨越 4 分钟窗口。
   这条决定了**一张票只需验证一次**。
3. **上游"带有效票就不下发、否则下发"**。这是被动收票的机制基础，也意味着注入生效后票源
   会暂时枯竭（见 §2.1）。
4. **能否拿到 292 取决于该出口的静默状态**，按出口 IP 计、与账号是否活跃无关。票 TTL 3600 秒，
   窗口约 4 分钟。

### 1.2 尚未关闭的前提（设计依赖它们，但证据不足）

1. **票不绑取票出口。** 设计据此让验证请求走流量出口（§4.2）。但"票是否编码打票出口 IP"仍是
   **阻塞性开放问题**——当前按"不绑定"推进是一个**判断**，不是实验结论。
   它若不成立，验证请求必须改走票据出口，而那会消耗静默，§4.2 / §7 的整套节奏都得重算。
2. **合成探针的归因结论适用于真实业务请求。** Q2 未确认。"票在 TTL 内稳定"只说明同一张票的档位
   不变，不能证明不同请求条件下的归因可以通用。
3. **静默是拿到 astra 的有效杠杆。** 这条最弱：静默决定能否拿到 **292** 有观察支撑，但 292 桶内
   **astra / sol 的成因仍未确定**（Q3）。已有的 9 小时零 miss 证明的是 292 成功率、不是 astra
   成功率，而且取自 60 分钟间隔，不能直接搬到别的间隔上。
4. **闭集归因不会把库外模型误判成 astra。** §4.1 已承认库外模型会被归到最相似的现有候选，那么
   高 softmax 概率并不能排除"库外的非 astra 被归为 astra"。
5. **挑战请求带 `instructions` 不影响归因。** 校准库取的是建库脚本里 environment-01 那一组，
   其条件是**无前缀（clean）**；但 codex responses 端点的请求带着 `openai.DefaultInstructions`
   （业务转发也带，不带可能直接被拒）。环境不同会让特征整体偏移，而**归因不会报错，只会给出一个
   看起来正常的错结论**。要关闭这一条得实测：同一账号同一时段，带与不带 instructions 各采若干份，
   比较归因分布。在关闭之前，指纹结论只能当作"相对可比的信号"，不能当作绝对档位判定。

这些不确定性**不改变方向**——票据优先、指纹准入、拒服不降智都仍然成立；但**不能反过来当业务
保证**用：任何"因为静默够长所以一定拿到 astra"式的推论都超出了证据。

### 1.3 已知风险

1. **静默门槛的观测很薄**（"约 34 分钟可开、19 分钟有时够"），样本不足、上游判据不可知，
   **不应作为常数依赖**——这正是 §5.4 要把静默时长记进事件表的原因：先观测，再谈取值。
2. **注入违反 codex 的 turn 契约**（跨 turn 复用、跨连接采集）。当前上游只验签、不校验 turn
   绑定。**这是整套方案的根本风险**，见 §8。官方客户端源码把这条契约写得很明确
   （`codex-rs/core/src/client.rs` 的 `ModelClientSession` 文档）：该令牌是**每轮**的
   sticky-routing token，"received at turn start … must not send it between different turns"，
   每轮都要新建 session，跨轮重放"violates the client/server contract"。这也解释了两件观察：
   它能决定档位（令牌钉住的是路由目标，不同路由不同档位），以及长度为何不足以判定档位。官方客户端源码把这条契约写得很明确
   （`codex-rs/core/src/client.rs` 的 `ModelClientSession` 文档）：该令牌是**每轮**的
   sticky-routing token，"received at turn start … **must not send it between different turns**"，
   每轮都要新建 session，跨轮重放"violates the client/server contract"。这也解释了两件观察：
   它能决定档位（令牌钉住的是路由目标，不同路由不同档位），以及长度不足以判定档位。

## 2. 总体设计

### 2.1 票有两个来源，各有适用面

| 来源 | 成本 | 票的质量取决于 |
|---|---|---|
| `ticket_observed`：业务响应头 | **零** | 流量出口当时是否静默 |
| `ticket_fetch`：经票据出口主动取 | 一次探测请求 + 出口静默 | 票据出口的静默（可控） |

诚实的判断：**在流量持续的账号上，被动收到的票多半是坏票**——流量出口不可能静默，而
静默是合格票的前提。所以票据出口是主力，被动收票是补充，它的价值集中在两种时刻：流量
稀疏的账号，以及流量出口恰好闲过一段之后的首个请求。

还有一个动态要注意：**注入生效后上游不再下发新票，票源暂时枯竭**。当前票过期后，下一个
不带有效票的业务请求会重新带回一张——但那时流量出口正活跃，拿回来的大概率是坏票。这正是
必须保留主动取票的原因。

⚠️ **「下一个业务请求会带回一张」这条在 `full` 的门控路径上不成立。** 那条路径无票即拒服
（§4.2），请求根本不会上送上游，也就不会有响应、不会有新票。被动收票只能发生在**已经上送**
的请求上：`off` / `observe` 模式的请求、非门控模型的请求，或该账号在别处产生的流量。这个区别
决定了 `full + none` 没有自主恢复能力，见 §3.1。

### 2.2 票的状态机

**「当前票」与「候选票」是两件事，必须分开判定**——把它们混在一组分支里是上游那套方案最大的
结构问题，也是本设计里最容易写错的地方：

| | 判据 | 谁在用 |
|---|---|---|
| **当前票**（服务资格） | verified、未过期、且未被判定注入失效（§2.4） | 业务请求 |
| **候选票**（取票资格） | 已入缓存、等待或正在验证 | 验证流程 |

由此得到两条规则：**候选票失败不影响仍然有效的当前票**；候选验证通过后立即接替当前票，
但**有效期不重算**——票的过期时刻由上游签发时决定，与我们何时验证完无关。

```
收票
  ├─ 长度命中 denylist（312）→ 丢弃，记事件，不入缓存
  └─ 长度合格 → 入缓存 unverified
        ├─ 来源 fetch    → 立即验证（§4.2）
        └─ 来源 observed → 满 min_ticket_age 才获得验证资格
              ↓
          指纹验证
            ├─ astra，且验证请求本身被上游接受 → verified，接替当前票（期限不变）
            ├─ 非 astra / 判不确定             → rejected，记事件，按 §4.3 处置
            └─ 验证请求未被接受（响应仍回 state）→ 本次结论作废，候选留在 unverified（§2.4）
```

长度筛是廉价的第一道门（零成本排除已知坏票），指纹是最终判据。**两者不可互相替代**：
长度判不了档位，指纹太贵不能每票都做。**不在 denylist 里也不等于合格**——未知长度同样必须
过指纹，denylist 只负责廉价排除已知的坏长度。

### 2.3 三种模式

**收票在三种模式下都进行**，这样模式切换时缓存里已有票可用，不必从零开始。模式的差异只
在"探测"与"注入"：

| 模式 | 收票入缓存 | 指纹探测 | 注入 | 需要票据出口 |
|---|---|---|---|---|
| `off` | 是 | 不 | 不 | 不需要 |
| `observe` | 是 | 对随请求返回的 state 探测 | **不** | 不需要 |
| `full` | 是 | 验证缓存中的票 | 是 | 可选，见 §3.1 |

`observe` 是纯诊断态：不改写业务请求，只回答"这个账号现在被降智了吗"。它不主动取票，完全不碰
票据出口的静默，**可以安全开在任何账号上**。它让降智的「监测」与「干预」解耦——先观察一段时间，
再决定要不要开 `full`。

**`off` 与 `observe` 的请求照常服务，即使模型在门控集合里。** 哪些账号需要保护是运维按观察结果
人工配的（观察到某个账号高概率降智才给它开 `full`），所以「这个账号没配 full」是常态，不是异常。
把它当成拒服理由，会让「设一次 `KONG_TICKET_GATED_MODELS`」把所有还没观察完的账号在该模型上全部
打死。这两种模式下的请求只是不受票据保障、也不做交付判定。

与票据出口三态（§3.1）交叉，九种组合的行为：

| | `none` | `direct` | `proxy` |
|---|---|---|---|
| `off` | 被动收票；不探测、不取票、不注入 | 同左，配置的出口不使用 | 同左，配置的出口不使用 |
| `observe` | 被动收票 + 诊断探测；不主动取票 | 同左，票据出口不使用 | 同左，票据出口不使用 |
| `full` | 验证被动候选；无候选即拒服，**且无自主恢复能力**（§3.1） | 经本机出口取票 | 经该代理取票；失效按 `none`（§3.1） |

**`observe` 也是请求驱动的**，不靠定时器：业务响应带回**新票**时，若距上次探测已满
`observe_probe_interval`，就对那张票做一次指纹探测；没有业务就没有探测。三条约束：

- **探测请求必须带着被观察的那张票发出。** 不带的话测到的是另一次请求的状态，归因结论落不到这张
  票上。这不违反"不注入"——那条指的是**不改写业务请求**。
- **结论有时效。** 界面显示"档位 + 探测时刻 + 所用票的来源"；票已过期、或结论距今超过一个 TTL 的，
  不能再显示成"现在"的状态。
- **无样本要能区分**：没收到新票、只收到 312、探测失败，三种状态各自显示，都不许拿旧结论顶替。
- **账号状态异常时不探测**：它同样是主动发起的请求，受 §4.2 那条「账号可正常调度」约束。账号被
  禁用、过期、限流暂停或额度耗尽时跳过本次探测并记事件，界面显示「账号不可调度，诊断暂停」——不是
  「无样本」，两者的处置完全不同。

### 2.4 保护边界：哪些请求受保护、保护到哪一刻

「不交付降智输出」要能端到端成立，先得说清保护从哪里开始、到哪里结束。这一节是对该目标的
执行规则，不是补充说明。

**范围**：

- **门控模型集合按最终上游模型判定**，不按客户端传来的名字——别名与模型映射解析之后再判，否则换个
  别名就绕过了整套保护。集合本身**来自配置、不硬编码**，并且要能在界面上看到当前生效的成员：判断
  「这个请求到底受不受保护」是运维的日常动作，靠读代码是不行的。
- 覆盖全部业务入口（流式、非流式及其它已有转发路径）。**新入口默认算未覆盖**，未覆盖的入口
  不得承载门控模型的请求。
- 票与验证结论按 `(account, model)` 绑定。§4.2 的 single-flight 是**账号级**的并发控制，不等于
  结论可以共享：模型 B 的等待者不能拿模型 A 的任务结果当自己的准入依据。

**交付边界的判据是「本轮证据」**：每次上送都要固定 `(本次使用的票, 账号, 最终模型, 该票的过期
时刻)`，交付前检查的是**这一次上送**是否确实带着那张票、且被上游接受。不是查此刻的全局当前票，
也不是查某次旧连接留下的握手响应头。

```
上送前：固定本次的 (票, 账号, 最终模型, 票的过期时刻)
上游响应 → 检查属于本轮的证据
  ├─ 本轮确实带该票上送，且响应未携带新 state → 注入生效，正常交付
  ├─ 响应仍携带 state → 该票没被接受
  │     ├─ 在第一个业务分片离开之前停止交付
  │     ├─ 只撤销**本次使用的那张票**（且仅当它此刻仍是当前票，才摘掉服务资格）
  │     └─ 该请求拒服，或改投一个同样满足准入条件的账号
  └─ 拿不到属于本轮的证据 → 一样不得交付（见下表）
```

**撤销对象必须是「本次使用的票」，不能是「当前票」。** 并发下两者会不同：请求带 K1 发出 → 预取
的 K2 验证通过、接替成当前票 → 带 K1 的那个请求才失败返回。按「当前票」撤销会作废无辜的 K2，
而预取已经把静默花掉了，白搭一段拒服。候选验证失败同理——只处理该候选，不动仍然有效的当前票。

**覆盖范围按上游传输方式定义，不能一概而论：**

| 上游传输 | 本轮证据 | 结论 |
|---|---|---|
| HTTP（含 SSE 流式、SSE 转 JSON） | `x-codex-turn-state` 在响应头，头先于 body 到达 | **可以拦**：判定点天然早于任何业务内容离开 |
| WebSocket 上游 | 票放在**握手头**，信号在**握手响应头**；连接池可复用连接，复用时本轮根本不发生握手 | **拿不到本轮证据** |

WebSocket 那一栏的问题不是「来不及拦截」，而是更糟：复用连接时新票压根没送上去（握手早就做完
了），而保存下来的旧握手响应头恰好「没有新 state」，**看起来像注入成功**。加大正文缓冲补不出这个
信号。另外注意 **HTTP 入站的请求也可能被转去 WebSocket 上游**，所以这不是「客户端用不用 WS」的
问题。

于是最小安全边界是：**凡是不能证明本轮使用了合格票的复用或长连接路径，对门控请求直接拒服。**
要支持这些路径，得先定义连接与票的绑定、换票时如何处置既有连接、以及同一连接上后续轮次的重新
准入——本期不做，就按拒服处理。

**只记录不拦截等于没有保护。** 那一刻降智输出已经生成，照常转发就直接违反目标；管理员事后切
`off` 只是关掉功能，救不回已经发出去的回答。

**WebSocket 与 HTTP 是两条对称的通道，WS 同样受保护。** 载体不同但都是**逐轮**的
（口径取自 codex 客户端源码 `codex-rs/core/src/client.rs` 与 `codex-rs/codex-api/src/sse/responses.rs`）：

| 方向 | HTTP | WS |
|---|---|---|
| 客户端 → 上游 | `x-codex-turn-state` 请求头 | payload 的 `client_metadata["x-codex-turn-state"]`，每个 `response.create` 帧都带 |
| 上游 → 客户端 | 响应头 | 握手响应头 **+ 带内 `response.metadata` 事件**的 `headers` |

所以「本轮证据」在 WS 上是存在的，而且 `response.metadata` 是上游在 **turn start** 下发路由令牌的
事件，先于任何内容分片——判定时机比 HTTP 的响应头还宽裕。连接池复用不构成障碍：证据不在握手里，
在本轮的下行事件里。

两条原生路径各有两个接入点：注入在逐帧解析/过滤器里（都在模型解析并改写**之后**，判定用的是实际
上送的那个模型，所以会话中途换成门控模型同样受保护），交付判定在下行写点（ctx_pool 的
`writeClientMessage`、passthrough 的 `BeforeWriteClient`）。

**不要把受保护账号改投 HTTP bridge。** 那条路虽然天然有证据边界，但 bridge 的适配层不是原生 WS 的
等价替代——它会删掉 `previous_response_id`（把续接静默改成新对话）、丢掉 `generate:false`（预热变成
普通生成）、且只接受 `response.create`（丢掉 `session.update` / `response.cancel`）。用它换证据是
拿非门控请求的语义去换，代价不对。

**接入点只有一处：`doOpenAIUpstream`。** 那是全部 OpenAI 上游发送的唯一汇聚点（二十来个调用点，
含 Responses 透传、chat-completions 转 Responses、messages 转 Responses、WS-HTTP bridge、
alpha-search、images 桥接）。准入放在它发送之前，交付判定放在它拿到响应头之后——响应头先于正文
到达、调用方此刻还拿不到 body，所以流式、非流式、SSE 转 JSON、compact 合成 SSE 一并覆盖。

按业务分支逐个去插是行不通的：那些分支各自构造请求、各自处理响应，漏一条就等于开一个降智出口，
而漏没漏不会有任何报错。**门控判定读最终请求体的顶层 `model`**——先用带引号的子串做廉价预筛，
命中了再解析 JSON 确认那个名字确实在 `model` 字段上（用户正文里提到模型名不算）。请求体读不出
模型时**拒服**，不放行。

准入状态按**单次上游发送**保存（一个局部变量），不按客户端请求保存：failover 会在同一个请求里换
账号重发，而票是 `(account, model)` 绑定的，用请求级状态会把上一个账号的票算到下一个账号头上。

拒服与交付拦截**不得被当成上游传输故障**。上游的传输错误处理会记一条 `request_error`、可能把账号
临时停掉调度、还会包装成 502 去换号——那等于把「拒服」悄悄变成「换个号照发」，而接手的账号是一个
未被观察过的账号。

**验证请求本身也要过这道检查。** 验证是带着候选票发出的——如果那次响应同样回了新 state，说明
候选票没被上游接受，这次回答的档位反映的**不是它**，不能用来判定它合格（否则会把一张坏票标成
verified，后患比拒服大得多）。这条检查只处理**该候选**：它既不证伪该候选的档位，也不影响当前票。
候选在这种终点之后能否再试，见 §4.2 的失败终点表。

**票是 `(account, model)` 绑定的，换号必须重新准入。** failover 在同一个客户端请求里换账号重发时，
不能把上一个账号的票注入到下一个账号的请求上——那张票对新账号无效，且会让判定张冠李戴。所以准入
状态按**单次上游发送**保存，不按客户端请求保存。

**哪些账号要保护是人工配的，没配不是异常。** 当前处于实验阶段，运维观察到某个账号高概率降智才给它
开 `full`。因此门控模型上遇到 `off` / `observe` 账号时**照常服务**，只是这次输出不受票据保障、也不做
交付判定。把它们当成拒服会让「设一次 `KONG_TICKET_GATED_MODELS`」把所有还没观察完的账号在该模型上
全部打死。

## 3. 账号级配置

**全局的启用开关与出口配置全部取消**，下沉到账号：

| 字段 | 含义 |
|---|---|
| `codex_ticket_mode` | `off` / `observe` / `full` |
| `proxy_id`（已有） | **流量出口**：业务、授权、验证请求走它 |
| `ticket_egress` + `ticket_proxy_id`（新增） | **票据出口**，三态，见下 |

两个出口分开是必须的，不是优化：合格票要求出口静默，而流量出口天生不静默。让它们指向
同一个代理，业务流量自己就会把合格票挤掉。

### 3.1 票据出口的三态

`proxy_id` 那套「NULL 即直连」在这里不够用——**「没配票据出口」与「票据出口就是直连」是两
种不同的行为**，必须能分开表达：

| `ticket_egress` | `ticket_proxy_id` | 行为 |
|---|---|---|
| `none`（默认） | — | **不主动取票**，只用被动收到的票 |
| `direct` | — | 经服务器本机出口取票 |
| `proxy` | 代理 id | 经该代理取票 |

`none` 的含义是「没有票据出口可用」，**不是「继承流量出口」**。后者在设计上就是错的：流量
出口天生不静默，继承只会稳定地取回坏票，还白烧额度。

`full` + `none` 是合法配置，但**它没有自主冷启动与恢复能力**——这一点必须写清楚，否则会被当成
「适合流量稀疏账号」的省事选项。原因在 §2.1：无票时 `full` 拒服，请求不上送，被动票源随之断掉，
靠自身流量回不到有票状态。一旦票用尽（首次启用、或闲置到全部过期），它就永久停在拒服。

它只在有**外部票源**时成立，而票源必须来自本账号**已经上送**的请求。这里有个容易忽略的约束：
§2.4 规定票与验证结论按 `(account, model)` 绑定，所以别的模型带回的票不能直接拿给门控模型用。
于是只有两条路：

- **允许跨模型移用原始票**：非门控模型的响应带回的票，可以作为门控模型的**候选**重新进入验证。
  注意只移用**原始票**，不移用验证结论——结论是 `(account, model)` 绑定的，必须重新验证。这条
  要在 §5.2 的缓存键上表达出来，否则实现会各按一种读法。
- **不允许**：那 `full + none` 就只剩一条路——运维临时把 mode 切到 `off` 放一段流量进去养票，
  而**那是明确的退出保护，不是「恢复了满血服务」**。

本期选哪条见 §9。另外提醒一个会写错的地方：**mode 是账号级的**，同一账号不可能同时处于 `full`
和 `off` / `observe`，所以「同账号另有 off/observe 流量在养票」根本不是一条可用路径。

没有上述补给就不要用这个组合。界面要显示成「等待外部票源」而不是「等待取票」（§6.2）：后者暗示
系统会自己解决，而它不会。

⚠️ **配置失效必须退回 `none`，绝不能退回 `direct`。** 理由不是洁癖：直连取票用的是服务器自己的
IP，静默被烧掉还能等回来，**把账号与服务器真实出口关联起来是不可逆的**。同理，票据代理也不能
继承上游那套「到期自动回退直连」的 fallback 策略。

**「失效」有两种，不止删除**，两种都按 `none` 处理并记事件：

- **代理被删除**：配置存在 `extra` 里（§5.1），没有外键替我们清空 id，所以会留下一个**指向已不
  存在的代理的 id**。取票前的存在性检查就是为它准备的。
- **代理到期**：更容易漏。上游的到期清扫只改写 `accounts.proxy_id`，**它不认识
  `ticket_proxy_id`**——所以票据代理到期后不会有任何自动处理，那一列会继续指向一个 expired
  代理，取票持续失败而配置看起来完好。取票前必须自己检查票据代理的有效期。

### 3.2 出口分离是取票时的运行条件，不只是保存时的校验

出口一旦合并，业务流量就会持续清零票据出口的静默，主动取票**必然**拿回坏票。所以这个条件
必须在**每次取票前**判定；保存时校验只是提前发现错误，它挡不住下面后三条——**业务出口可以在
无人保存票据配置的情况下改变**：

| 路径 | 保存时校验能否挡住 |
|---|---|
| `ticket_egress=proxy` 且 `ticket_proxy_id == proxy_id` | 能 |
| `ticket_egress=direct` 且 `proxy_id` 为空（业务也直连） | 能 |
| **业务代理到期，被上游的到期清扫自动改投为直连** | **不能**——那条路径改写的是 `accounts.proxy_id`，没有经过票据配置的保存 |
| 管理员在账号页改 `proxy_id`、或删掉流量代理 | **不能**——同样不经过票据配置的保存 |
| 两条代理记录指向同一个公网 IP | **不能**——代理 id 不同并不证明出口不同，任何 id 层面的比较都挡不住 |

判定不通过时：**停止主动取票并记事件**（等同 §3.1 的 `none`），但**已经验证通过的票按原期限
继续使用**——配置出错不该作废一张好票。

事件里要记下当时判定的两个出口。上面每条路径的表象都只是「一直拿不到合格票」，不记就得靠猜。

最后一条（同 IP）是 id 层面无解的，本期只能靠运维约定：票据出口专用、不与任何账号的业务出口
共享（见 §4.4 的部署前提）。

命名沿用 `ticket_*` 而不是上游的 `harvest_*`：上游那里主动采集是唯一路径，我们有两个来源，
再叫 harvest 会把次要路径说成全部；而 `proxy_id` / `ticket_proxy_id` 并列也更好读。

## 4. 验证

### 4.1 指纹归因（Go 内实现）

判据是**粗粒度的**——只需分出"归因落在被请求模型的接受白名单内还是外"，比区分具体档位容易得多
（astra 与 luna 的区分度 p 普遍 >0.94）。只门控 astra 时它就退化成 astra / 非 astra 的二分，
sol 与 5.5 难分那件事不影响结论，它们同属白名单外。但**"容易"不等于"有保障"**：闭集归因的残余
风险见下文与 §1.2.4。

**采纳判据：白名单内各归因结果的概率之和 ≥ 置信度。**

每个被门控的模型有一个接受白名单，**永远含自己**，可以额外声明接受哪些别的归因结果——实测同一
账号为 `gpt-5.6-sol` 取票时归因常返回 `gpt-6-astra`，拿到更高档位同样应当接受。方向是单向的：
astra 不接受 sol，那正是这个功能要挡的降智。

两个设计点：

1. **白名单，不是一条档位链。** 校准资料是 13 个候选，跨 gpt 与 claude 两个家族（claude 那 7 个
   纯属归因干扰项），`gpt-5.6` 内部还有 sol / terra / luna 三个同辈。它们之间并不存在全序，硬排
   成链等于凭空给互不可比的模型定先后，而那个先后直接决定放行与拒服。
2. **求和，不取 argmax。** 门要判的是"有没有掉到白名单之外"，那是一个并事件。astra / sol 各占
   0.6 / 0.35 时，"sol 或更好"已有 0.95 的证据，按 argmax(0.6) 判会把一张合格的票拒掉——无谓
   拒服与放行降智同样是缺陷。同一个量也用作挑战循环的提前停止条件，否则证据分散在两个都接受的
   归因之间时会白加挑战，加满还是拒。

判据只在一处实现（`KongTicketAccept`）：落结论、读当前票、管理面显示都走它。仓储只按
`status = verified AND expires_at > now()` 返回候选，不在 SQL 里复制一份判据——两套规则的缝隙
不会报错，只会让同一张票在两条路径上得出不同答案。

因此票行要存**完整归因分布**而不只是 argmax（§5.3）：白名单来自配置、可以改，只存 argmax 就只
剩"照旧放行"与"一律作废"两个都不对的选项。

**采用 ModelTrace 的官方口径**，不自创变体：

```text
0.75 × 去除环境方向后的 Hellinger 模型中心相似度
+ 0.25 × 有序块数字序列特征
```

归因时先从数字分布与有序特征中投影掉建库时估计的共享环境干扰方向，再与全部模型中心比较；
**每份回答分别评分后取平均**，用**与份数对应的校准温度**做一次全局 softmax 得到概率。

⚠️ 两处容易做错、必须照官方来：

1. **三条挑战是三个不同的 prompt，不是同一 prompt 重复采样。** 因此**多份回答的数字序列
   不能合并**——分布基准不同，池化会污染特征。只能各自评分、在**分数层面**平均。
2. **校准温度按份数选（档 1/2/3）**。平均会压低分数方差，温度必须跟着变；用档 `"1"` 去
   解释两份的平均分，概率标定就是错的。

**份数按置信度递增，达到阈值即停——早停是常态而非妥协**，多数票应该停在一份（1500
tokens）。升到三份只是模糊样本的兜底：

```
挑战 #1 → 评分 → softmax(档 "1")
  p ≥ 阈值 → 采纳
  否则 → 换挑战 #2 → 两份分数平均 → softmax(档 "2")
      p ≥ 阈值 → 采纳
      否则 → 换挑战 #3 → 三份平均 → softmax(档 "3")   ← 官方标准用法
          仍不足 → 判不确定，按拒绝处理（§4.3）
```

中途停下不偏离算法——每一档都有自己标定好的校准温度，档 `"1"` 下的 p 是完整有效的概率，
不是"还没测完的临时值"。上限 3 也不是随意定的：**校准表只有三档**。

其余实现要点：

- 校准表用 `go:embed` 带进二进制，同时留一个外部覆盖路径——模型演进后不必重新构建镜像。
- 偶发拒答或截断会导致解析不到足够数字（需 ≥80 个或期望值的 55%），该次作废、不计入平均。
- 每份探测的**原始数字序列**与分数长期留存（§5.6），作废的那几份同样存。归因之所以能离线
  重算、能换校准表回溯，前提是输入还在。
- 概率是**闭集、均匀先验**下的结果：库外模型会被归到最相似的现有候选。**所以高 softmax 概率
  并不能排除「库外的非 astra 被判成 astra」**（§1.2.4）——这是准入侧的残余风险，不是可以忽略的
  细节，更不是"绝对置信度"。

⚠️ **移植正确性必须用回归测试锁住**：仓库里的
`backend/internal/service/testdata/kong_fingerprint_golden.jsonl` 含输入序列与 Python 侧归因结果，
作为 golden file 锁住移植偏差（见 `kong_fingerprint_test.go`）。没有这个测试，算法偏差会静默地变成
"把 astra 判成 sol，然后把好票全拒掉"——而表象只是"一直拿不到合格票"。

### 4.2 调度：取票由请求驱动，没有定时器

唯一的触发点是门控模型的业务请求到来：

```
请求到来（full 模式、门控模型）
  ├─ 有 verified 票，剩余 > refresh_before → 注入，放行
  ├─ 有 verified 票，剩余 ≤ refresh_before → 注入当前票放行，同时异步预取下一张
  └─ 无可用 verified 票 → 同步路径，当前请求等待：
        ├─ 缓存有满 min_ticket_age 的 unverified 票 → 验证最早的一张（零采集成本）
        ├─ 否则票据出口非 none 且 ticket_fetch_min_idle 已满 → 取票 → 立即验证
        └─ 取不到 / 验证不通过 → 拒绝该请求，按 §4.3 处置
```

三条性质：

1. **闲置期完全无活动，而且闲置本身在为下一张好票做准备。** 账号停流后票自然过期、没有任何
   后台刷新，票据出口于是开始积累静默——而静默是拿到 292 的前提（§1.1.4）。周期性刷票恰好相反：
   没人用的时候持续烧额度，同时把自己需要的静默一次次清零。
2. **不需要「近期是否有请求」这样的时间窗参数**，请求本身就是触发器。代价只有一种：异步预取
   刚发起、账号随即停流，那次取票白花——可接受。
3. **阻塞只发生在完全无票时。** 有票但接近过期走异步预取，当前请求不受影响。这也是空窗伪影
   的正解：提前量仍由 `refresh_before` 给，但提前的动作不再需要定时器托着。

**冷启动**（账号首次使用，或闲置后票已全部过期）走的就是上面的同步路径：配了票据出口时，
首个请求等一次「取票 + 验证」（约 30 秒）；等不到合格票就拒绝该请求，**不放行降智请求**。

⚠️ **同一账号的同步取票必须 single-flight。** 并发请求各自取票不只是烧额度——每次取票都是
票据出口上的一次活动，N 个并发请求会互相把静默清零，最后谁也拿不到合格票。一个账号同一
时刻只允许一次在途的取票 + 验证，其余请求等同一个结果。

**`min_ticket_age` 只作用于 observed 票。** 它压的是被动票的验证频率：上游在同一时段可能
连发多张，逐张验证纯属烧额度。主动取来的票是我们自己要的唯一一张，等待毫无意义——**fetch
票立即验证**，否则冷启动要凭空多阻塞一个 `min_ticket_age`。

**「一次无票」指一段连续无可用票的时期，不是一个请求。** 这个区分直接决定成本：缓存里的
observed 票大概率是同一段流量下收的，坏也坏在同一个原因（流量出口不静默）。若按请求理解，
每个新请求各验一张，一批坏票会被逐张验完——每张 1500 tokens，验的还是同一个事实。

规则：**同一段无票期内只验证一个 observed 候选**；失败后除非出现**新信息**，不再验证别的
observed 票。新信息只有两种——该时期**开始之后**新收到的 observed 票，或一次成功的主动取票。
**新的业务请求本身不是新信息。** 无票期在拿到 verified 票时结束。

验证请求走**流量出口**（前提 §1.2.1：票不绑取票出口——注意它尚未被实验关闭），因此不占用票据出口的静默。这是分离
拓扑带来的直接好处。

#### 取票验证任务的生命周期

三个触发点——同步冷启动、异步预取、observed 失败后的升级——**走同一个取票验证流程**，由它统一
检查：**账号是否可正常调度**（见下）、有无在途任务、出口分离是否成立（§3.2）、票据代理是否有效、
静默与冷却期限（§4.3）。任何一条不满足就终止本次尝试并记事件，没有绕过的入口。

⚠️ **本功能主动发起的请求，一律要求账号处于可正常调度的状态**——取票、指纹验证、`observe` 的诊断
探测三类都算。「可正常调度」指账号未禁用、未过期、未被限流暂停、额度未耗尽，也就是调度器此刻愿意
把业务派给它。两条理由：异常账号上的探测既烧额度又可能加重问题（例如已被限流还继续发请求），而且
**异常状态下测出的档位不代表正常状态**，拿它当结论会误导。不满足就跳过并记事件，**不排队等待**。

业务请求驱动的路径天然满足这条（请求能落到该账号，说明调度器认为它可用），但**主动探测不经过调度
器**，必须自己检查。

| 边界 | 规则 |
|---|---|
| 已有在途任务 | 加入等待，**不另起一个**——并发取票会互相清零静默 |
| 等待者超时或断开 | 只结束该等待者。**已发生的出口活动无法撤销**，所以任务继续跑完、结果入缓存 |
| 全部等待者都离开 | 任务照常完成：它的产物是一张票，对下一个请求仍有价值 |
| 同账号不同模型的等待者 | 账号级 single-flight 会让模型 B 的请求等在模型 A 的任务上。任务结束后 **B 必须重新判定**：A 的票与结论对 B 不构成准入依据（§2.4 的 `(account, model)` 绑定）。B 要么用自己的候选走验证，要么按无票处置 |
| 结果晚到 | 落库前重新校验模式、账号与出口配置还是发起时那套；已变更则丢弃结论、只记事件 |
| 进程重启 | 在途任务不保证恢复。票缓存在库里不丢——**重启后先查库，仍然有效的 verified 票直接接着用**；只有确实没有可用票才走同步路径 |
| 验证的总尝试上限 | 三档校准温度**不等于**最多三次网络请求——无效回答（拒答、截断、数字不足）不计入平均，但重试必须有次数上限与整体超时，否则一次验证能无限拖下去 |

失败终点按类型定义，它们的后续处置并不相同：

| 失败类型 | 候选票 | 是否进冷却 |
|---|---|---|
| 长度命中 denylist | 丢弃，不入缓存 | 否——没有消耗验证成本 |
| 取票请求网络错误、上游拒绝 | 无候选产生 | **是**——出口活动已经发生（§4.3 里的 A 已被刷新） |
| 取票成功但响应没带 state | 无候选产生 | **是**——同上，出口已被用掉。这种情况本身值得记事件：它意味着上游认为该请求已有有效票，与我们"无票"的认知冲突 |
| 归因非 astra 或判不确定 | rejected | 来源 fetch 进冷却；来源 observed 按 §4.3 升级 |
| 验证请求未被上游接受（§2.4） | 留在 unverified，但本段无票期内不再选它 | 否——结论作废，这张票并没有被证伪 |
| 票在等待或验证期间过期 | 丢弃 | 否 |

**「没被证伪」不等于「现在可以重试」。** 候选未被上游接受，说明本轮上送根本没带上它（多半是连接
复用，或它已被新票替换），重新选中同一张只会重复同一个无效尝试。所以它留在缓存里——它可能仍是
好票——但**在本段无票期内不再被选中**；要恢复资格，需要与 §4.2 同一套「新信息」：收到新的 observed
票，或一次成功的主动取票。这样既不会无限重试同一张，也不挡住后续合格新票恢复服务。

#### 串行化有两个维度，都是必需的

一个账号同一时刻只允许一次在途取票 + 验证；**同一个票据出口也只允许一次**。

出口维度不是冗余：静默是按**出口 IP** 积累的，两个账号配同一个 `direct`（都用服务器本机 IP）或
同一个代理时，并发取票会互相把静默清零，最后谁也拿不到合格票。只按账号串行拦不住这种情况。

出口被**另一个账号**占着时直接拒服，不等待——它产出的票属于那个账号，等它没有意义。

**认领到任务之后必须重新核对调度前提**再动网络：决策是在认领之前算的，两者之间前一个任务可能刚
结束，那时的「可以取票」到此刻已经不成立，照旧决策再取一次就把刚积累的静默清零了。

#### 任务有总期限，等待者有单独的期限

后台任务不跟随调用者的 context（产物对下一个请求仍有价值），但必须有自己的总期限
（`kongTaskBudget`）：各个 HTTP 调用自己的超时管不住整条任务——数据库调用、代理解析、三份挑战
串起来的总时长都在它们之外，而卡住的任务会一直占着账号与出口的槽位。

等待者另有一个明显更短的期限（`kongWaitBudget`）：任务最坏要跑十几分钟，让一个业务请求挂那么久
毫无意义。等待超时只结束该等待者，任务继续跑完。

### 4.3 验证失败的处置

```
rejected 之后：
  该票来自 observed，票据出口非 none → 升级：经票据出口主动取票再试
  该票来自 observed，票据出口为 none → 无处可升级，拒绝该请求
  该票来自 fetch                     → 退避：进入冷却期，期间不再取票也不再验证
```

`none` 的账号没有升级退路，这是它的固有代价——换来的是完全不碰主动取票（不烧额度、不动任何
出口的静默）。

冷却期是**全局配置，不设硬下限**。直觉上它该长于静默门槛——否则冷却结束时出口还没闲够，
取回来仍是坏票，退化成"用验证成本反复确认同一件坏事"。但静默门槛本身的观测并不可靠
（§1.3.1），拿它去约束另一个参数只会放大不确定性。所以给一个经验默认值，由 §5.4 的数据校准。

**失败原因不要硬归因。** 一次发生在远超已知门槛的静默之后的失败，只能说明「再多等一截没有已知
的理论收益」，**不能反推原因一定不是静默不足**——门槛本身就没测准（§1.3.1），astra 的成因更是
未定（§1.2.3）。

**失败的代价是一段拒服。** 它的长度不能用「静默门槛」一个数概括——必须按下面的口径算，
因为静默与冷却的**起算点不同**：

```text
A = 票据出口最后一次活动的时刻     M = ticket_fetch_min_idle
F = 失败判定的时刻                 C = verify_fail_cooldown
E = 当前可用票的过期时刻           L = 下一次尝试到验证完成的耗时

下一次尝试 = 不早于 max(A + M, F + C) 的首个业务触发
拒服时长   = max(0, 下一次尝试 + L − E)
```

**`A ≠ F`是关键**：取票本身要花时间、验证更慢，所以失败判定总晚于最后一次出口活动。这条口径
纠正了三处容易算错的地方：

- **不存在「拒服 25 分钟」这个上界。** 那个数来自 `A=F=3300、L=0、后续请求立即到达`的简化例子，
  只是量级。失败耗时 30 秒、恢复再耗 30 秒就变成约 26 分钟；再失败一次继续延长。
- **预取失败后不能立即重来。** 一次刚刚接触过出口的失败，已经把静默重新计时了——此时距「静默
  重新满 M」比长期闲置后的冷启动更远，而不是更近。用默认值构造：请求在刷新窗口末尾 `t=3590`
  才到达 → 预取在 `t=3595` 产生出口活动 → `t=3610` 失败 → 最早下一次尝试 `t=5410` → 恢复约
  `t=5440`，**拒服 30 分 40 秒**。
- **`C = M` 不等于「不额外加时」。** 两者起点不同（C 从 F、M 从 A），所以 `C = M` 时实际期限是
  `max(A+M, F+C)`，仍比单独的静默门槛晚了 `F − A`。同理 `C < M` 也不必然让该参数失效，要比
  两个绝对期限。

想让第二次尝试落在票过期之前，需要 `refresh_before ≥ M + 取票耗时`，可那样取票间隔只剩
1700s，低于门槛。写成不等式：**TTL(3600) < 2 × M(1800) + 耗时**——在「当前票由上次取票产生、
两次尝试各自要重新积累 M」这个配置模型下，一个票周期装不下两次静默都充足的尝试。它是该模型
的结论，不是上游静默门槛的物理定律；但在本设计选定的参数下它成立，调参解决不了：缩短间隔只会
把「偶发失败 + 长中断」换成「持续失败」。

**当前接受这个代价，票据出口只有一个。** 把中断压到秒级的唯一办法是备用出口（两个出口各按
自己的周期走、互不干扰，A 失败即换 B），代价是出口配置从单值变成有序列表，数据模型与界面
都要跟着动。另外多账号池本身会大幅削弱影响——某个账号拿不到票时调度器避开它即可，**只有单
账号部署才会让账号中断等于服务中断**。

### 4.4 部署前提：静默这个量必须可信

整套调度都建立在"票据出口已静默 N 秒"这个判断上，而系统只看得见**自己产生的**活动。所以先把
适用范围限定住，本期不做分布式协调：

| 前提 | 不满足会怎样 |
|---|---|
| 票据出口是**固定 IP** | IP 会变的出口（拨号、动态住宅代理）上，静默时长根本无从归属 |
| 票据出口**专用**，不承载任何账号的业务流量 | 别的账号在用它，静默被清零而本系统看不见 |
| 一个票据出口只由**一个协调者**调度（单进程，或多进程共享锁） | 两个进程各自认为"已静默 30 分钟"，同时取票、互相清零 |
| 取票的串行范围是**出口**，不只是账号 | §4.2 的 single-flight 是账号级的：它挡住了同一账号的并发取票，但两个**不同账号**共用一个票据出口时，各自都满足账号级约束，仍会互相清零 |

**界面上要说清那个数是什么**：它是"距本系统最后一次使用该出口"的时长，**不是实际静默**。存在
系统外的活动时，前者会**高估**后者——所以不能把它显示成"现在一定能取到好票"。事件表记的是同一
口径（§5.3）。

`A`（§4.3 的最后活动时刻）取**该次取票最后一次网络活动**的时刻，不是请求开始时刻：取票本身有
耗时，用开始时刻会把静默算多。

### 4.5 成本

验证一次约 1500 tokens 输出、25 秒，**是真实生成消耗**。不设预算上限（已定），但事件表
要能算出每账号的验证次数与失败率，否则票源不稳时的额度消耗无从察觉。

## 5. 数据模型

### 5.1 账号配置存进 `accounts.extra`，不加列

三项配置——`codex_ticket_mode`、`ticket_egress`、`ticket_proxy_id`（后两者的三态语义见 §3.1）——
放进上游**已有的 `extra` jsonb 字段**，键名带 `kong_` 前缀避免与上游将来的实现撞名。

**理由是 fork 成本，而且这条差距很大。** 上游用 `ent` 且把生成代码全部入库（三百多个文件）。给
`accounts` 加一列就必须重新生成 ent，而重新生成会改动 `ent/migrate/schema.go`、`ent/mutation.go`、
`ent/client.go`——这几个文件上游每周都改数次。**生成代码的 rebase 冲突是最难手工解决的那一类**，
为三个配置项换来每次跟进都要处理它，不值。`ent/schema/account.go` 本身倒是很稳定，但它不是问题所在。

代价与应对：

- **失去 `ON DELETE SET NULL`**：代理被删后 `ticket_proxy_id` 会变成悬空 id，而不是自动清空。
  但这不新增负担——§3.1 已经要求取票前自行检查票据代理（到期同样不会触发上游的清扫），「代理还
  存在吗」正是同一次检查的一部分。**外键本来就不是这套设计的依赖**，去掉它反而让两条失效路径
  （删除、到期）走同一个判定。
- **失去类型约束**：读取时必须校验取值，非法值按 `none` / `off` 这一侧的保守语义处理并记事件。
- **查询稍绕**：`extra->>'kong_ticket_mode' = 'full'` 这类条件可以用表达式索引；本功能的账号数量
  是个位数到十几，索引与否都不构成问题。

### 5.2 三张新表：纯 SQL，不走 ent

票缓存（§5.3）、事件（§5.4）、指纹探测记录（§5.6）三张表**用手写 SQL 迁移 + 手写 repository**，
理由同上——新增 ent schema 一样会触发全量重新生成。上游本身就有这个先例：`audit_log_repo.go`、
`auth_cache_invalidation_outbox_repo.go` 等直接持有 `*sql.DB`，照它们的形态写即可。

迁移文件用 **`9xx_kong_*.sql` 独立编号段**——上游在 `238_` 附近递增，占 900 以后永不撞号。三张表
都是本 fork 独有的新表，除了编号段不与上游产生任何交集。

### 5.3 票缓存表（短期）

存 `state` 原始值（验证与注入都要用它）、长度、来源、状态、归因结果、
`captured_at` / `expires_at`。

**归因结果存三样，分工是刻意的**：`fingerprint_model` 与 `fingerprint_p` 是**证据**（最像哪个、
有多像），`fingerprint_probs` 是**完整分布**。采纳与否不入库——它按当前白名单对分布求和得出
（§4.1）。白名单来自配置、可以改，把结论固化进票行就只剩"照旧放行"（可能放行白名单外的档位）
与"一律作废"（把合格票也丢掉）两个都不对的选项。

**`expires_at` 怎么来必须写明**：能从票本身解析出签发期限就用解析值；只能按「收到时刻 + TTL」推算
的话，它是一个**下界估计**——被动收到的票可能已经用掉了一部分寿命——此时 `refresh_before` 的余量
必须覆盖这个误差，否则会在票实际已过期之后才开始预取。

**它天然是短生命周期的**：票过期即无用，按 `expires_at` 清理。另需限制每
`(account, model)` 保留的条数——`off` 模式也收票，高流量账号会快速堆积。

**入库边界上有三件事**，fetch 与 observed 两条路径共用：

1. **票原值唯一**：`(account_id, model, state)` 上有唯一索引，靠 `ON CONFLICT DO NOTHING
   RETURNING` 原子区分「新票」与「重复票」。先查再插在并发下会双双判为新票。重复票保留原 id、
   期限与状态——重复出现不是新信息，不该凭空延长寿命，也不该把一张已 rejected / skip 的票重新
   变成候选。
2. **长度黑名单**：312 是已实测的降智档位长度，照样会被上游接受。不拦住它就会进候选、烧掉一次
   完整验证（三份挑战、每份几百 token），而结论是注定的。292 **不在**黑名单里——同为 292 的票也
   可能不同，长度只能否定、不能肯定。
3. **顺手清掉已过期的行**：过期票没有任何用途，票原值本身也是敏感串。清理必须是**请求驱动**的
   （插入新票时顺带删一批），不能加定时任务——这套设计刻意没有定时器，闲置期必须完全无活动。
   清理只按 `expires_at` 判断，动不到仍然有效的当前票；`kong_fingerprint_probes` 不参与清理，
   它按 §5.6 长期留存。

⚠️ 探测记录里的累计概率是 **argmax 的**，而采纳看的是白名单内的概率之和（§4.1）。所以页面上会
出现「累计概率 0.6 却通过了」——那不是矛盾。判据用的那个数字记在 verify 最终事件的
`accept_mass` / `accept_models` 里，看探测列表解释不了放行原因时去看事件。

### 5.4 事件表

取票、收票、验证、注入失败、回捞、进入冷却、票据出口配置失效（§3.1 那种按 `none` 降级的
状态），逐条记录。除显而易见的字段（账号、模型、事件类型、状态码、`proxy_id`、票长度、
指纹结果）之外，**必须记该出口距上次活动的静默时长**。

理由：这让"多久静默才能拿到合格票"从一次性手工实验变成持续观测。现在知道的"34 分钟必开、
19 分钟有时够"全是手工试出来的，而上游判据随时可能变；有了这一列，`成功率 vs 静默时长`
是一句 SQL。

### 5.5 `usage_logs.features`（jsonb）

记 `ts_len`、`ts_injected`、`ticket_id`。**不存 state 原始值**——信息熵高、长期留存无意义，
需要原值的场景（验证、注入）都在票的 TTL 内，由票缓存表承担。

这一条与 §5.6「长期存指纹的数字序列」不矛盾，两者是不同性质的数据：state 过期即彻底无用，
**留着也不能回答任何新问题**；数字序列是归因算法的完整输入，**换算法或换校准表后可以重新
回答旧问题**。判据是「将来能否据此重算出新结论」，不是体积或熵。

用 jsonb 而非固定列：特征会持续增加，每加一个就迁移一次很快失控。

顺带一个零成本能力：**响应仍携带 state 就意味着注入没生效**（上游规则：带了有效票就不
下发）。每个请求自动做一次有效性检测。但它**判不了档位**，档位只能靠指纹——两者互补。

两条限定（细节见 §2.4）：这个信号必须属于**本轮上送**才有意义——复用连接留下的旧握手响应头不算；
而「没有 state」也只在能证明本轮确实带票上送时才等于「注入生效」。

### 5.6 指纹探测记录表（长期留存）

**每次指纹探测的原始数字序列都要存下来，长期保留。** 这张表与事件表分开，因为一次验证
包含 1~3 份探测（§4.1 的份数递增），是 1:N 关系，塞不进验证事件那一行。

每份探测记录：所属票与验证事件、第几份、用的哪条挑战、**原始数字序列**、解析到的数字个数、
与各模型中心比较的分数向量、该份的单独归因结果、累计到该份时的平均分与 softmax 概率、
用的哪档校准温度、耗时与 tokens。

**光有数字序列还不够——记录必须能脱离票缓存独立解释。** 票按 §5.3 过期即删，而这些记录要留很久，
所以三样东西得随记录快照下来，不能只存一个指向已被删除的票的外键：

1. **解释上下文**：账号、被验证的那个模型、票的稳定标识与来源、采集与验证时走的出口、当时的空闲值、
   时间，以及该次验证的最终处置。
2. **归因资料的版本**：挑战集、模型中心与环境方向、校准表各自的版本。档 `"1"` 不是一份永不变的
   资料——换了校准表，同一序列会算出不同概率。没有版本标识，历史结论既不能复核也不能重算，而"能
   重算"正是长期留存的全部理由。
3. **有效性与处置分开记**：每份记「解析是否有效、是否计入平均」，整次验证记「最终处置」。

⚠️ **作废的原因不止解析失败。** 候选未被上游接受（§2.4）、配置变更导致晚到结果作废、验证期间票
过期，都会留下**格式完全正常**的数字序列。它们作为观测样本有价值，但**不能当成该候选的有效指纹**,
尤其不能用来标定票的档位——否则将来一次离线重算就会把"当时根本没生效的一次探测"算成该票的证据。

**离线重判不得覆盖原始判定。** 当时系统据以做出决定的那条结论要原样保留，重算结果另存——否则事后
就分不清"当初判错了"和"现在用新校准表看法不同"。

**为什么长期存序列**：数字序列是归因算法的**完整输入**——§4.1 的两个特征（Hellinger 中心
相似度、有序块特征）都由它算出，存了它就能离线重算任何归因变体。这带来三件当下做不到的事：

- **换校准表后能回溯重判**。模型演进会让校准表过时（§4.1 留了外部覆盖路径），届时历史数据
  能用新表重算，而不是只剩一堆无法复核的结论。
- **能自建、修正校准库**。外部校准表是别人在别的环境下标定的；自己积累的序列配上其它手段
  确证的档位，才有可能做本地标定。
- **是移植正确性的持续语料**。§8 那条「归因移植偏差」风险目前靠一份离线 golden file 兜着，
  线上采集的序列 + 当时的归因结果本身就是不断扩充的回归语料。

⚠️ **解析失败而作废的探测也要存**，连同作废原因（拒答、截断、数字不足）。作废率本身是信号：
它上升可能意味着上游改了行为或触发了风控，而不只是"运气不好"。不存就只能看到"没拿到结论"，
看不到为什么。

**不存原始回答文本**——数字序列已是它对本算法的完整提取，文本体积大一个量级。代价是将来若
要用文本类特征（而非数字特征），历史数据用不上、只能重新采集。

体积量级：每份探测的序列约 1 KB，每账号每天约 24 轮验证，即每账号每天几十 KB。默认不做自动
清理——它是分析资产，不是缓存。

## 6. 管理界面

需求是「票的刷新有数据库记录、并能在 web 上查看」。形态由一个工程事实决定，不是偏好问题。

### 6.1 票据配置不进账号表单

上游的账号编辑与新建是两个各自维护的手写巨型模板（`EditAccountModal.vue` / `CreateAccountModal.vue`，
均 5000 行以上），**没有 schema 挂载点**——新字段只能插在某两个 `<div>` 之间，且要在两个模板里
各写一遍；账号类型集中在单体 `src/types/index.ts`。这几个文件都是上游每周改数次的最热文件。

所以：**票据配置和状态、日志一起放进本 fork 自己的页面，不碰账号表单。** 代价是配置分散在
两处（账号的其它设置仍在账号页）。换来的不只是避开冲突——配置只有一个编辑入口，不必再在
Create / Edit 两个模板之间保持字段同步。

这也让「独立小服务渲染」这个备选方案失去了意义：它的全部优势是「零 rebase 成本」，而一旦
配置不进上游表单，页面本身的增量成本已经降到几行追加，再拆一个服务只是多一处鉴权与入口。

### 6.2 单页三区

```
┌─ Codex 票据 ─────────────────────────────────────┐
│ 账号 [OpenAI-PH-610542 ▾]                        │
├─ 配置 ───────────────────────────────────────────┤
│ 模式      ( ) off   ( ) observe   (•) full        │
│ 流量出口  PH-proxy            ← 只读，链到账号页  │
│ 票据出口  ( ) 不设置  ( ) 直连  (•) 代理 [..▾]    │
├─ 当前状态 ───────────────────────────────────────┤
│ 票 #1a2b  astra (p=0.97)  剩余 41m  来源 fetch    │
│ 未验证 2 张 │ 冷却 无 │ 票据出口空闲 12m（本系统可见）│
├─ 事件 ───────────────────────────────────────────┤
│ 时间 │ 类型 │ 出口 │ 静默时长 │ 票长 │ 结果       │
└──────────────────────────────────────────────────┘
```

- **票据出口的三态在 UI 上就是一组单选 + 条件下拉**，「不设置 / 直连 / 代理」各占一项。反过来
  看：若界面上只能用一个「留空即直连」的下拉框表达，那就是在界面层重现 §3.1 要消除的歧义。
- **「票据出口空闲 N 分钟」是运维最关心的那个数**，但它的口径必须按 §4.4 说清：那是距**本系统**
  最后一次使用该出口的时长，**不是实际静默**。系统外的活动看不见，所以这个数会**高估**真实静默
  ——界面不能把它显示成「现在一定能取到好票」。
- **聚合统计（成功率 vs 静默时长）不做成图表。** 样本量还在个位数的阶段，固定图表只会让噪声
  看起来像规律；那是探索性分析，事件表能查即可。请求级特征（`ts_len` / `ts_injected`）同理
  不做新页面，挂在已有的请求日志详情里。
- 验证事件可以点开看该次的探测明细（几份、每份的归因结果与概率、有没有作废）。但 §5.6 存下的
  **原始数字序列不进界面**——几百个数字在页面上看不出任何东西，它的用途是离线重算，走 SQL 或
  导出即可。

### 6.3 隔离约定

- 页面代码全部放 `src/features/codex-ticket/`，自带 `api.ts` / `types.ts` / `components/`。
  上游的 `features/prompt-audit/` 就是这个形态，隔离范例来自上游自己。
- **我们的 API 自给自足，不调用任何上游 admin 端点。** 账号列表、代理列表、票状态都由自己的
  端点返回。复用上游的 accounts API 等于把 fork 绑在它的响应形状上，而那是个高频改动面。
- UI 组件只读复用（`TablePageLayout` / `DataTable` / `Pagination` / `Select`，事件表可用
  `useTableLoader`）：import 不产生冲突，props 变化会在编译期暴露。
- **页面文案直接写中文，不进 i18n。** `localeKeyCompleteness` 测试只扫 `t()` 字面量，不用
  `t()` 就不受它约束；只有侧边栏 label 需要一个 key。

对上游文件的改动全是追加型单行：

| 文件 | 改动 |
|---|---|
| `frontend/src/router/index.ts` | 一个路由对象 |
| `frontend/src/components/layout/AppSidebar.vue` | `baseItems` 一行 |
| `frontend/src/i18n/locales/{en,zh}/common.ts` | 侧边栏 label 各一行（双语必须同时加，否则 build 失败） |
| `backend/internal/server/routes/admin.go` | `registerKongTicketRoutes(admin, h)` 一行（该文件已是一长串同构注册行） |
| backend handlers 聚合结构体 | 一个字段 |

### 6.4 端点

| 端点 | 用途 |
|---|---|
| `GET /admin/kong/ticket/overview` | 账号列表 + 各自的配置与当前票状态（区 1、2） |
| `PUT /admin/kong/ticket/accounts/:id` | 改 `codex_ticket_mode` / `ticket_egress` / `ticket_proxy_id` |
| `GET /admin/kong/ticket/events` | 事件分页，按账号、类型、时间筛（区 3） |

⚠️ 不要沿用上游 `proxy_id` 那个「提交 `0` 表示清除」的约定（前端把 `null` 转成 `0` 发出）。
`ticket_egress` 是显式枚举，空值语义不靠哨兵数字表达——那正是 §3.1 要避免的歧义。

## 7. 配置项

**全局**（只剩与调度节奏相关的，不再有启用开关与出口）：

| 项 | 作用 | 默认 |
|---|---|---|
| `refresh_before` | 票剩余时间低于此值即异步预取下一张 | **300s** |
| `ticket_fetch_min_idle` | 两次主动取票之间该出口的最小静默 | **1800s** |
| `verify_fail_cooldown` | `fetch` 来源的票验证失败后的退避期 | **1800s** |
| `min_ticket_age` | **observed** 票入缓存后至少等多久才验证（fetch 票不受此限，见 §4.2） | 300s |
| `observe_probe_interval` | `observe` 模式的探测周期 | 3600s |
| `state_len_denylist` | 已知的坏票长度 | `[312]` |
| `accept_extra` | 各门控模型额外接受的归因结果（自接受由代码补齐，见 §4.1） | 空 |

门控模型集合走环境变量 `KONG_TICKET_GATED_MODELS`（留空则整个功能不装配）；`accept_extra` 在
上游 config 结构里，键路径 `gateway.kong_codex_ticket.accept_extra`，因此也可用
`GATEWAY_KONG_CODEX_TICKET_ACCEPT_EXTRA` 设定（viper 的 env 绑定）。格式
`model:accepted[,accepted…][;model:…]`。**没有热更新**——上游没有配置文件监视，config 只在启动时
读一次，改了要重启。

### 7.1 为什么 `refresh_before` 要小

**取票间隔 = TTL − `refresh_before`**，而这个间隔就是票据出口积累到的静默时长。静默是拿到 292
的**必要条件**（§1.1.4）——注意它不等于「astra 的充分条件」，292 桶内还分档，成因未定（§1.2.3）。
所以这个参数越小，可积累的静默越多、越有机会拿到好票：

```
refresh_before = 300s   → 间隔 3300s（55 分钟静默）
refresh_before = 1900s  → 间隔 1700s（28 分钟静默）← 踩在实测门槛下沿
```

**直觉会把提前量往大调**（怕空窗），而它挤掉的正是静默。这个取舍不是请求驱动独有的——定时器
方案里只要「票剩余不足即刷新」，提前量同样会压缩间隔（v026 实例把提前量设到 1900s 就把间隔压到
了 28 分钟）。区别只在请求驱动下没有一个独立的 `interval` 参数兜着，取舍暴露得更彻底。

由此还能否掉一个看似合理的想法：**「给预取失败留重试余量」是自我拆台**。静默是目前唯一已知
可控的杠杆（§1.2.3），而加大 `refresh_before` 恰好缩短它——用可能提高失败率换来的重试机会，
重试时多半还是坏票。

下界只由「一次预取要多久」决定：取票几秒 + 验证最多三份（三份都一次成功约 75 秒），300s 留了三倍
余量。**75 秒不是上界**——允许无效回答重试之后，真正的上界由重试次数与整体超时决定（§4.2）。

⚠️ **但预取失败或超时的代价远不止「那个请求多等一会」。** 预取一旦接触过票据出口就把静默重新
计时了，之后的同步路径会撞上 `ticket_fetch_min_idle` 这道门——此时的处境比长期闲置后的冷启动
更差，不是更好。具体算法见 §4.3 的时序口径；用默认值算出来是约 30 分钟拒服。

### 7.2 其余取值的依据

- **`ticket_fetch_min_idle` = 1800s**：实测「PH 出口空闲 30 分钟后会拿到 292」的直接对应值。
  正常路径上它根本不起作用（间隔 3300s 远超它），只在异常路径（预取失败后重试、冷启动紧接
  上一次取票）上当门。在那种时刻**宁可拒服，也不要浪费一次把静默清零的取票**——所以这个门
  宁高勿低。
- **`verify_fail_cooldown` = 1800s**，与 `ticket_fetch_min_idle` 取同值。**取同值不等于「失败后
  不额外加时」**——两者起算点不同（C 从失败判定时刻 `F`、M 从最后活动时刻 `A`），实际期限是
  `max(A+M, F+C)`，仍比单纯的静默门槛晚了 `F − A`（§4.3）。取同值的理由是：失败发生在远超已知
  门槛的静默之后，再多等一截**没有对应的理论收益**；但也不宜更小，否则它在多数分支里不起作用。
  保留可调，若数据显示「等更久确实更容易成功」再抬高。
- **`min_ticket_age` = 300s**：真正限制 observed 票验证频率的是 §4.2 的结构（只在无票时验、
  每次只验一张），不是它。它只负责在同一批票里偏向较早收到的那张，取值不敏感。
- **`observe_probe_interval` = 3600s**：与票 TTL 同尺度，每个票周期取一个采样点。成本每账号每天
  约 36k tokens 输出——那是"持续有样本、每次只需一份"的下限，需要三份时约三倍，不是固定值。

**账号级**：`codex_ticket_mode`、`proxy_id`、`ticket_egress` + `ticket_proxy_id`（见 §3）。

`state_len_denylist` 刻意做成"拒绝已知坏的"而非"要求特定长度"：上游改长度时，前者只在
出现新坏长度时需要人工介入，后者会让整个特性静默失效并触发拒绝服务。

### 7.3 值域与组合约束

默认组合 `300 / 1800 / 1800 / 300 / 3600 / [312]` 之间没有代数矛盾。但参数一旦可调，就不能再只
靠默认值推导——每个参数各有约束：

| 参数 | 约束 |
|---|---|
| `refresh_before` | 必须小于票的可用寿命；要真正避免续期空窗，还得覆盖「请求到达延迟 + 取票验证耗时」 |
| `ticket_fetch_min_idle` | 约束**所有**取票入口。若它大于可用续期间隔（`TTL − refresh_before`），预取会被这道门推迟——**推迟不等于空窗**，见下面的条件 |
| `verify_fail_cooldown` | 与静默门槛**共同**决定下次尝试时刻（§4.3 的 `max`）。作用域按 **`(账号, 票据出口)`**——它退避的是取票，而取票消耗的是出口静默（§1.1.4）；它**不阻止 observed 验证**，那条路径不碰票据出口 |
| `min_ticket_age` | 取值 `≥ TTL` 会让被动票永远得不到验证资格。选候选时还要看**剩余寿命**，不能只看存放了多久 |
| `observe_probe_interval` | 是相邻两次探测的**最小间隔**，不是定时周期（§2.3） |
| `state_len_denylist` | 只做廉价排除。不在表里**不等于**合格，仍须过指纹（§2.2） |

无空窗续期的条件要按**实际完成时刻**写，不能按"能不能立刻开始取票"写：

```text
无空窗 ⟺ Q + L ≤ E
  Q = 刷新窗口开启后，同时满足静默与冷却期限的首个业务触发时刻
  L = 该次取票到验证完成的耗时
  E = 当前票的过期时刻
```

`L ≤ refresh_before` 是其中一项必要条件。而 `ticket_fetch_min_idle ≤ TTL − refresh_before`
**只是"预取不被静默门槛推迟"的理想条件，不是无空窗的必要条件**——反例：`M = 3400`、`A = 0`、刷新
窗口在 3300 开启，取票要等到 3400 才允许开始，但 `L = 30` 时它在 3430 完成，早于 `E = 3600`，空窗
为 0。把这两件事混为一谈，会让参数提示和保存校验拒绝掉本来可用的配置。

**会让某条路径真正不可达的配置应当拒绝保存或明确提示**，而不是等运维从事件表里反推。

**哪个条件约束哪一步**，不要混在一起谈——混了就会出现"因为出口还没静默够所以连缓存里的票都不验"
这类错误实现：

| 条件 | 约束范围 |
|---|---|
| 账号可正常调度（§4.2） | 本功能主动发起的**全部**请求：取票、指纹验证、`observe` 探测 |
| 出口分离（§3.2）、票据代理有效 | 只约束**主动取票** |
| 静默门槛 `M`、冷却 `C`（§4.3） | 只约束**主动取票**——验证走流量出口，不消耗票据出口的静默 |
| 无票期内的候选限制（§4.2、§4.3） | 只约束 **observed 候选的重复验证** |

## 8. 风险与退出

| 风险 | 触发信号 | 应对 |
|---|---|---|
| **上游收紧校验**（校验 turn / 连接绑定） | 注入后响应**仍下发**新票，或请求开始报错 | `mode=off` 一键退回。这个开关必须始终可用 |
| 上游改票长度 | 出现既非已知合格也非 denylist 的长度 | denylist 机制只告警不拒服 |
| 票据出口失效 | 连续取票失败 | 事件表可见。票据出口是单点，失败即一段拒服，长度与边界见 §4.3 |
| 归因移植偏差 | 合格票被判为非 astra、成功率异常低 | §4.1 的 golden file 回归测试；§5.5 留存的线上序列持续扩充语料，也可回溯重判历史结论 |
| 冷启动阻塞触发客户端超时 | 首个请求约 30 秒无响应 | 首字节延迟的容忍度由调用方决定。实测触发超时就改成直接拒绝并给明确错误、由调用方重试，**不降级放行** |
| 账号风控 | 档位下降、授权失效 | 注入违反客户端契约的固有代价，**不可完全规避** |

**退出条件**：若上游变更使注入失效且无法在合理成本内恢复，**放弃该特性**而不是层层加码
绕过——继续对抗只会把风险推到账号上。

## 9. 待定

- 默认值已按 §7.1 / §7.2 定下，但 `ticket_fetch_min_idle` 与 `verify_fail_cooldown` 仍站在少量
  手工探测上（§1.3.1），**要靠 §5.4 的事件数据校准**——「成功率 vs 静默时长」是最该先弄清的
  一条。`refresh_before` 不在此列：它由 TTL 与预取耗时决定，与静默门槛无关。
- 票缓存每 `(account, model)` 的保留条数。
- 升份数的置信阈值（初始取 0.9 —— astra/luna 的区分度 p 普遍 >0.94，落到 0.9 以下即视为
  采样不干净）。**是否要分档设阈值**待定：档 `"1"` 的 p=0.9 与档 `"3"` 的 p=0.9 背后证据量
  不同，严格些可以证据少时要求更高（如 0.95 / 0.90 / 0.85），但也可能是过度设计——先用单一
  阈值跑，看数据是否支持区分。
- **`full + none` 的产品语义**：现在定义为「依赖外部票源、不承诺自主恢复」（§3.1）。另一条路是
  保存时直接禁止该组合——更不容易误用，但也挡掉了"账号上另有非门控流量在养票"这种正当场景。
- **是否允许跨模型移用原始票**（§3.1）：允许则 `full + none` 有一条真正走得通的补给路径，代价是
  §5.2 的缓存键与候选选择都要按"原始票可跨模型、验证结论不可"来表达；不允许则该组合只剩手工养票
  一条路。两者都不影响"验证结论按 `(account, model)` 绑定"这条不变量。
- 票据页是否需要跨账号总览。当前设计用账号选择器（§6.2），账号数量上去之后可能需要一张列
  所有 `mode ≠ off` 账号的表——等真的不够用再加。
