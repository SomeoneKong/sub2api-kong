# Codex 票：被动收票与请求特征

**状态**：已实现，无配置项。只观测，不改变任何请求：不注入、不拒服、不拦截交付，任何一步失败都不影响转发。
账号的指纹测试是另一个功能，见 `DESIGN-openai-fingerprint-test.md`；仓库组织与发布流程见 `FORK-NOTES.md`。

## 0. 票是什么

"票"指 codex 协议里的 `x-codex-turn-state`：上游下发的一段不透明 token，客户端在同一会话的后续请求里原样
回带。它出现在四个地方：

| 方向 | HTTP | 原生 WebSocket |
|---|---|---|
| 上游下发 | 响应头 `x-codex-turn-state` | 带内事件 `codex.response.metadata`（SSE 上是 `response.metadata`）的 headers；以及 WS 握手响应头 |
| 客户端回带 | 请求头 `x-codex-turn-state` | 每个 `response.create` 帧的 `client_metadata`；或 WS upgrade 请求头（由本服务放进与上游那条连接的握手头） |

票是上游的加密认证串，**不从它的长度或前缀推断任何东西**：长度只是明文长度的旁路泄露，上游一改明文结构就
变（2026-09-23 就变过一次）。本 fork 曾用它做验票、注入与拒服，那套功能已删除（完整实现在归档分支
`archive/pre-ticket-slimdown`）。现在只做两件事：

- **G1 收票**：被动收下上游下发的票，按原值去重存进票表，保留 30 天。不提供查看界面，需要时直接读库。
- **G2 请求特征**：每条用量行记下这一轮**发往上游**的票与**上游下发**的票各自的长度和指纹，能看出同一会话里
  票是否换了，并能按内容在票表里找到它。

## 1. 收票

**来源**：只收上游给的，不收客户端自带的。

- HTTP：所有 OpenAI HTTP 上游发送的汇聚点 `doOpenAIUpstream` 拿到响应头后读取。**不看状态码**：错误响应里
  带回的票同样收。
- WS 带内：三条原生 WS 通路（ctx_pool、HTTP→WS、passthrough）读到上游 metadata 事件时读取。客户端断连后
  上游照样在发事件，那一侧也收。
- WS 握手响应头：一条物理连接只收一次。池化通路（ctx_pool、HTTP→WS）在第一次租用这条连接的那一轮、
  写出首帧之前收（`ConsumeHandshakeTurnState` 一次性交出），passthrough 在它自己拨号之后收。

**范围**：走 codex 协议的 OpenAI 账号（OAuth 与 setup token）上的所有模型。

**票的身份与观测上下文**：票表唯一键是 (账号, 模型, 原值)。同一张票在不同模型的请求里出现时各有一行，这些行
是同一张票的不同观测上下文。模型取本次上送给上游的模型名（HTTP 取请求体顶层 `model`，读的是发送前的明文，
不受 zstd 压缩影响；WS 取这一轮或建连时的模型），判不出时记空串照收。同一观测上下文重复出现不再入库，
`captured_at` 是首次收到的时刻。

**异步写入**（`kong_codex_ticket_collector.go`）：

- 请求路径只把 (账号, 模型, 原值, 收到时刻) 放进容量 1024 的队列；收到时刻在这时固定。
- 后台协程逐条写入。重复的静默跳过；写入失败计数并按分钟节流打 Warn，**不重试**。
- 队列满时丢弃并计数；进程退出时队列里剩下的票与停止之后才到的票同样计入丢弃，不阻塞退出，最后打一行累计计数。
- 每 10 分钟在计数有变化时打一行 `kong ticket: 收票计数`（进程启动以来累计的写入、重复、丢弃、失败、清理）。

**保留期**：按 `captured_at` 保留 30 天。写入协程启动时与之后每小时清理一次，每批 1000 行、每轮最多 50 批，
没删完或失败的留给下一轮。清理按 `captured_at` 取最早的一批，靠迁移 905 加的索引定位。

**票表列的取值**：`source = observed`、`status = unverified`、`expires_at_source = estimated`；`expires_at` 按
"收到时刻 + 1 小时"填写，只因为列非空，不据它判断任何事。验证、出口相关的列一律为空。票原值只存在票表里，
不进用量行、界面与日志。

**已知采不到的情形**（只损失观测，接受）：

- 预热后从未被租用就关闭的池化连接的握手票。
- 握手失败时响应头里带回的票。
- zstd 被上游拒绝、改明文重发时，第一次响应里带回的票。
- HTTP→WS 的 generate 预热（`prewarm_generate_enabled`，默认关闭）自己读掉的那一轮事件里的票。
- HTTP→WS 上客户端恰在等待上游事件时断开：上游原有的排空逻辑先把连接标记损坏并关闭，再用脱离取消的
  context 续读已经读不到东西，之后到达的票（与用量）都收不到。这是上游代码的行为，不在本功能里修。
- 队列满、写入失败、进程退出时的丢弃（有计数）。

握手票的收到时刻与模型是第一次租用那一轮的，而不是握手的那一刻。

## 2. 请求特征

`usage_logs.kong_request_features`（jsonb，迁移 903），只有四个键：

| 键 | 含义 |
|---|---|
| `state_len` / `state_fp` | 这一轮**实际发往上游**的票的长度与指纹 |
| `reissued_len` / `reissued_fp` | 上游在**这一轮响应里**下发的票的长度与指纹；一轮里下发了多张时是最后一张 |

**指纹**是原值 sha256 的前 12 个 hex 字符（48 bit，`kongStateFingerprint`）：足以区分相邻几轮是不是同一张票、
按内容在票表里找到它，而不可逆。

**测量口径**：

1. 按真正上送的那份载体测量：HTTP 出站请求头、WS 上送帧（跨账号剥离之后的）、HTTP→WS 的 map 形态 payload。
2. WS 上帧内优先；帧里没有时，取这条连接握手时**实际发出去**的那个值（连接池复用时本轮根本不握手，客户端
   这次带来的值一个字节都没上送，不能用它）。
3. 缺省即"没有这一项"：长度 0 表示带了空串，与没带是两件事。一项都没有时整列为 NULL，不写 `{}`。
4. 握手响应头里的票只进票表，不算进 `reissued_*`：它属于整条连接，复用时对不上具体哪一轮。

**逐轮归属**：WS 上一条连接先后服务多轮，下行事件要记到正确的一轮。

1. 记录器逐轮新建，不跨轮复用。
2. ctx_pool 与 HTTP→WS：一条连接同一时刻只转发一轮，读循环里收到的事件记进这一轮的记录器。
3. passthrough：两个方向并发转发，事件记进**最近一次上送的 `response.create`** 建的记录器。适配器不允许轮次
   重叠（上一轮的终止帧写给客户端之前又来 `response.create`，按策略关闭连接），所以只有上一轮终止之后才到的
   事件会错记：在下一轮发出之前到的，改不了已经落定的那一行（只收票）；在下一轮发出之后到的，记到下一轮上。
4. 这一轮结算时取快照（副本）。之后到达的事件只收票，不改已经落定的那一行。
5. 没有记录器（归属不明）时只收票。

**覆盖范围**：`/v1/responses` 的直通与转换、三条原生 WS 通路、WS→HTTP bridge。其他走 `doOpenAIUpstream` 的
协议适配只收票、不填这一列，空表示"没采到"。

**界面**：用量明细的"请求特征"列逐行显示 `state: 长度 (指纹)` 与 `上游回发: 长度 (指纹)`
（`frontend/src/features/request-features/`）。

**旧行**：删除前的版本还写过 `client_state_*`、`ticket_id`、`reissued_ticket_id` 等键，旧行里原样保留，界面
不再显示。

## 3. 读库

按指纹在票表里找一张票（指纹由原值现算）：

```sql
SELECT id, account_id, model, state_len, captured_at
FROM kong_ticket_cache
WHERE account_id = $1
  AND left(encode(sha256(convert_to(state, 'UTF8')), 'hex'), 12) = $2;
```

- **零行**：这张票没被收下——客户端从别处带来的、被丢弃的、超过 30 天已清理的、或属于 §1 的已知采不到的
  情形。零行不代表上游没下发过它。
- **一行或多行**：同一张票的观测上下文（多行时模型不同）。模型可以作为附加条件，但握手票与判不出模型的票的
  模型可能是空串，按模型关联会漏掉它们。

48 bit 在一个账号 30 天的量级里碰撞概率可以忽略。

## 4. 实现位置

| 位置 | 作用 |
|---|---|
| `service/kong_codex_ticket_observe.go` | 观测入口：HTTP 的发送前后、WS 事件、握手票 |
| `service/kong_request_features.go` | 逐轮记录器、指纹、各载体的取值 |
| `service/kong_codex_ticket_collector.go` | 异步写入与保留期清理 |
| `repository/kong_codex_ticket_repo.go` | 票表写入与清理（与指纹测试的落库同一个仓储） |
| `repository/kong_usage_features.go` | 用量行这一列的序列化 |

上游文件上的挂钩位置见 `FORK-NOTES.md`。

**数据表**：票表 `kong_ticket_cache` 与探测表 `kong_fingerprint_probes` 由迁移 900 创建（后者现在归指纹测试
用）。同一迁移建的事件表 `kong_ticket_events` 不再写入，已有数据原样保留，确认不需要时可手工删表。
