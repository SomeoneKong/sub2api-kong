# Codex 票据：验证与注入设计

**状态**：已实现。仓库组织、版本号与发布流程见 `FORK-NOTES.md`。

本文的事实基础来自一批独立的协议实验（门票结构、A-B-A 因果性、时序常数、客户端源码行为）。
**那批原始观测记录不随本仓库发布**，所以文中凡是「实测」「已关闭的前提」这类说法，读者只能按本文
的转述看待；下面 §1 按可靠性把前提分成三类，正是为了让转述与推断不混在一起。

## 0. 为什么不直接用上游那套

上游 PR #7315 的方案（采票 → 按长度筛 → 注入）方向是对的，A-B-A 实验证明门票能决定实际
服务的模型档位。但它解决不了问题，有四处缺陷：

| 缺陷 | 后果 |
|---|---|
| **只按长度判定合格** | 292 与 312 相差正好一个 AES 块，292 桶的明文有 16 字节变化空间，**同为 292 的票档位可以不同**（实测同账号门票全程 292，模型却从 astra 变成 sol）。长度只能判"没掉到 luna" |
| 全局开关，无账号级豁免 | 一旦开启，任何拿不到合格票的账号在门控模型上直接被拒 |
| 时间参数会自锁 | 采票本身是出口上的活动，默认 6s 轮询让首次失败后的重试永久锁死在坏票 |
| 无档位可观测 | 拿到 292 就当成功，实际档位无从得知，降智完全静默 |

本设计的核心变化：**合格判定从"看长度"换成"看指纹"**，长度只用来做第一道廉价筛除。

## 1. 设计前提

**分三类，因为它们的可靠性差得很远。** 把条件性前提当成已证事实用，是这类系统最容易犯的错：
推理链会悄悄超出证据范围，而症状只是"怎么老拿不到好票"。

### 1.1 已实测的事实

1. **票是因不是果**。同一降智态下只改"是否注入"，注入 292 票即得 astra，撤掉立刻回落 luna。
2. **票的档位在其 TTL 内稳定**。注入同一张票可将 astra 维持 47 分钟、跨越 4 分钟窗口。
   这条决定了**一张票只需验证一次**。
3. **上游"带有效票就不下发、否则下发"**。这是被动收票的机制基础，也意味着注入生效后票源
   会暂时枯竭（见 §2.1）。
4. **能否拿到 292 取决于该出口的静默状态**，按出口 IP 计、与账号是否活跃无关。票 TTL 3600 秒，
   窗口约 4 分钟。

### 1.2 尚未关闭的前提（设计依赖它们，但证据不足）

1. **票不绑取票出口。** 设计据此让验证请求走流量出口（§4.2）。但"票是否编码打票出口 IP"仍是
   **阻塞性开放问题**——当前按"不绑定"推进是一个**判断**，不是实验结论。
   它若不成立，验证请求必须改走票据出口，而那会消耗静默，§4.2 / §7 的整套节奏都得重算。
2. **合成探针的归因结论适用于真实业务请求。** Q2 未确认。"票在 TTL 内稳定"只说明同一张票的档位
   不变，不能证明不同请求条件下的归因可以通用。
3. **静默是拿到 astra 的有效杠杆。** 这条最弱：静默决定能否拿到 **292** 有观察支撑，但 292 桶内
   **astra / sol 的成因仍未确定**（Q3）。已有的 9 小时零 miss 证明的是 292 成功率、不是 astra
   成功率，而且取自 60 分钟间隔，不能直接搬到别的间隔上。
4. **闭集归因不会把库外模型误判成 astra。** §4.1 已承认库外模型会被归到最相似的现有候选，那么
   高 softmax 概率并不能排除"库外的非 astra 被归为 astra"。
5. **挑战请求带 `instructions` 不影响归因。** 校准库取的是建库脚本里 environment-01 那一组，
   其条件是**无前缀（clean）**；但 codex responses 端点的请求带着 `openai.DefaultInstructions`
   （业务转发也带，不带可能直接被拒）。环境不同会让特征整体偏移，而**归因不会报错，只会给出一个
   看起来正常的错结论**。要关闭这一条得实测：同一账号同一时段，带与不带 instructions 各采若干份，
   比较归因分布。在关闭之前，指纹结论只能当作"相对可比的信号"，不能当作绝对档位判定。

这些不确定性**不改变方向**——票据优先、指纹准入、拒服不降智都仍然成立；但**不能反过来当业务
保证**用：任何"因为静默够长所以一定拿到 astra"式的推论都超出了证据。

### 1.3 已知风险

1. **静默门槛的观测很薄**（"约 34 分钟可开、19 分钟有时够"），样本不足、上游判据不可知，
   **不应作为常数依赖**——这正是 §5.4 要把静默时长记进事件表的原因：先观测，再谈取值。
2. **注入违反 codex 的 turn 契约**（跨 turn 复用、跨连接采集）。当前上游只验签、不校验 turn
   绑定。**这是整套方案的根本风险**，见 §8。官方客户端源码把这条契约写得很明确
   （`codex-rs/core/src/client.rs` 的 `ModelClientSession` 文档）：该令牌是**每轮**的
   sticky-routing token，"received at turn start … must not send it between different turns"，
   每轮都要新建 session，跨轮重放"violates the client/server contract"。这也解释了两件观察：
   它能决定档位（令牌钉住的是路由目标，不同路由不同档位），以及长度为何不足以判定档位。官方客户端源码把这条契约写得很明确
   （`codex-rs/core/src/client.rs` 的 `ModelClientSession` 文档）：该令牌是**每轮**的
   sticky-routing token，"received at turn start … **must not send it between different turns**"，
   每轮都要新建 session，跨轮重放"violates the client/server contract"。这也解释了两件观察：
   它能决定档位（令牌钉住的是路由目标，不同路由不同档位），以及长度不足以判定档位。

## 2. 总体设计

### 2.1 票有两个来源，各有适用面

| 来源 | 成本 | 票的质量取决于 |
|---|---|---|
| `ticket_observed`：业务响应头 | **零** | 流量出口当时是否静默 |
| `ticket_fetch`：经票据出口主动取 | 一次探测请求 + 出口静默 | 票据出口的静默（可控） |

诚实的判断：**在流量持续的账号上，被动收到的票多半是坏票**——流量出口不可能静默，而
静默是合格票的前提。所以票据出口是主力，被动收票是补充，它的价值集中在两种时刻：流量
稀疏的账号，以及流量出口恰好闲过一段之后的首个请求。

还有一个动态要注意：**注入生效后上游不再下发新票，票源暂时枯竭**。当前票过期后，下一个
不带有效票的业务请求会重新带回一张——但那时流量出口正活跃，拿回来的大概率是坏票。这正是
必须保留主动取票的原因。

⚠️ **「下一个业务请求会带回一张」这条在 `full` 的门控路径上不成立。** 那条路径无票即拒服
（§4.2），请求根本不会上送上游，也就不会有响应、不会有新票。被动收票只能发生在**已经上送**
的请求上：`off` / `observe` 模式的请求、非门控模型的请求，或该账号在别处产生的流量。这个区别
决定了 `full + none` 没有自主恢复能力，见 §3.1。

### 2.2 票的状态机

**「当前票」与「候选票」是两件事，必须分开判定**——把它们混在一组分支里是上游那套方案最大的
结构问题，也是本设计里最容易写错的地方：

| | 判据 | 谁在用 |
|---|---|---|
| **当前票**（服务资格） | verified、未过期、且未被判定注入失效（§2.4） | 业务请求 |
| **候选票**（取票资格） | 已入缓存、等待或正在验证 | 验证流程 |

由此得到两条规则：**候选票失败不影响仍然有效的当前票**；候选验证通过后立即接替当前票，
但**有效期不重算**——票的过期时刻由上游签发时决定，与我们何时验证完无关。

```
收票
  ├─ 长度命中 denylist（312）→ 丢弃，记事件，不入缓存
  └─ 长度合格 → 入缓存 unverified
        ├─ 来源 fetch    → 立即验证（§4.2）
        └─ 来源 observed → 满 min_ticket_age 才获得验证资格
              ↓
          指纹验证
            ├─ astra，且验证请求本身被上游接受 → verified，接替当前票（期限不变）
            ├─ 非 astra / 判不确定             → rejected，记事件，按 §4.3 处置
            └─ 验证请求未被接受（响应仍回 state）→ 本次结论作废，候选留在 unverified（§2.4）
```

长度筛是廉价的第一道门（零成本排除已知坏票），指纹是最终判据。**两者不可互相替代**：
长度判不了档位，指纹太贵不能每票都做。**不在 denylist 里也不等于合格**——未知长度同样必须
过指纹，denylist 只负责廉价排除已知的坏长度。

### 2.3 三种模式

**收票在三种模式下都进行**，这样模式切换时缓存里已有票可用，不必从零开始。模式的差异只
在"探测"与"注入"：

| 模式 | 收票入缓存 | 自动指纹探测 | 手动验票 | 注入 | 需要票据出口 |
|---|---|---|---|---|---|
| `off` | 是 | 不（只在人工点击时验） | 可以 | 不 | 不需要 |
| `observe` | 是 | 对随请求返回的 state 探测 | 可以 | **不** | 不需要 |
| `full` | 是 | 验证缓存中的票 | 可以 | 是 | 可选，见 §3.1 |

`observe` 是纯诊断态：不改写业务请求，只回答"这个账号现在被降智了吗"。它不主动取票，完全不碰
票据出口的静默，**可以安全开在任何账号上**。它让降智的「监测」与「干预」解耦——先观察一段时间，
再决定要不要开 `full`。

**手动验票在三种模式下都可用。** 表格里的"指纹探测"一列说的是**自动**探测：`off` 不自动烧额度，
但它手上那些被动收下的票照样可以在票列表上逐张点验。验证走**流量出口**、不注入、不消耗票据出口
的静默，所以模式与它无关；只有**取票**是 `full` 专属（那是保护行为）。`off` 的票能不能验，决定的
是"该不该给这个账号开 `full`"这个判断有没有依据——验不了就只能靠猜。

**`off` 与 `observe` 的请求照常服务，即使模型在门控集合里。** 哪些账号需要保护是运维按观察结果
人工配的（观察到某个账号高概率降智才给它开 `full`），所以「这个账号没配 full」是常态，不是异常。
把它当成拒服理由，会让「设一次 `KONG_TICKET_GATED_MODELS`」把所有还没观察完的账号在该模型上全部
打死。这两种模式下的请求只是不受票据保障、也不做交付判定。

与票据出口三态（§3.1）交叉，九种组合的行为：

| | `none` | `direct` | `proxy` |
|---|---|---|---|
| `off` | 被动收票；不自动探测（可人工验）、不取票、不注入 | 同左，配置的出口不使用 | 同左，配置的出口不使用 |
| `observe` | 被动收票 + 诊断探测；不主动取票 | 同左，票据出口不使用 | 同左，票据出口不使用 |
| `full` | 验证被动候选；无候选即拒服，**且无自主恢复能力**（§3.1） | 经本机出口取票 | 经该代理取票；失效按 `none`（§3.1） |

**`observe` 也是请求驱动的**，不靠定时器：业务响应带回**新票**时，若距上次探测已满
`observe_probe_interval`，就对那张票做一次指纹探测；没有业务就没有探测。三条约束：

- **探测请求必须带着被观察的那张票发出。** 不带的话测到的是另一次请求的状态，归因结论落不到这张
  票上。这不违反"不注入"——那条指的是**不改写业务请求**。
- **结论有时效。** 界面显示"档位 + 探测时刻 + 所用票的来源"；票已过期、或结论距今超过一个 TTL 的，
  不能再显示成"现在"的状态。
- **无样本要能区分**：没收到新票、只收到 312、探测失败，三种状态各自显示，都不许拿旧结论顶替。
- **账号状态异常时不探测**：它同样是主动发起的请求，受 §4.2 那条「账号可正常调度」约束。账号被
  禁用、过期、限流暂停或额度耗尽时跳过本次探测并记事件，界面显示「账号不可调度，诊断暂停」——不是
  「无样本」，两者的处置完全不同。

### 2.4 保护边界：哪些请求受保护、保护到哪一刻

「不交付降智输出」要能端到端成立，先得说清保护从哪里开始、到哪里结束。这一节是对该目标的
执行规则，不是补充说明。

**范围**：

- **门控模型集合按最终上游模型判定**，不按客户端传来的名字——别名与模型映射解析之后再判，否则换个
  别名就绕过了整套保护。集合本身**来自配置、不硬编码**，并且要能在界面上看到当前生效的成员：判断
  「这个请求到底受不受保护」是运维的日常动作，靠读代码是不行的。
- 覆盖全部业务入口（流式、非流式及其它已有转发路径）。**新入口默认算未覆盖**，未覆盖的入口
  不得承载门控模型的请求。
- 票与验证结论按 `(account, model)` 绑定。§4.2 的 single-flight 是**账号级**的并发控制，不等于
  结论可以共享：模型 B 的等待者不能拿模型 A 的任务结果当自己的准入依据。

**交付边界的判据是「本轮证据」**：每次上送都要固定 `(本次使用的票, 账号, 最终模型, 该票的过期
时刻)`，交付前检查的是**这一次上送**是否确实带着那张票、且被上游接受。不是查此刻的全局当前票，
也不是查某次旧连接留下的握手响应头。

```
上送前：固定本次的 (票, 账号, 最终模型, 票的过期时刻)
上游响应 → 检查属于本轮的证据
  ├─ 本轮确实带该票上送，且响应未携带新 state → 注入生效，正常交付
  ├─ 响应仍携带 state → 该票没被接受
  │     ├─ 在第一个业务分片离开之前停止交付
  │     ├─ 只撤销**本次使用的那张票**（且仅当它此刻仍是当前票，才摘掉服务资格）
  │     └─ 该请求拒服，或改投一个同样满足准入条件的账号
  └─ 拿不到属于本轮的证据 → 一样不得交付（见下表）
```

**撤销对象必须是「本次使用的票」，不能是「当前票」。** 并发下两者会不同：请求带 K1 发出 → 预取
的 K2 验证通过、接替成当前票 → 带 K1 的那个请求才失败返回。按「当前票」撤销会作废无辜的 K2，
而预取已经把静默花掉了，白搭一段拒服。候选验证失败同理——只处理该候选，不动仍然有效的当前票。

**覆盖范围按上游传输方式定义，不能一概而论：**

| 上游传输 | 本轮证据 | 结论 |
|---|---|---|
| HTTP（含 SSE 流式、SSE 转 JSON） | `x-codex-turn-state` 在响应头，头先于 body 到达 | **可以拦**：判定点天然早于任何业务内容离开 |
| WebSocket 上游 | 票放在**握手头**，信号在**握手响应头**；连接池可复用连接，复用时本轮根本不发生握手 | **拿不到本轮证据** |

WebSocket 那一栏的问题不是「来不及拦截」，而是更糟：复用连接时新票压根没送上去（握手早就做完
了），而保存下来的旧握手响应头恰好「没有新 state」，**看起来像注入成功**。加大正文缓冲补不出这个
信号。另外注意 **HTTP 入站的请求也可能被转去 WebSocket 上游**，所以这不是「客户端用不用 WS」的
问题。

于是最小安全边界是：**凡是不能证明本轮使用了合格票的复用或长连接路径，对门控请求直接拒服。**
要支持这些路径，得先定义连接与票的绑定、换票时如何处置既有连接、以及同一连接上后续轮次的重新
准入——本期不做，就按拒服处理。

**只记录不拦截等于没有保护。** 那一刻降智输出已经生成，照常转发就直接违反目标；管理员事后切
`off` 只是关掉功能，救不回已经发出去的回答。

**WebSocket 与 HTTP 是两条对称的通道，WS 同样受保护。** 载体不同但都是**逐轮**的
（口径取自 codex 客户端源码 `codex-rs/core/src/client.rs` 与 `codex-rs/codex-api/src/sse/responses.rs`）：

| 方向 | HTTP | WS |
|---|---|---|
| 客户端 → 上游 | `x-codex-turn-state` 请求头 | payload 的 `client_metadata["x-codex-turn-state"]`，每个 `response.create` 帧都带 |
| 上游 → 客户端 | 响应头 | 握手响应头 **+ 带内 `response.metadata` 事件**的 `headers` |

所以「本轮证据」在 WS 上是存在的，而且 `response.metadata` 是上游在 **turn start** 下发路由令牌的
事件，先于任何内容分片——判定时机比 HTTP 的响应头还宽裕。连接池复用不构成障碍：证据不在握手里，
在本轮的下行事件里。

两条原生路径各有两个接入点：注入在逐帧解析/过滤器里（都在模型解析并改写**之后**，判定用的是实际
上送的那个模型，所以会话中途换成门控模型同样受保护），交付判定在下行写点（ctx_pool 的
`writeClientMessage`、passthrough 的 `BeforeWriteClient`）。

**不要把受保护账号改投 HTTP bridge。** 那条路虽然天然有证据边界，但 bridge 的适配层不是原生 WS 的
等价替代——它会删掉 `previous_response_id`（把续接静默改成新对话）、丢掉 `generate:false`（预热变成
普通生成）、且只接受 `response.create`（丢掉 `session.update` / `response.cancel`）。用它换证据是
拿非门控请求的语义去换，代价不对。

**接入点只有一处：`doOpenAIUpstream`。** 那是全部 OpenAI 上游发送的唯一汇聚点（二十来个调用点，
含 Responses 透传、chat-completions 转 Responses、messages 转 Responses、WS-HTTP bridge、
alpha-search、images 桥接）。准入放在它发送之前，交付判定放在它拿到响应头之后——响应头先于正文
到达、调用方此刻还拿不到 body，所以流式、非流式、SSE 转 JSON、compact 合成 SSE 一并覆盖。

按业务分支逐个去插是行不通的：那些分支各自构造请求、各自处理响应，漏一条就等于开一个降智出口，
而漏没漏不会有任何报错。**门控判定读最终请求体的顶层 `model`**——先用带引号的子串做廉价预筛，
命中了再解析 JSON 确认那个名字确实在 `model` 字段上（用户正文里提到模型名不算）。请求体读不出
模型时**拒服**，不放行。

准入状态按**单次上游发送**保存（一个局部变量），不按客户端请求保存：failover 会在同一个请求里换
账号重发，而票是 `(account, model)` 绑定的，用请求级状态会把上一个账号的票算到下一个账号头上。

拒服与交付拦截**不得被当成上游传输故障**。上游的传输错误处理会记一条 `request_error`、可能把账号
临时停掉调度、还会包装成 502 去换号——那等于把「拒服」悄悄变成「换个号照发」，而接手的账号是一个
未被观察过的账号。

**验证请求本身也要过这道检查。** 验证是带着候选票发出的——如果那次响应同样回了新 state，说明
候选票没被上游接受，这次回答的档位反映的**不是它**，不能用来判定它合格（否则会把一张坏票标成
verified，后患比拒服大得多）。这条检查只处理**该候选**：它既不证伪该候选的档位，也不影响当前票。
候选在这种终点之后能否再试，见 §4.2 的失败终点表。

**票是 `(account, model)` 绑定的，换号必须重新准入。** failover 在同一个客户端请求里换账号重发时，
不能把上一个账号的票注入到下一个账号的请求上——那张票对新账号无效，且会让判定张冠李戴。所以准入
状态按**单次上游发送**保存，不按客户端请求保存。

**哪些账号要保护是人工配的，没配不是异常。** 当前处于实验阶段，运维观察到某个账号高概率降智才给它
开 `full`。因此门控模型上遇到 `off` / `observe` 账号时**照常服务**，只是这次输出不受票据保障、也不做
交付判定。把它们当成拒服会让「设一次 `KONG_TICKET_GATED_MODELS`」把所有还没观察完的账号在该模型上
全部打死。

## 3. 账号级配置

**全局的启用开关与出口配置全部取消**，下沉到账号：

| 字段 | 含义 |
|---|---|
| `codex_ticket_mode` | `off` / `observe` / `full` |
| `proxy_id`（已有） | **流量出口**：业务、授权、验证请求走它 |
| `ticket_egress` + `ticket_proxy_id`（新增） | **票据出口**，三态，见下 |

两个出口分开是必须的，不是优化：合格票要求出口静默，而流量出口天生不静默。让它们指向
同一个代理，业务流量自己就会把合格票挤掉。

### 3.1 票据出口的三态

`proxy_id` 那套「NULL 即直连」在这里不够用——**「没配票据出口」与「票据出口就是直连」是两
种不同的行为**，必须能分开表达：

| `ticket_egress` | `ticket_proxy_id` | 行为 |
|---|---|---|
| `none`（默认） | — | **不主动取票**，只用被动收到的票 |
| `direct` | — | 经服务器本机出口取票 |
| `proxy` | 代理 id | 经该代理取票 |

`none` 的含义是「没有票据出口可用」，**不是「继承流量出口」**。后者在设计上就是错的：流量
出口天生不静默，继承只会稳定地取回坏票，还白烧额度。

`full` + `none` 是合法配置，但**它没有自主冷启动与恢复能力**——这一点必须写清楚，否则会被当成
「适合流量稀疏账号」的省事选项。原因在 §2.1：无票时 `full` 拒服，请求不上送，被动票源随之断掉，
靠自身流量回不到有票状态。一旦票用尽（首次启用、或闲置到全部过期），它就永久停在拒服。

它只在有**外部票源**时成立，而票源必须来自本账号**已经上送**的请求。这里有个容易忽略的约束：
§2.4 规定票与验证结论按 `(account, model)` 绑定，所以别的模型带回的票不能直接拿给门控模型用。
于是只有两条路：

- **允许跨模型移用原始票**：非门控模型的响应带回的票，可以作为门控模型的**候选**重新进入验证。
  注意只移用**原始票**，不移用验证结论——结论是 `(account, model)` 绑定的，必须重新验证。这条
  要在 §5.2 的缓存键上表达出来，否则实现会各按一种读法。
- **不允许**：那 `full + none` 就只剩一条路——运维临时把 mode 切到 `off` 放一段流量进去养票，
  而**那是明确的退出保护，不是「恢复了满血服务」**。

本期选哪条见 §9。另外提醒一个会写错的地方：**mode 是账号级的**，同一账号不可能同时处于 `full`
和 `off` / `observe`，所以「同账号另有 off/observe 流量在养票」根本不是一条可用路径。

没有上述补给就不要用这个组合。界面要显示成「等待外部票源」而不是「等待取票」（§6.2）：后者暗示
系统会自己解决，而它不会。

⚠️ **配置失效必须退回 `none`，绝不能退回 `direct`。** 理由不是洁癖：直连取票用的是服务器自己的
IP，静默被烧掉还能等回来，**把账号与服务器真实出口关联起来是不可逆的**。同理，票据代理也不能
继承上游那套「到期自动回退直连」的 fallback 策略。

**「失效」有两种，不止删除**，两种都按 `none` 处理并记事件：

- **代理被删除**：配置存在 `extra` 里（§5.1），没有外键替我们清空 id，所以会留下一个**指向已不
  存在的代理的 id**。取票前的存在性检查就是为它准备的。
- **代理到期**：更容易漏。上游的到期清扫只改写 `accounts.proxy_id`，**它不认识
  `ticket_proxy_id`**——所以票据代理到期后不会有任何自动处理，那一列会继续指向一个 expired
  代理，取票持续失败而配置看起来完好。取票前必须自己检查票据代理的有效期。

### 3.2 出口分离是取票时的运行条件，不只是保存时的校验

出口一旦合并，业务流量就会持续清零票据出口的静默，主动取票**必然**拿回坏票。所以这个条件
必须在**每次取票前**判定；保存时校验只是提前发现错误，它挡不住下面后三条——**业务出口可以在
无人保存票据配置的情况下改变**：

| 路径 | 保存时校验能否挡住 |
|---|---|
| `ticket_egress=proxy` 且 `ticket_proxy_id == proxy_id` | 能 |
| `ticket_egress=direct` 且 `proxy_id` 为空（业务也直连） | 能 |
| **业务代理到期，被上游的到期清扫自动改投为直连** | **不能**——那条路径改写的是 `accounts.proxy_id`，没有经过票据配置的保存 |
| 管理员在账号页改 `proxy_id`、或删掉流量代理 | **不能**——同样不经过票据配置的保存 |
| 两条代理记录指向同一个公网 IP | **不能**——代理 id 不同并不证明出口不同，任何 id 层面的比较都挡不住 |

判定不通过时：**停止主动取票并记事件**（等同 §3.1 的 `none`），但**已经验证通过的票按原期限
继续使用**——配置出错不该作废一张好票。

事件里要记下当时判定的两个出口。上面每条路径的表象都只是「一直拿不到合格票」，不记就得靠猜。

最后一条（同 IP）是 id 层面无解的，本期只能靠运维约定：票据出口专用、不与任何账号的业务出口
共享（见 §4.4 的部署前提）。

命名沿用 `ticket_*` 而不是上游的 `harvest_*`：上游那里主动采集是唯一路径，我们有两个来源，
再叫 harvest 会把次要路径说成全部；而 `proxy_id` / `ticket_proxy_id` 并列也更好读。

## 4. 验证

验证由**按成本递增串联的若干道门**组成，逐层判"这次服务实际是哪个模型提供的"。当前两道：

| 层 | 判据 | 成本 | 分辨力 |
|---|---|---|---|
| **stg0**（§4.0） | 上游响应里回报的 `model` | 零——字段已在解析中 | 只看得见上游**愿意声明**的替换 |
| **stg1**（§4.1） | 指纹归因（ModelTrace） | 一次验证最多三份长答案 | 看得见上游不声明的替换，但有测量噪声 |

**串联语义**：任一层判 fail 即整体 fail；判 inconclusive 的层不阻止其他层出结论（"没测出来"对票什么
都没说，据它作废是无谓拒服，见 §5.4）。**顺序上 stg0 先判**——它 fail 就不必再烧 stg1 的额度，这是
分层的实际收益，不只是多一道门。

两层各自看得见对方看不见的东西，所以不能相互替代：stg0 抓的是上游明说"我给了别的模型"，stg1 抓的是
上游什么都不说但输出分布变了。

### 4.0 stg0：上游回报的 model

上游在响应里回报本次实际使用的 model。请求 astra 而回报 luna，就是一条**由上游自己声明**的降智证据
——比指纹归因硬，因为它没有测量噪声。

**采集不需要新代码路径**：上游 sub2api 已经在业务路径上观测并落库（`upstreamResponseModelObserver`
挂在请求 context 上，透传 SSE 时本来就逐事件解 JSON 来抽 usage 与 service_tier），结果写进
`usage_logs.upstream_response_model` 与 `upstream_model_mismatch`。验票的挑战请求不走那条路径，需要
自己采，但同样只是在既有的 SSE 解析里多取一个字段。

两个数据源用途不同，**不要混**：

| 源 | 时机 | 用途 |
|---|---|---|
| 验票挑战 | 验证时 | **参与判定**：stg0 fail 即票判 rejected |
| 业务请求 | 每次受控模型请求 | **只统计**，不作处置（见本节末） |

#### 判据：四档，默认拒绝极性

| 响应 model | stg0 |
|---|---|
| 与请求的一致 | pass |
| 命中 stg0 白名单 | pass |
| 没观测到（字段缺失、非 SSE、流读失败） | **inconclusive**，不阻止 stg1 |
| 其他 | **fail** → 票判 rejected |

匹配按**"精确或带版本后缀"**（大小写无关），不是纯字面相等。版本后缀的判据是
**`<base>-` 之后的第一个字符是数字**：

| 基名 | 响应 model | 匹配 | 为什么 |
|---|---|---|---|
| `gpt-5.6-sol` | `gpt-5.6-sol` | ✓ | 相等 |
| `gpt-5.6-sol` | `gpt-5.6-sol-2026-03-17` | ✓ | 后缀以数字开头 = 同一模型的快照 |
| `gpt-5.6-sol` | `gpt-5.6-sol-mini` | ✗ | 后缀不是数字，是另一个模型 |
| `gpt-6` | `gpt-6-astra` | ✗ | 同上——**没有这一条，门控 `gpt-6` 会连 astra 一起吞掉** |

⚠️ 后缀容错这一条是必需的，不是宽容：实测存在 `gpt-5.4-mini` → `gpt-5.4-mini-2026-03-17` 的回报形态
（上游的审计判据是字面相等，把它记成了 mismatch）。按字面相等判死票，上游每换一次快照就会把该模型
的票全部作废，而那不是降智。而"后缀必须以数字开头"把容错限制在快照/日期上，代号不会被当成后缀混进来。

⚠️ **stg0 白名单的取值不校验是否在指纹校准资料里**，这一点与 `accept_extra` 不同：后者的值是归因
结果，闭集之外的值恒不命中、属死配置；而 stg0 的值是上游可能回报的任何 model 名（`gpt-6-sol` 就不在
校准资料里）。照 `accept_extra` 那样校验会把正确的配置当成笔误忽略掉。

#### stg0 的白名单必须独立于 stg1 的接受白名单

两者语义看起来都是"什么算可接受"，但来源完全不同，**共用会让 stg0 失去判力**：

`KONG_TICKET_ACCEPT_EXTRA`（stg1）存在的理由是补偿**指纹归因的区分度不足**。最典型的是
`gpt-5.6-sol` 的白名单默认含 `gpt-5.5`——指纹法分不开这两者，那 17.8% 是测量噪声而非降级证据
（§4.1）。而 stg0 没有测量噪声：上游回报 `gpt-5.5` 就是它自己声明给了 5.5。若 stg0 复用这张表，
**一条为"指纹分不清"打的补丁会放掉一次上游明说的降智**。

所以 stg0 用自己的白名单（§7 的 `stg0_accept`）。除表里列出的，每个模型只接受上游回报它自己。

**表里只收"升级投放"**：上游有时把新模型的流量投到旧模型名上（如请求 `gpt-5.6-sol` 给
`gpt-6-sol`），那是拿到更好的东西，判死票等于为了一次升级白搭一整段出口静默。**反方向绝不进这张
表**——回报一个更旧或更小的模型正是要拦的降智。内置默认已收 `gpt-5.6-sol:gpt-6-sol` 这一条。

⚠️ **不做"同代号、版本号更高即放行"的结构化规则。** 那要解析 `5.6` / `6` 这类非标准版本串并给它们
定序，多一处会出错的地方；而升级投放的频率目前未知，等它确实频繁打扰再说。代价是新投放要手工配一次
白名单，缓解见下。

#### 默认拒绝极性的代价与发现机制

白名单是默认拒绝的，所以上游投放一个**没配过**的新模型时，stg0 会把票判 fail：不断判死、不断重取。
既有的验证失败冷却（§4.3）限制了重试速率，但额度仍在烧。

⚠️ **近三天的业务统计发现不了这件事**，不能把代价兜在它身上。三条独立的原因：

1. **票被判死之后，业务请求压根不产生上游响应 model**——`full` 模式拿不到票就在注入前拒服，于是
   `usage_logs` 里不会新增 mismatch 记录。故障越严重，这个统计越安静。
2. 验票的 mismatch 只进**票据事件表**，不进 `usage_logs` 的统计口径。
3 天窗口里的历史正常请求还会把刚发生几分钟的变化稀释掉，页面也要人工刷新。

所以那个统计回答的是"**业务流量上被降智了多少**"，不是"**验票是不是在持续判死票**"。两者是不同的
数据通路，不能互相代替。

**真正的发现路径**是逐模型的诊断行（§6.2）：它读最近一次**最终验证事件**，stg0 判死会在那里显示成
"上游回报 X"。新投放的表现是该模型的诊断行持续停在 stg0 拒绝上、候选数不断增长而当前票为空。业务
统计仍然要显示，但它的用途是量化业务面的降智比例，**不是这条代价的安全网**。

统计页面上必须列出 mismatch 的响应 model 原值及次数（§6.2）——那是唯一能告诉运维"该往白名单加
什么"的信息，而诊断行只给最近一次。

#### 上线基线（2026-09-20 实测，生产库 3 天窗口）

| 请求 | 一致 | 不一致 | 未观测 |
|---|---|---|---|
| `gpt-5.6-sol` | 11550 | 0 | 0 |
| `gpt-6-astra` | 3601 | **4**（全部回报 `gpt-5.6-luna`） | 4 |

两点要记住：

1. **那 4 次降智是真实发生的，而 stg1 完全没拦住** ——它们是业务请求，不经过验票。这是 stg0 存在的
   直接理由，不是理论上的补充。
2. 回报形态**目前就是字面相等**（`gpt-5.6-sol` → `gpt-5.6-sol`，无后缀）。但同期观测到
   `gpt-5.4-mini` → `gpt-5.4-mini-2026-03-17`，证明快照后缀这种形态确实存在，所以后缀容错不能省。

聚合查询实测约 120ms（窗口内 1.5 万行，走 `created_at` 索引后按模型过滤）。

#### 业务路径只记不撤

业务请求上发现 mismatch 时**不撤销当前票**，只进统计。两条理由：

1. 业务路径用的是**上游那个较宽的判据**（字面相等、无版本后缀容错），误报形态确实存在（见上面的
   日期后缀）。据它撤票就是拿已知会误报的信号做不可逆处置。
2. 这是新判据，误报率尚无生产数据支撑。而错误撤票会让账号失去票据保护、重新进入取票循环。

需要自动处置时再加，那时应当有统计数据说明误报率。

### 4.1 stg1：指纹归因（Go 内实现）

判据是**粗粒度的**——只需分出"归因落在被请求模型的接受白名单内还是外"，比区分具体档位容易得多
（astra 与 luna 的区分度 p 普遍 >0.94）。只门控 astra 时它退化成 astra / 非 astra 的二分，那一档
分辨力充足。

⚠️ **但门控 `gpt-5.6-sol` 时不是**：指纹法分不开 sol 与 5.5。实测一次为 sol 取的 292 票，分布是
sol 0.8214 / 5.5 0.1779，只看 sol 达不到 0.9。所以 sol 的白名单默认含 `gpt-5.5`——那 17.8% 是
测量噪声而非降级证据（挑战是为 sol 发的），拿分辨不出的差异拒票就是无谓拒服。**代价是 sol 这道门
发现不了 sol → 5.5 的下降**，只保住"不掉到 luna / terra / 更低"；要恢复那一档分辨力得换一份能分开
两者的校准资料，调阈值没用。

**"容易"不等于"有保障"**：闭集归因的残余风险见下文与 §1.2.4。

**采纳判据：白名单内各归因结果的概率之和 ≥ 置信度。**

每个被门控的模型有一个接受白名单，**永远含自己**，可以额外声明接受哪些别的归因结果——实测同一
账号为 `gpt-5.6-sol` 取票时归因常返回 `gpt-6-astra`，拿到更高档位同样应当接受。方向是单向的：
astra 不接受 sol，那正是这个功能要挡的降智。

两个设计点：

1. **白名单，不是一条档位链。** 校准资料是 13 个候选，跨 gpt 与 claude 两个家族（claude 那 7 个
   纯属归因干扰项），`gpt-5.6` 内部还有 sol / terra / luna 三个同辈。它们之间并不存在全序，硬排
   成链等于凭空给互不可比的模型定先后，而那个先后直接决定放行与拒服。
2. **求和，不取 argmax。** 门要判的是"有没有掉到白名单之外"，那是一个并事件。astra / sol 各占
   0.6 / 0.35 时，"sol 或更好"已有 0.95 的证据，按 argmax(0.6) 判会把一张合格的票拒掉——无谓
   拒服与放行降智同样是缺陷。同一个量也用作挑战循环的提前停止条件，否则证据分散在两个都接受的
   归因之间时会白加挑战，加满还是拒。

判据只在一处实现（`KongTicketAccept`）：落结论、读当前票、管理面显示都走它。仓储只按
`status = verified AND expires_at > now()` 返回候选，不在 SQL 里复制一份判据——两套规则的缝隙
不会报错，只会让同一张票在两条路径上得出不同答案。

因此票行要存**完整归因分布**而不只是 argmax（§5.3）：白名单来自配置、可以改，只存 argmax 就只
剩"照旧放行"与"一律作废"两个都不对的选项。

**采用 ModelTrace 的官方口径**，不自创变体：

```text
0.75 × 去除环境方向后的 Hellinger 模型中心相似度
+ 0.25 × 有序块数字序列特征
```

归因时先从数字分布与有序特征中投影掉建库时估计的共享环境干扰方向，再与全部模型中心比较；
**每份回答分别评分后取平均**，用**与份数对应的校准温度**做一次全局 softmax 得到概率。

⚠️ 两处容易做错、必须照官方来：

1. **三条挑战是三个不同的 prompt，不是同一 prompt 重复采样。** 因此**多份回答的数字序列
   不能合并**——分布基准不同，池化会污染特征。只能各自评分、在**分数层面**平均。
2. **校准温度按份数选（档 1/2/3）**。平均会压低分数方差，温度必须跟着变；用档 `"1"` 去
   解释两份的平均分，概率标定就是错的。

**份数按置信度递增，达到阈值即停——早停是常态而非妥协**，多数票应该停在一份（1500
tokens）。升到三份只是模糊样本的兜底：

```
挑战 #1 → 评分 → softmax(档 "1")
  p ≥ 阈值 → 采纳
  否则 → 换挑战 #2 → 两份分数平均 → softmax(档 "2")
      p ≥ 阈值 → 采纳
      否则 → 换挑战 #3 → 三份平均 → softmax(档 "3")   ← 官方标准用法
          仍不足 → 判不确定，按拒绝处理（§4.3）
```

中途停下不偏离算法——每一档都有自己标定好的校准温度，档 `"1"` 下的 p 是完整有效的概率，
不是"还没测完的临时值"。上限 3 也不是随意定的：**校准表只有三档**。

其余实现要点：

- 校准表用 `go:embed` 带进二进制，同时留一个外部覆盖路径——模型演进后不必重新构建镜像。
- 偶发拒答或截断会导致解析不到足够数字（需 ≥80 个或期望值的 55%），该次作废、不计入平均。
- 每份探测的**原始数字序列**与分数长期留存（§5.6），作废的那几份同样存。归因之所以能离线
  重算、能换校准表回溯，前提是输入还在。
- 概率是**闭集、均匀先验**下的结果：库外模型会被归到最相似的现有候选。**所以高 softmax 概率
  并不能排除「库外的非 astra 被判成 astra」**（§1.2.4）——这是准入侧的残余风险，不是可以忽略的
  细节，更不是"绝对置信度"。

⚠️ **移植正确性必须用回归测试锁住**：仓库里的
`backend/internal/service/testdata/kong_fingerprint_golden.jsonl` 含输入序列与 Python 侧归因结果，
作为 golden file 锁住移植偏差（见 `kong_fingerprint_test.go`）。没有这个测试，算法偏差会静默地变成
"把 astra 判成 sol，然后把好票全拒掉"——而表象只是"一直拿不到合格票"。

### 4.2 调度：取票由请求驱动，没有定时器

唯一的触发点是门控模型的业务请求到来：

```
请求到来（full 模式、门控模型）
  ├─ 有 verified 票，剩余 > refresh_before → 注入，放行
  ├─ 有 verified 票，剩余 ≤ refresh_before → 注入当前票放行，同时异步预取下一张
  └─ 无可用 verified 票 → 同步路径，当前请求等待：
        ├─ 缓存有满 min_ticket_age 的 unverified 票 → 验证最早的一张（零采集成本）
        ├─ 否则票据出口非 none 且 ticket_fetch_min_idle 已满 → 取票 → 立即验证
        └─ 取不到 / 验证不通过 → 拒绝该请求，按 §4.3 处置
```

三条性质：

1. **闲置期完全无活动，而且闲置本身在为下一张好票做准备。** 账号停流后票自然过期、没有任何
   后台刷新，票据出口于是开始积累静默——而静默是拿到 292 的前提（§1.1.4）。周期性刷票恰好相反：
   没人用的时候持续烧额度，同时把自己需要的静默一次次清零。
2. **不需要「近期是否有请求」这样的时间窗参数**，请求本身就是触发器。代价只有一种：异步预取
   刚发起、账号随即停流，那次取票白花——可接受。
3. **阻塞只发生在完全无票时。** 有票但接近过期走异步预取，当前请求不受影响。这也是空窗伪影
   的正解：提前量仍由 `refresh_before` 给，但提前的动作不再需要定时器托着。

**冷启动**（账号首次使用，或闲置后票已全部过期）走的就是上面的同步路径：配了票据出口时，
首个请求等一次「取票 + 验证」（约 30 秒）；等不到合格票就拒绝该请求，**不放行降智请求**。

⚠️ **同一账号的同步取票必须 single-flight。** 并发请求各自取票不只是烧额度——每次取票都是
票据出口上的一次活动，N 个并发请求会互相把静默清零，最后谁也拿不到合格票。一个账号同一
时刻只允许一次在途的取票 + 验证，其余请求等同一个结果。

**`min_ticket_age` 只作用于 observed 票。** 它压的是被动票的验证频率：上游在同一时段可能
连发多张，逐张验证纯属烧额度。主动取来的票是我们自己要的唯一一张，等待毫无意义——**fetch
票立即验证**，否则冷启动要凭空多阻塞一个 `min_ticket_age`。

**「一次无票」指一段连续无可用票的时期，不是一个请求。** 这个区分直接决定成本：缓存里的
observed 票大概率是同一段流量下收的，坏也坏在同一个原因（流量出口不静默）。若按请求理解，
每个新请求各验一张，一批坏票会被逐张验完——每张 1500 tokens，验的还是同一个事实。

规则：**同一段无票期内只验证一个 observed 候选**；失败后除非出现**新信息**，不再验证别的
observed 票。新信息只有两种——该时期**开始之后**新收到的 observed 票，或一次成功的主动取票。
**新的业务请求本身不是新信息。** 无票期在拿到 verified 票时结束。

验证请求走**流量出口**（前提 §1.2.1：票不绑取票出口——注意它尚未被实验关闭），因此不占用票据出口的静默。这是分离
拓扑带来的直接好处。

#### 取票验证任务的生命周期

三个触发点——同步冷启动、异步预取、observed 失败后的升级——**走同一个取票验证流程**，由它统一
检查：**账号是否可正常调度**（见下）、有无在途任务、出口分离是否成立（§3.2）、票据代理是否有效、
静默与冷却期限（§4.3）。任何一条不满足就终止本次尝试并记事件，没有绕过的入口。

⚠️ **本功能主动发起的请求，一律要求账号处于可正常调度的状态**——取票、指纹验证、`observe` 的诊断
探测三类都算。「可正常调度」指账号未禁用、未过期、未被限流暂停、额度未耗尽，也就是调度器此刻愿意
把业务派给它。两条理由：异常账号上的探测既烧额度又可能加重问题（例如已被限流还继续发请求），而且
**异常状态下测出的档位不代表正常状态**，拿它当结论会误导。不满足就跳过并记事件，**不排队等待**。

业务请求驱动的路径天然满足这条（请求能落到该账号，说明调度器认为它可用），但**主动探测不经过调度
器**，必须自己检查。

| 边界 | 规则 |
|---|---|
| 已有在途任务 | 加入等待，**不另起一个**——并发取票会互相清零静默 |
| 等待者超时或断开 | 只结束该等待者。**已发生的出口活动无法撤销**，所以任务继续跑完、结果入缓存 |
| 全部等待者都离开 | 任务照常完成：它的产物是一张票，对下一个请求仍有价值 |
| 同账号不同模型的等待者 | 账号级 single-flight 会让模型 B 的请求等在模型 A 的任务上。任务结束后 **B 必须重新判定**：A 的票与结论对 B 不构成准入依据（§2.4 的 `(account, model)` 绑定）。B 要么用自己的候选走验证，要么按无票处置 |
| 结果晚到 | 落库前重新校验模式、账号与出口配置还是发起时那套；已变更则丢弃结论、只记事件 |
| 进程重启 | 在途任务不保证恢复。票缓存在库里不丢——**重启后先查库，仍然有效的 verified 票直接接着用**；只有确实没有可用票才走同步路径 |
| 验证的总尝试上限 | 三档校准温度**不等于**最多三次网络请求——无效回答（拒答、截断、数字不足）不计入平均，但重试必须有次数上限与整体超时，否则一次验证能无限拖下去 |

失败终点按类型定义，它们的后续处置并不相同：

| 失败类型 | 候选票 | 是否进冷却 |
|---|---|---|
| 长度命中 denylist | 丢弃，不入缓存 | 否——没有消耗验证成本 |
| 取票请求网络错误、上游拒绝 | 无候选产生 | **是**——出口活动已经发生（§4.3 里的 A 已被刷新） |
| 取票成功但响应没带 state | 无候选产生 | **是**——同上，出口已被用掉。这种情况本身值得记事件：它意味着上游认为该请求已有有效票，与我们"无票"的认知冲突 |
| 归因非 astra 或判不确定 | rejected | 来源 fetch 进冷却；来源 observed 按 §4.3 升级 |
| 验证请求未被上游接受（§2.4） | 留在 unverified，但本段无票期内不再选它 | 否——结论作废，这张票并没有被证伪 |
| 票在等待或验证期间过期 | 丢弃 | 否 |

**冷却与候选淘汰都落在结论提交之后。** 两者各是一次数据库写入，而验证的前提（账号 mode、调度资格、
出口配置）在那段时间照样会变，提交本身只检查票状态、期限与跳过标记。放在提交之前等于在最后一道前提
复核与提交之间开一个没人再核的写库窗口；结论没落库（票在验证期间被撤销或过期）时罚出口也没有依据。
候选淘汰另有一个更硬的理由：`SkipCandidatesFor` 会把边界内所有 unverified 候选标成 skip，其中包括正
在验证的这一张，而提交条件里有 `skip_until_new = FALSE`——反过来做必然更新 0 行。

⚠️ 残余风险：**前提与提交仍不在同一个事务里**。挪完顺序之后两者之间只剩一次原子写，窗口已经收到最
小，但严格保证需要让 `CommitVerification` 在事务里校验预期的账号/出口版本——那要改仓储接口、真实 SQL
与测试桩，**本期未做**。

**「没被证伪」不等于「现在可以重试」。** 候选未被上游接受，说明本轮上送根本没带上它（多半是连接
复用，或它已被新票替换），重新选中同一张只会重复同一个无效尝试。所以它留在缓存里——它可能仍是
好票——但**在本段无票期内不再被选中**；要恢复资格，需要与 §4.2 同一套「新信息」：收到新的 observed
票，或一次成功的主动取票。这样既不会无限重试同一张，也不挡住后续合格新票恢复服务。

#### 串行化有两个维度，都是必需的

一个账号同一时刻只允许一次在途取票 + 验证；**同一个票据出口也只允许一次**。

出口维度不是冗余：静默是按**出口 IP** 积累的，两个账号配同一个 `direct`（都用服务器本机 IP）或
同一个代理时，并发取票会互相把静默清零，最后谁也拿不到合格票。只按账号串行拦不住这种情况。

出口被**另一个账号**占着时直接拒服，不等待——它产出的票属于那个账号，等它没有意义。

**认领到任务之后必须重新核对调度前提**再动网络：决策是在认领之前算的，两者之间前一个任务可能刚
结束，那时的「可以取票」到此刻已经不成立，照旧决策再取一次就把刚积累的静默清零了。

#### 任务有总期限，等待者有单独的期限

后台任务不跟随调用者的 context（产物对下一个请求仍有价值），但必须有自己的总期限
（`kongTaskBudget`）：各个 HTTP 调用自己的超时管不住整条任务——数据库调用、代理解析、三份挑战
串起来的总时长都在它们之外，而卡住的任务会一直占着账号与出口的槽位。

等待者另有一个明显更短的期限（`kongWaitBudget`）：任务最坏要跑十几分钟，让一个业务请求挂那么久
毫无意义。等待超时只结束该等待者，任务继续跑完。

### 4.3 验证失败的处置

```
rejected 之后：
  该票来自 observed，票据出口非 none → 升级：经票据出口主动取票再试
  该票来自 observed，票据出口为 none → 无处可升级，拒绝该请求
  该票来自 fetch                     → 退避：进入冷却期，期间不再取票也不再验证
```

`none` 的账号没有升级退路，这是它的固有代价——换来的是完全不碰主动取票（不烧额度、不动任何
出口的静默）。

冷却期是**全局配置，不设硬下限**。直觉上它该长于静默门槛——否则冷却结束时出口还没闲够，
取回来仍是坏票，退化成"用验证成本反复确认同一件坏事"。但静默门槛本身的观测并不可靠
（§1.3.1），拿它去约束另一个参数只会放大不确定性。所以给一个经验默认值，由 §5.4 的数据校准。

**失败原因不要硬归因。** 一次发生在远超已知门槛的静默之后的失败，只能说明「再多等一截没有已知
的理论收益」，**不能反推原因一定不是静默不足**——门槛本身就没测准（§1.3.1），astra 的成因更是
未定（§1.2.3）。

**失败的代价是一段拒服。** 它的长度不能用「静默门槛」一个数概括——必须按下面的口径算，
因为静默与冷却的**起算点不同**：

```text
A = 票据出口最后一次活动的时刻     M = ticket_fetch_min_idle
F = 失败判定的时刻                 C = verify_fail_cooldown
E = 当前可用票的过期时刻           L = 下一次尝试到验证完成的耗时

下一次尝试 = 不早于 max(A + M, F + C) 的首个业务触发
拒服时长   = max(0, 下一次尝试 + L − E)
```

**`A ≠ F`是关键**：取票本身要花时间、验证更慢，所以失败判定总晚于最后一次出口活动。这条口径
纠正了三处容易算错的地方：

- **不存在「拒服 25 分钟」这个上界。** 那个数来自 `A=F=3300、L=0、后续请求立即到达`的简化例子，
  只是量级。失败耗时 30 秒、恢复再耗 30 秒就变成约 26 分钟；再失败一次继续延长。
- **预取失败后不能立即重来。** 一次刚刚接触过出口的失败，已经把静默重新计时了——此时距「静默
  重新满 M」比长期闲置后的冷启动更远，而不是更近。用默认值构造：请求在刷新窗口末尾 `t=3590`
  才到达 → 预取在 `t=3595` 产生出口活动 → `t=3610` 失败 → 最早下一次尝试 `t=5410` → 恢复约
  `t=5440`，**拒服 30 分 40 秒**。
- **`C = M` 不等于「不额外加时」。** 两者起点不同（C 从 F、M 从 A），所以 `C = M` 时实际期限是
  `max(A+M, F+C)`，仍比单独的静默门槛晚了 `F − A`。同理 `C < M` 也不必然让该参数失效，要比
  两个绝对期限。

想让第二次尝试落在票过期之前，需要 `refresh_before ≥ M + 取票耗时`，可那样取票间隔只剩
1700s，低于门槛。写成不等式：**TTL(3600) < 2 × M(1800) + 耗时**——在「当前票由上次取票产生、
两次尝试各自要重新积累 M」这个配置模型下，一个票周期装不下两次静默都充足的尝试。它是该模型
的结论，不是上游静默门槛的物理定律；但在本设计选定的参数下它成立，调参解决不了：缩短间隔只会
把「偶发失败 + 长中断」换成「持续失败」。

**当前接受这个代价，票据出口只有一个。** 把中断压到秒级的唯一办法是备用出口（两个出口各按
自己的周期走、互不干扰，A 失败即换 B），代价是出口配置从单值变成有序列表，数据模型与界面
都要跟着动。另外多账号池本身会大幅削弱影响——某个账号拿不到票时调度器避开它即可，**只有单
账号部署才会让账号中断等于服务中断**。

### 4.4 部署前提：静默这个量必须可信

整套调度都建立在"票据出口已静默 N 秒"这个判断上，而系统只看得见**自己产生的**活动。所以先把
适用范围限定住，本期不做分布式协调：

| 前提 | 不满足会怎样 |
|---|---|
| 票据出口是**固定 IP** | IP 会变的出口（拨号、动态住宅代理）上，静默时长根本无从归属 |
| 票据出口**专用**，不承载任何账号的业务流量 | 别的账号在用它，静默被清零而本系统看不见 |
| 一个票据出口只由**一个协调者**调度（单进程，或多进程共享锁） | 两个进程各自认为"已静默 30 分钟"，同时取票、互相清零 |
| 取票的串行范围是**出口**，不只是账号 | §4.2 的 single-flight 是账号级的：它挡住了同一账号的并发取票，但两个**不同账号**共用一个票据出口时，各自都满足账号级约束，仍会互相清零 |

**界面上要说清那个数是什么**：它是"距本系统最后一次使用该出口"的时长，**不是实际静默**。存在
系统外的活动时，前者会**高估**后者——所以不能把它显示成"现在一定能取到好票"。事件表记的是同一
口径（§5.3）。

`A`（§4.3 的最后活动时刻）取**该次取票最后一次网络活动**的时刻，不是请求开始时刻：取票本身有
耗时，用开始时刻会把静默算多。

### 4.5 成本

验证一次约 1500 tokens 输出、25 秒，**是真实生成消耗**。不设预算上限（已定），但事件表
要能算出每账号的验证次数与失败率，否则票源不稳时的额度消耗无从察觉。

## 5. 数据模型

### 5.1 账号配置存进 `accounts.extra`，不加列

三项配置——`codex_ticket_mode`、`ticket_egress`、`ticket_proxy_id`（后两者的三态语义见 §3.1）——
放进上游**已有的 `extra` jsonb 字段**，键名带 `kong_` 前缀避免与上游将来的实现撞名。

**理由是 fork 成本，而且这条差距很大。** 上游用 `ent` 且把生成代码全部入库（三百多个文件）。给
`accounts` 加一列就必须重新生成 ent，而重新生成会改动 `ent/migrate/schema.go`、`ent/mutation.go`、
`ent/client.go`——这几个文件上游每周都改数次。**生成代码的 rebase 冲突是最难手工解决的那一类**，
为三个配置项换来每次跟进都要处理它，不值。`ent/schema/account.go` 本身倒是很稳定，但它不是问题所在。

代价与应对：

- **失去 `ON DELETE SET NULL`**：代理被删后 `ticket_proxy_id` 会变成悬空 id，而不是自动清空。
  但这不新增负担——§3.1 已经要求取票前自行检查票据代理（到期同样不会触发上游的清扫），「代理还
  存在吗」正是同一次检查的一部分。**外键本来就不是这套设计的依赖**，去掉它反而让两条失效路径
  （删除、到期）走同一个判定。
- **失去类型约束**：读取时必须校验取值，非法值按 `none` / `off` 这一侧的保守语义处理并记事件。
- **查询稍绕**：`extra->>'kong_ticket_mode' = 'full'` 这类条件可以用表达式索引；本功能的账号数量
  是个位数到十几，索引与否都不构成问题。

### 5.2 三张新表：纯 SQL，不走 ent

票缓存（§5.3）、事件（§5.4）、指纹探测记录（§5.6）三张表**用手写 SQL 迁移 + 手写 repository**，
理由同上——新增 ent schema 一样会触发全量重新生成。上游本身就有这个先例：`audit_log_repo.go`、
`auth_cache_invalidation_outbox_repo.go` 等直接持有 `*sql.DB`，照它们的形态写即可。

迁移文件用 **`9xx_kong_*.sql` 独立编号段**——上游在 `238_` 附近递增，占 900 以后永不撞号。三张表
都是本 fork 独有的新表，除了编号段不与上游产生任何交集。

### 5.3 票缓存表（短期）

存 `state` 原始值（验证与注入都要用它）、长度、来源、状态、归因结果、
`captured_at` / `expires_at`。

**归因结果存三样，分工是刻意的**：`fingerprint_model` 与 `fingerprint_p` 是**证据**（最像哪个、
有多像），`fingerprint_probs` 是**完整分布**。采纳与否不入库——它按当前白名单对分布求和得出
（§4.1）。白名单来自配置、可以改，把结论固化进票行就只剩"照旧放行"（可能放行白名单外的档位）
与"一律作废"（把合格票也丢掉）两个都不对的选项。

**`expires_at` 怎么来必须写明**：能从票本身解析出签发期限就用解析值；只能按「收到时刻 + TTL」推算
的话，它是一个**下界估计**——被动收到的票可能已经用掉了一部分寿命——此时 `refresh_before` 的余量
必须覆盖这个误差，否则会在票实际已过期之后才开始预取。

**它天然是短生命周期的**：票过期即无用，按 `expires_at` 清理。另需限制每
`(account, model)` 保留的条数——`off` 模式也收票，高流量账号会快速堆积。

**入库边界上有三件事**，fetch 与 observed 两条路径共用：

1. **票原值唯一**：`(account_id, model, state)` 上有唯一索引，靠 `ON CONFLICT DO NOTHING
   RETURNING` 原子区分「新票」与「重复票」。先查再插在并发下会双双判为新票。重复票保留原 id、
   期限与状态——重复出现不是新信息，不该凭空延长寿命，也不该把一张已 rejected / skip 的票重新
   变成候选。
2. **长度黑名单**：312 是已实测的降智档位长度，照样会被上游接受。不拦住它就会进候选、烧掉一次
   完整验证（三份挑战、每份几百 token），而结论是注定的。292 **不在**黑名单里——同为 292 的票也
   可能不同，长度只能否定、不能肯定。
3. **顺手清掉已过期的行**：过期票没有任何用途，票原值本身也是敏感串。清理必须是**请求驱动**的
   （插入新票时顺带删一批），不能加定时任务——这套设计刻意没有定时器，闲置期必须完全无活动。
   清理只按 `expires_at` 判断，动不到仍然有效的当前票；`kong_fingerprint_probes` 不参与清理，
   它按 §5.6 长期留存。

⚠️ 探测记录里的累计概率是 **argmax 的**，而采纳看的是白名单内的概率之和（§4.1）。所以页面上会
出现「累计概率 0.6 却通过了」——那不是矛盾。判据用的那个数字记在 verify 最终事件的
`accept_mass` / `accept_models` 里，看探测列表解释不了放行原因时去看事件。

verify 最终事件还记 `top_models`——归因分布的前三名连概率。只记 argmax 解释不了拒票：一张真的
sol 票可能是 sol 0.82 / 5.5 0.18，看不见第二名就判断不出该往白名单里加什么、还是该换校准资料。
记在事件里而不是只靠票行的 `fingerprint_probs`，是因为票随过期被清理、事件长期保留。

### 5.4 事件表

取票、收票、验证、注入失败、回捞、进入冷却、票据出口配置失效（§3.1 那种按 `none` 降级的
状态），逐条记录。除显而易见的字段（账号、模型、事件类型、状态码、`proxy_id`、票长度、
指纹结果）之外，**必须记该出口距上次活动的静默时长**。

理由：这让"多久静默才能拿到合格票"从一次性手工实验变成持续观测。现在知道的"34 分钟必开、
19 分钟有时够"全是手工试出来的，而上游判据随时可能变；有了这一列，`成功率 vs 静默时长`
是一句 SQL。

**`outcome` 四档，区别在"对票说了什么"**：

| outcome | 含义 | 票的去向 |
|---|---|---|
| `success` | 通过 | verified，可注入 |
| `failure` | **测出了结论且是坏的**：归因不合格（证据完整），或上游明确重发票（这张注入不作数）| rejected |
| `inconclusive` | **测了但没测出来**：前提中途失效、没有有效回答、证据写库失败、融合样本未达标 | 留在候选池等下一次机会 |
| `skipped` | **压根没开始测**：模式不符、被跳过、前提一开始就不成立 | 不变 |

`inconclusive` 与 `failure` 必须分开，因为两者对"下一步该做什么"给出相反答案——前者什么都没说，
据它作废一张票就是无谓拒服；与 `skipped` 也要分开，否则看不出"到底测没测"，而那正是排查"额度花在
哪了"的第一个问题。

⚠️ `outcome` **只用于展示与查询**，不参与任何业务判断：静默按 `event_type = fetch` 算、冷却按
`event_type = cooldown` 算，都不看 outcome。所以重新分档不会改变调度行为。**历史记录按旧口径读，
不回填**——那些 `skipped` 里混着今天会记成 `inconclusive` 的条目。

### 5.5 `usage_logs.kong_request_features`（jsonb）

**状态**：已实现（迁移 903）。

stg0 的两列（§5.7）回答的是"上游声明给了什么模型"，回答不了"这条请求手里那张票是什么"。后者只能
逐请求记，因为现有的三处都对不上：票表 `kong_ticket_cache.state_len` 是**票**的长度（一张票服务
几十次请求）；`kong_ticket_events` 是取样时刻的记录，与某条业务请求没有对应关系；`ops_error_logs`
虽然存了请求头，但只存错误请求。

**用 JSONB 而不是逐特征加列**：这一列注定会长出更多特征，而该表的插入路径有一份按位置对齐的参数表
与八处 VALUES 列清单，每加一列都要同步改那九处；特征是观测用的稀疏键值，不参与计费与调度，也不需要
索引。新列**插在 `upstream_request_id` 之前**而不是表尾：上游有用例按倒数第 3 / 2 / 1 位断言
`session_id`、`native_compaction_v2`、`created_at` 的位置。

| 键 | 含义 |
|---|---|
| `state_fp` / `state_len` | **实际发往上游**那个 state 的指纹与长度：上游真正收到的东西 |
| `client_state_fp` / `client_state_len` | **客户端自己带来**的那一份，只在与出站不同（即被我们替换掉）时才有 |
| `ticket_id` | 我们注入的那张票（`kong_ticket_cache.id`）。票全量入库，所以这里给的是精确身份而不是指纹 |
| `reissued_fp` / `reissued_len` / `reissued_ticket_id` | 上游**在响应里又下发**的 state；被动收票入库成功才有 id |

四条口径，每条都不能含糊：

1. **一律从真正上送的那份载体上量**（HTTP 出站请求头 / WS 上送帧 / HTTP→WS 的 map），不记一个旁路
   变量。载体就是上游收到的东西，而旁路值会在注入之后被改写、或在 failover 换号后错配到另一次发送上。
2. **只存指纹不存原值**，12 个 hex 字符（sha256 前 6 字节）。48 bit 足以在一天几千条里区分是不是同一个
   state，又不足以重建原值。
3. **缺省即"没有这一项"，不能用零值表达**。长度 `0` 是"带了个空串"，与"没带"是两件事；
   `reissued_ticket_id` 缺省是"这张回发的票没入库"（如 312 落在长度黑名单里），给个 id 就是假的。
4. **逐轮记，不记连接级**。WS 一次会话每轮各有自己的票，沿用上一轮的记录器会让这一轮的用量行顶着
   上一轮的票 id 显示——那是彻头彻尾的错误陈述，比缺值严重。

`client_state_*` 与 `reissued_*` 的价值不限于 full 模式：**off / observe 的账号本来就不注入**，
那时这两项正是"客户端手里那张是什么档"与"上游现在给什么档"的唯一逐请求读数。

采集点接在票据网关的准入与交付判定上（三条上送通路 + 两条下行判定），因此天然覆盖三种模式。
未接的通路（chat-completions 等其它协议适配器）该列为空——**空表示"没采到"，不表示"没有 state"**。

**收票与记特征不挂在"客户端还连得上"这个条件下。** ctx_pool 那条路的下行处理整块在 `clientDisconnected`
之后被跳过，而上游照样在发 `response.metadata`、用量行照样结算；所以那一侧另有一个**只收票不判定**的
入口（`ObserveWSDownstream`）。交付判定仍然只在写客户端那条路上做：断连之后没有业务输出会送出去，
再产出一条拒服错误只是凭空给调用方加错误路径。

**不存 state 原始值**——信息熵高、长期留存无意义，需要原值的场景（验证、注入）都在票的 TTL 内，
由票缓存表承担；何况它是可注入的凭据，落进用量表等于扩大它的存放面。

这一条与 §5.6「长期存指纹的数字序列」不矛盾，两者是不同性质的数据：state 过期即彻底无用，
**留着也不能回答任何新问题**；数字序列是归因算法的完整输入，**换算法或换校准表后可以重新
回答旧问题**。判据是「将来能否据此重算出新结论」，不是体积或熵。

用 jsonb 而非固定列：特征会持续增加，每加一个就迁移一次很快失控。

顺带一个零成本能力：**响应仍携带 state 就意味着注入没生效**（上游规则：带了有效票就不
下发）。每个请求自动做一次有效性检测。但它**判不了档位**，档位只能靠指纹——两者互补。

两条限定（细节见 §2.4）：这个信号必须属于**本轮上送**才有意义——复用连接留下的旧握手响应头不算；
而「没有 state」也只在能证明本轮确实带票上送时才等于「注入生效」。

### 5.6 指纹探测记录表（长期留存）

**每次指纹探测的原始数字序列都要存下来，长期保留。** 这张表与事件表分开，因为一次验证
包含 1~3 份探测（§4.1 的份数递增），是 1:N 关系，塞不进验证事件那一行。

每份探测记录：所属票与验证事件、第几份、用的哪条挑战、**原始数字序列**、解析到的数字个数、
与各模型中心比较的分数向量、该份的单独归因结果、累计到该份时的平均分与 softmax 概率、
用的哪档校准温度、耗时与 tokens。

**光有数字序列还不够——记录必须能脱离票缓存独立解释。** 票按 §5.3 过期即删，而这些记录要留很久，
所以三样东西得随记录快照下来，不能只存一个指向已被删除的票的外键：

1. **解释上下文**：账号、被验证的那个模型、票的稳定标识与来源、采集与验证时走的出口、当时的空闲值、
   时间，以及该次验证的最终处置。
2. **归因资料的版本**：挑战集、模型中心与环境方向、校准表各自的版本。档 `"1"` 不是一份永不变的
   资料——换了校准表，同一序列会算出不同概率。没有版本标识，历史结论既不能复核也不能重算，而"能
   重算"正是长期留存的全部理由。
3. **有效性与处置分开记**：每份记「解析是否有效、是否计入平均」，整次验证记「最终处置」。

⚠️ **作废的原因不止解析失败。** 候选未被上游接受（§2.4）、配置变更导致晚到结果作废、验证期间票
过期，都会留下**格式完全正常**的数字序列。它们作为观测样本有价值，但**不能当成该候选的有效指纹**,
尤其不能用来标定票的档位——否则将来一次离线重算就会把"当时根本没生效的一次探测"算成该票的证据。

**离线重判不得覆盖原始判定。** 当时系统据以做出决定的那条结论要原样保留，重算结果另存——否则事后
就分不清"当初判错了"和"现在用新校准表看法不同"。

### 5.7 stg0 不新增任何存储

业务路径的 stg0 观测**已经落在上游既有的两列**上，本功能一行都不加：

| 列 | 含义 |
|---|---|
| `usage_logs.upstream_response_model` | 上游回报的 model 原值（归一化后，上限 200 字符） |
| `usage_logs.upstream_model_mismatch` | 上游的审计判据结论；**NULL 表示没观测到**，与 false 是两件事 |

近三天统计因此是一条 `GROUP BY` 查询，分子分母同在一行、同一总体——这正是不另建聚合表的理由：
分母若取自别的表（例如只数请求数的计数器），两侧口径不同（失败请求、非 codex 平台是否计入）会让
"比例"变成两个不同总体的商，无法解释。

⚠️ **聚合键要用"发给上游的 model"，不是 `usage_logs.model`**：存在 model mapping 时客户端请求的名字
与发给上游的名字不同，而门控是按后者判的。上游已有现成表达式与索引
（`COALESCE(NULLIF(BTRIM(upstream_model),''), model)`，见 `idx_usage_logs_effective_upstream_model_created`），
照用即可，不要自己写一个近似的。

验票路径的 stg0 结论走既有事件表（§5.4），`detail` 带响应 model 原值——判死一张票时必须能回答
"上游到底说它给了什么"，否则无从判断是真降智还是该往白名单加一条。

**为什么长期存序列**：数字序列是归因算法的**完整输入**——§4.1 的两个特征（Hellinger 中心
相似度、有序块特征）都由它算出，存了它就能离线重算任何归因变体。这带来三件当下做不到的事：

- **换校准表后能回溯重判**。模型演进会让校准表过时（§4.1 留了外部覆盖路径），届时历史数据
  能用新表重算，而不是只剩一堆无法复核的结论。
- **能自建、修正校准库**。外部校准表是别人在别的环境下标定的；自己积累的序列配上其它手段
  确证的档位，才有可能做本地标定。
- **是移植正确性的持续语料**。§8 那条「归因移植偏差」风险目前靠一份离线 golden file 兜着，
  线上采集的序列 + 当时的归因结果本身就是不断扩充的回归语料。

⚠️ **解析失败而作废的探测也要存**，连同作废原因（拒答、截断、数字不足）。作废率本身是信号：
它上升可能意味着上游改了行为或触发了风控，而不只是"运气不好"。不存就只能看到"没拿到结论"，
看不到为什么。

**不存原始回答文本**——数字序列已是它对本算法的完整提取，文本体积大一个量级。代价是将来若
要用文本类特征（而非数字特征），历史数据用不上、只能重新采集。

体积量级：每份探测的序列约 1 KB，每账号每天约 24 轮验证，即每账号每天几十 KB。默认不做自动
清理——它是分析资产，不是缓存。

## 6. 管理界面

需求是「票的刷新有数据库记录、并能在 web 上查看」。形态由一个工程事实决定，不是偏好问题。

### 6.1 票据配置不进账号表单

上游的账号编辑与新建是两个各自维护的手写巨型模板（`EditAccountModal.vue` / `CreateAccountModal.vue`，
均 5000 行以上），**没有 schema 挂载点**——新字段只能插在某两个 `<div>` 之间，且要在两个模板里
各写一遍；账号类型集中在单体 `src/types/index.ts`。这几个文件都是上游每周改数次的最热文件。

所以：**票据配置和状态、日志一起放进本 fork 自己的页面，不碰账号表单。** 代价是配置分散在
两处（账号的其它设置仍在账号页）。换来的不只是避开冲突——配置只有一个编辑入口，不必再在
Create / Edit 两个模板之间保持字段同步。

这也让「独立小服务渲染」这个备选方案失去了意义：它的全部优势是「零 rebase 成本」，而一旦
配置不进上游表单，页面本身的增量成本已经降到几行追加，再拆一个服务只是多一处鉴权与入口。

### 6.2 单页三区

```
┌─ Codex 票据 ─────────────────────────────────────┐
│ 账号 [OpenAI-PH-610542 ▾]                        │
├─ 配置 ───────────────────────────────────────────┤
│ 模式      ( ) off   ( ) observe   (•) full        │
│ 流量出口  PH-proxy            ← 只读，链到账号页  │
│ 票据出口  ( ) 不设置  ( ) 直连  (•) 代理 [..▾]    │
├─ 当前状态 ───────────────────────────────────────┤
│ 票 #1a2b  astra (p=0.97)  剩余 41m  来源 fetch    │
│ 未验证 2 张 │ 冷却 无 │ 票据出口空闲 12m（本系统可见）│
├─ 事件 ───────────────────────────────────────────┤
│ 时间 │ 类型 │ 出口 │ 静默时长 │ 票长 │ 结果       │
└──────────────────────────────────────────────────┘
```

- **票据出口的三态在 UI 上就是一组单选 + 条件下拉**，「不设置 / 直连 / 代理」各占一项。反过来
  看：若界面上只能用一个「留空即直连」的下拉框表达，那就是在界面层重现 §3.1 要消除的歧义。
- **「票据出口空闲 N 分钟」是运维最关心的那个数**，但它的口径必须按 §4.4 说清：那是距**本系统**
  最后一次使用该出口的时长，**不是实际静默**。系统外的活动看不见，所以这个数会**高估**真实静默
  ——界面不能把它显示成「现在一定能取到好票」。
- **`off` 账号照样显示逐模型的当前状态**（当前票、候选数、取样、历史诊断）。`off` 与 `observe`
  的差别只是不主动探测/取票，票仍然被动收下（§2.3），这些信息本就在库里，藏起来只会让"这个
  账号现在到底有没有票"无据可查。两处必须标清：`off` 下的当前票**不会被注入**（那是上次开
  `full` 时留下的），以及「出口空闲 / 下次可取票」对 `off` 不适用——它从不取票，报一个时刻等于
  陈述一件不会发生的事。**但验票按钮对 `off` 照常可用**（§2.3）：那是人工按需的一次提问，而
  "这个账号的票是什么档位"正是决定要不要给它开 `full` 的依据。
- **聚合统计（成功率 vs 静默时长）不做成图表。** 样本量还在个位数的阶段，固定图表只会让噪声
  看起来像规律；那是探索性分析，事件表能查即可。请求级特征同理不做新页面，
  作为一列挂在 `/admin/usage` 的用量明细上（键值逐行显示，键见 §5.5）。
- 验证事件可以点开看该次的探测明细（几份、每份的归因结果与概率、有没有作废）。但 §5.6 存下的
  **原始数字序列不进界面**——几百个数字在页面上看不出任何东西，它的用途是离线重算，走 SQL 或
  导出即可。
- **每个受控模型显示近三天的 stg0 统计**（数据来自业务请求，见 §5.7）：不一致的**绝对次数与占比**，
  外加三样不能省的东西：

  1. **"未观测"计数单列**。`upstream_model_mismatch` 为 NULL 表示没拿到响应 model，它与"一致"是
     两件事。全是 NULL 时"0 次不一致"是假的安全感——那说明观测根本没工作，不是没被降智。
  2. **不一致的响应 model 原值及次数**（如 `gpt-5.6-luna ×4`）。只给比例的话，运维知道"有降智"
     却不知道该往 `stg0_accept` 加什么（§4.0）。这个 top-N 直接从 `upstream_response_model` 聚合，
     零额外存储。
  3. **不一致要按 `stg0_accept` 拆成"已接受"与"未接受"两份，标红只看后者。** SQL 里那一列用的是
     上游的审计口径（字面相等），它把白名单认可的升级投放、以及同一模型的快照后缀都算成不一致；
     合成一个数时，配好白名单之后页面仍然长期红着，于是这块读数变成背景噪音，一次真正的降智反而
     看不出来。**无谓告警与放过降智同级**。

     拆分的判据必须是验票用的那一个（`KongStg0Accept.Verdict`，连快照匹配一起判），不能是"在不在
     白名单列表里"，也不能在 SQL 里重写一份——两处判据分叉时，页面标红的与实际判死票的就不是同一批
     请求。**明细没覆盖到的剩余量（回报值种类超过取回上限时）一律计入"未接受"**：那是未知，按已接受
     处理会把一次真正的降智显示成中性色。

  这块统计归在**账号那一列**，不与票放在一起：它统计的是业务请求，与任何一张票的生命周期无关，
  是"这个账号有没有在被降智"的读数。仍然逐模型一行——票与结论都是 `(账号, 模型)` 绑定的，合成一个
  数就分不清是哪个模型。

  占比的分母是该 `(账号, 模型)` 近三天的受控请求数，**与分子同一总体**。三天无请求时显示"无样本"
  而不是 0%——后者会被读成"查过了、没问题"。

  ⚠️ **这块统计回答的是"业务流量上被降智了多少"，不是"验票在不在持续判死票"**（§4.0 的同一条
  警告）。票被判死后业务在注入前就拒服、不产生上游响应 model，那时这块统计反而会安静下来。后者要
  看下面那行诊断。

- **顶部的"生效范围"要连白名单一起显示**（`stg0_accept` 与 `accept_extra` 各一行，`模型 → 接受值`）。
  门控集合说的是"保护谁"，这两张表说的是"上游给了别的东西时算不算合格"，同属生效范围。不显示它们，
  上面那行 stg0 的红字就无法判断是**白名单没配上**还是**配上了但页面按别的口径在标红**——而这两种情况
  的处置完全相反。空表要明写"每个门控模型只接受它自己"，留空白会让"没配"与"没显示出来"看起来一样。
  两张表分开列，理由见 §4.0：互抄值会放过真的降智。

- **逐模型的诊断行要能显示 stg0 的结论**。它读最近一次最终验证事件，stg0 判死时显示"上游回报 X"。
  这一行是**默认拒绝极性的真正安全网**：新模型投放导致票被持续判死时，只有它会一直停在 stg0 拒绝上。

  ⚠️ **不能按"概率是不是 1"去猜结论来源**，要读事件里的 `stg`：stg0 的结论没有概率，把上游声明显示
  成 `p=1.000` 会让它看起来像"一次恰好很确定的归因"，而两者的可信度完全不同；显示成 `p=0.000` 更糟
  ——那看起来像归因失败。

  ⚠️ **判据是"最后一次已提交归因的事件"，`final=true` 不够**：一次 inconclusive 收尾也是最终事件，
  但它不写票行——按 `final` 取会读到那条、把来源改回 stg1，于是页面重新显示概率。真正提交了归因的
  事件都填了 `fingerprint_model`（stg0 填回报值、stg1 填 argmax），未提交的不填，所以那一列才是判据。
  这个筛选必须**下沉到 SQL**：在应用层筛就得先按行数上限取一批事件，而一张票被反复重验时那批里可能
  一条已提交的都不剩——票数一多就静默退化成"全部显示为 stg1"。

- **取样行与诊断行分开**。诊断回答"最近一次归因得出了什么"，取样回答"现在还在采样吗、采到的是
  什么"。一个账号可能一直在收票、而每张都被长度黑名单或间隔未满挡在验证之前，那时诊断只会停在好
  几天前的旧结论上，只有取样行能看出现在采到的是什么。

  ⚠️ **取样行的判据是取样的全部出口，不是其中一两类事件**：主动取票（`fetch`，成败都算）、取票
  未发出（`fetch_skipped`）、被动收票含黑名单与去重（`observe`）、未取样（`probe_skipped`）。漏掉
  一个的后果不是少显示一条，而是这一行**停在某个更早的事件上、继续声称一件已经不成立的事**——例如
  只查 `observe` / `probe_skipped` 时，一个连日只能取到 312 的账号在真的取到 292 并进了验证流之后，
  这一行仍显示"长度在黑名单内，不验证，长度 312"，把已经解除的故障说成还在。`verify` 与
  `observe_probe` 不属于取样：前者是结论，后者只标记"探测开始了"。

- **事件表在总览页与单账号明细页共用同一个组件**（`TicketEventTable.vue`）。两处看的是同一份事件
  流，各写一份的话每次改动（给取票事件加耗时这类）都要改两遍，漏一边就变成两种口径。明细页固定
  `accountId`（**名字不能写成 `accountID`**：模板上的 `:account-id` 经 Vue 的 kebab→camel 只会得到
  `accountId`，连续大写不还原，写错那个 prop 永远收不到值、事件表会悄悄列出全部账号，而 `vue-tsc`
  不报错），于是账号筛选器与账号列一起收起来——那一列全是同一个值。**出口列一律把 `proxy:<id>`
  显示成代理名**：裸 id 要人去代理管理页对号才知道走的是哪条线，而查不到时保留原串（代理被删或
  列表未加载完），换成"未知"会把 id 丢掉。

### 6.3 隔离约定

- 页面代码全部放 `src/features/codex-ticket/`，自带 `api.ts` / `types.ts` / `components/`。
  上游的 `features/prompt-audit/` 就是这个形态，隔离范例来自上游自己。
- **我们的 API 自给自足，不调用任何上游 admin 端点。** 账号列表、代理列表、票状态都由自己的
  端点返回。复用上游的 accounts API 等于把 fork 绑在它的响应形状上，而那是个高频改动面。
- UI 组件只读复用（`TablePageLayout` / `DataTable` / `Pagination` / `Select`，事件表可用
  `useTableLoader`）：import 不产生冲突，props 变化会在编译期暴露。
- **页面文案直接写中文，不进 i18n。** `localeKeyCompleteness` 测试只扫 `t()` 字面量，不用
  `t()` 就不受它约束；只有侧边栏 label 需要一个 key。

对上游文件的改动全是追加型单行：

| 文件 | 改动 |
|---|---|
| `frontend/src/router/index.ts` | 一个路由对象 |
| `frontend/src/components/layout/AppSidebar.vue` | `baseItems` 一行 |
| `frontend/src/i18n/locales/{en,zh}/common.ts` | 侧边栏 label 各一行（双语必须同时加，否则 build 失败） |
| `backend/internal/server/routes/admin.go` | `registerKongTicketRoutes(admin, h)` 一行（该文件已是一长串同构注册行） |
| backend handlers 聚合结构体 | 一个字段 |

### 6.4 端点

| 端点 | 用途 |
|---|---|
| `GET /admin/kong/ticket/overview` | 账号列表 + 各自的配置与当前票状态（区 1、2） |
| `PUT /admin/kong/ticket/accounts/:id` | 改 `codex_ticket_mode` / `ticket_egress` / `ticket_proxy_id` |
| `GET /admin/kong/ticket/events` | 事件分页，按账号、模型、类型、时间筛（区 3）。前三者多值，组内 OR、组间 AND，逗号分隔或重复同名参数皆可，空即不过滤 |
| `GET /admin/kong/ticket/probes/:verification_id` | 一次验证的各份探测明细 |
| `POST /admin/kong/ticket/accounts/:id/refresh` | **人工干预**：对指定模型立刻取票/验票 |
| `POST /admin/kong/ticket/accounts/:id/verify` | **人工干预**：立刻重验**当前票**，不通过即作废 |

人工触发的语义与自动路径刻意不同：

- **跳过静默与冷却**。那两条是给自动运行用的保守估计——系统只看得见自己产生的出口活动（§6.2），
  真实静默常常更长，运维能据带外信息判断此刻可不可以取。代价由触发者承担（静默确实不足时会拿到
  坏票、清零静默并进冷却）。
- **不跳过结构性约束**：没配票据出口、出口失效或与流量出口合并、账号不可调度。那些不是调度规则，
  而是物理上做不到或做了有害。
- 先尝试**复位一张未过期的已拒票**再走决策。改过白名单或阈值之后这一步最有价值：同一张票的证据在
  新判据下可能就合格了，而重验走流量出口、不消耗票据出口的静默。
- 同步返回结论，且**不下发票原值**——那是可注入的凭据，只回 `allowed` / `ticket_id` /
  `deny_reason` / `retry_after`。

**立即验票（`/verify`）与它分工不同**：`/refresh` 是"想办法拿到一张可用票"（**可以取新票**），
`/verify` 只验**现有的票**、**永不取票**。这条分界是两个按钮的全部区别。

- **一次最多验两张，顺序固定：先正在服务的那一张，再一张未验候选。**
  - 先验当前票是**安全优先而非效率优先**：它正在被注入、降智输出正在流出，哪怕它马上就要过期、
    哪怕池子里那张候选更新。
  - 候选只验**一张**，取**最新**那张（剩余 TTL 最长；批量取票之后它通常正是刚在 292 窗口里取回
    的那张）。**注意与自动路径相反**——自动路径用 `OldestCandidate`，那是在清一个队列，先验快过期
    的才有机会用上它；人工按钮要的是"这个模型尽快有一张能用的当前票"。
  - 不往更老的翻：候选都来自同一条出口、入库时已过长度黑名单，最新那张归因不合格基本就说明账号
    当前档位低，更老的多半同样不合格，而每张的代价是最多三份真实挑战。唯一的例外场景（账号刚被
    降级、老票是降级前取的）不值得为它常态多烧两倍额度。
- **只有当前票"真降档"才继续验第二张**，其余三种一律到此为止——每一条都是为了不白烧额度：

  | 第一张的结果 | 是否验候选 | 为什么 |
  |---|---|---|
  | 通过 | 否 | 票好着，没有替代需求 |
  | 上游重发了票 | 否 | 我们注入的票**一律**不被接受，候选必然同样被拒 |
  | 未得出结论 | 否 | 旧票与旧结论都还在、**并不缺票**；429/超时那类故障多半在候选上原样重演 |
  | 真降档（`rejected`） | **是** | 当前票已被作废，这时才真的缺票 |

- **同步端点的余量守卫**：实测单份挑战 27–31s、一张票用满三份约 87s，而本端点预算 300s（客户端
  330s）。开始验第二张之前要求剩余 ≥100s，不够就停下并报 `budget_exhausted`，让调用方知道"还有
  一张没验、可以再点一次"——宁可不开始，也不要烧掉额度又在中途被掐断。
- **三态，只在"真验出问题"时作废**：
  1. **已作废** —— 证据完整而归因不合格（真降档，验证流程自己提交 `rejected`），或上游**明确重发了
     票**（`ErrKongTicketNotAccepted`，说明我们这张注入已经不作数）；
  2. **通过** —— 重新自证合格，继续服务；
  3. **未得出结论** —— 超时、429、前提失效、证据写库失败、本端点期限用尽。**旧票与旧结论保留。**

  第 3 类刻意不作废：那些结果没有证伪票的原结论，而重新取票要等约 34 分钟静默——据此作废一张仍在
  TTL 内的好票就是无谓拒服，与放行降智同级。同理，重验路径下**证据不完整时不提交 `rejected`**
  （一份有效回答加两份 429 会让归因概率天然偏低），也**不淘汰别的候选、不推进取票冷却**——那些是
  候选路径的机制，重验一张旧票失败说明不了这条出口现在有问题。
- **不受静默与冷却约束**：验证走流量出口（`VerifyEgress = trafficEgress`），不消耗票据出口的静默
  ——那是全系统最稀缺的资源。占的是账号级槽位，只防"同一账号同时发两拨挑战"。
- **返回 `steps`**：每一张的结果按执行顺序列出，顶层字段等于最后一步。两者都要有——只看顶层会漏掉
  "当前票已被作废、随后候选补上了"这种过程，而"当前票被作废"恰恰是调用方最需要看到的。
- **模式不参与判定**：三种模式都能验（§2.3）。`not_applicable` 只表示**压根没票可验**——当前票与
  可验候选都没有。
- ⚠️ 因此**结论提交的前置状态收 `unverified` 与 `verified` 两种**（`rejected` 仍排除——那是验证期间
  被撤销的信号，不能凭一个此前的结论复活它）。只收 `unverified` 的话重验永远提交不上，每次都以
  「验证期间被改写」收尾，把一张其实合格的票判成失效。

**页面上"立即验票"的判据是"手上有票可验"**：有当前票、或有可验候选，二者之一即显示（只有账号
不可调度时不显示）。**刻意不看票据出口**——验证走流量出口，也不看模式。

⚠️ **"可验候选"的计数必须与服务端实际会选的那一张同口径**（含"没有被排出候选池"这一条）。宽于
它就会显示一个点下去必然报"没有票可验"的按钮，而页面同时还写着候选有几张——两句互相打脸。被排出
候选池的票仍由明细页逐行列出、逐行可验。批量取票之后"有一张未验候选、但没有当前票"
是常态局面，那时正是最该让人把它验起来的时候。

**页面上"立即取票验票"只对 `full` 且可调度的账号显示。** 判据里**刻意不含"票据出口可用"**：
那个端点叫 refresh，它不只取票——服务端的决策里「有成熟候选」优先于取票，而候选验证走流量出口、
压根不要求票据出口可用。按出口可用性藏按钮会把 `egress=none` + 有候选这种真能干活的局面也藏掉，
那是无谓地削弱人工干预手段。

### 票据明细页（`/admin/kong-ticket/accounts/:id`）

总览页每行一张当前票 + 一个候选计数，回答不了"为什么现在没票可用"。明细页列该账号名下的**全部**票，
从总览页那一行点进来。

- **含已过期与已拒的票**：只列可用的那些，等于把答案藏起来。
- **`is_current` 由服务端算**，不是"最新的 verified"。它取决于当前的接受白名单与阈值，前端按
  `status` 自己猜会把"verified 但按现行白名单不合格"的票错标成在用——那正是最需要看清的一种状态。
- **绝不下发票原值**（`state`）。响应只给派生信息：长度、来源、归因分布、时间、状态。同 §6.4 的
  理由——`state` 是可注入的凭据。
- **逐行手工验票**（`POST /accounts/:id/tickets/:ticket_id/verify`）：只验点名那一张，不多验、不改
  挑法。页面上点第 3 行却去验第 1 行，是最容易让人误判的一种行为。三种对象语义不同：
  - 正在服务的那张 → 按重验处理，受"证据不完整不推翻既有结论"保护；
  - 候选 → 首次验证，走正常候选路径；
  - **已拒票 → 先按 id 复位成候选再验**。改过接受白名单或阈值之后同一份证据可能就合格了；而"注入
    未被上游接受"那种拒绝重验会再次以 `candidate_not_accepted` 失败，两者都安全，不必分辨。
  - 已过期的不验——票本身已失效，验它只是白烧额度（最多三份真实挑战）。
- `account_id` 必须参与票的查询条件。那是**权限边界**不是优化：只按票 id 查，换个 id 就能让一个
  账号去验别人的票。
- 触发验票的响应带一份**新的整页**，前端整份替换而不是只更新被点那一行：一次验证会连带改变"当前票
  是哪张"（新验过的票可能顶替旧的、旧票被作废后当前票可能换成别张），只改一行会让页面上出现两个
  "当前"或一个都没有。

**两条即时性由现有机制保证，没有额外代码**：

- **验证通过即加入可用集合**，随后**能否成为当前票由剩余寿命决定**——`VerifiedTickets` 按
  `expires_at DESC` 排序，`kongPickCurrent` 取第一个按当前白名单仍然合格的。多数时候新验过的票
  过期最晚、于是立刻接替；但**一张更早过期的票验过之后不会顶替当前票**（它进可用集合备用，当前
  票撤销后由它接替）。这是刻意的：目标是让票服务最久，让更早过期的顶替更晚过期的好票，只会让该
  模型更早进入无票期——那是无谓拒服。页面文案据此写"已进入可用集合"，不写"已升为当前票"。
- **当前票被判失效后立即停止注入**——业务每次请求都走 `kongPickCurrent` 查库，没有任何缓存，而
  `rejected` 与 `skip_until_new` 都被那条查询排除。已经取到票、正在飞行中的那个请求不受影响
  （它手里的快照不会被追回）。

⚠️ 不要沿用上游 `proxy_id` 那个「提交 `0` 表示清除」的约定（前端把 `null` 转成 `0` 发出）。
`ticket_egress` 是显式枚举，空值语义不靠哨兵数字表达——那正是 §3.1 要避免的歧义。

## 7. 配置项

**全局**（只剩与调度节奏相关的，不再有启用开关与出口）：

| 项 | 作用 | 默认 |
|---|---|---|
| `refresh_before` | 票剩余时间低于此值即异步预取下一张 | **300s** |
| `ticket_fetch_min_idle` | 两次主动取票之间该出口的最小静默 | **1800s** |
| `verify_fail_cooldown` | `fetch` 来源的票验证失败后的退避期 | **1800s** |
| `min_ticket_age` | **observed** 票入缓存后至少等多久才验证（fetch 票不受此限，见 §4.2） | 300s |
| `observe_probe_interval` | `observe` 模式的探测周期 | 3600s |
| `state_len_denylist` | 已知的坏票长度 | `[312]` |
| `accept_extra` | 各门控模型额外接受的**归因**结果（stg1；自接受由代码补齐，见 §4.1） | `gpt-5.6-sol:gpt-6-astra,gpt-5.5` |
| `stg0_accept` | 各门控模型额外接受的**上游回报 model**（stg0，见 §4.0） | `gpt-5.6-sol:gpt-6-sol` |
| `batch_fetch_all_models` | 一次取票是否**并发**把所有门控模型的票一起取回来 | **true** |

⚠️ **`stg0_accept` 与 `accept_extra` 是两张表，不要合并、也不要互相抄值**（理由见 §4.0）：
`accept_extra` 里的项是**指纹区分度不足的补偿**——`gpt-5.6-sol` 接受 `gpt-5.5` 是因为指纹分不开
这两者。而 stg0 读的是上游自己的声明，没有这种噪声，把那条补偿抄过来会放掉一次上游明说的降智。

`stg0_accept` 的用途是**升级投放**：上游有时把新模型投到旧模型名上（请求 `gpt-5.6-sol` 回
`gpt-6-sol`）。这一条已经是内置默认，别处再遇到同类情况按 `<请求的模型>:<回报的模型>` 追加。匹配含
版本后缀（`gpt-6-sol-2026-08-01` 命中 `gpt-6-sol`），所以上游换快照不必改配置。

**每加一项都要能说清"为什么这个替换不算降智"**，判据只有一条：拿到的是不是更新/更强的模型。说不清
就不加——这张表是默认拒绝极性上唯一的缺口。设成**空串**是一个有意义的指令（回退到只接受模型自己），
不会被当成"没配"忽略。

门控模型集合走环境变量 `KONG_TICKET_GATED_MODELS`，**默认 `gpt-6-astra,gpt-5.6-sol`**；
**未设置取该默认、显式设成空串则整个功能不装配**（区分这两者的理由与时间参数一致：把"配了个
空值"当成默认，等于运维以为关了、系统其实在跑）。默认开着门控不等于默认改变行为——账号级
`mode` 缺省是 off，此时每个请求都判 `NotApplicable`，照常服务、不注入也不拒服。

**同时门控两个模型靠 `batch_fetch_all_models` 成立**（默认开）。不开它的话这是算不通的，且与
参数取值无关：票按 (账号, 模型) 分别存而取票共用出口槽（静默门槛跨模型），两个模型各自续期就
要求出口上相邻两次取票相隔 `I/2 ≥ 门槛`，即 `I ≥ 2×门槛 > TTL`；而
`I = TTL − refresh_before < TTL`。无解——那时只能要么只把一个模型设成 `full`，要么接受空窗
（静默设到门槛之上，每个模型每周期都有一段无票期，约 11.8%）。

批量取票换掉了这个推导的前提。它依据一条实测事实：**292 窗口一旦打开约 4 分钟内有效，且窗口内
的活动不会把它提前关闭**（三次独立观测一致；42 秒一次轮询仍撑满 4 分钟）。所以一次静默换来的不是
"一张票"，而是一个**可连续取票的窗口**——在窗口里把所有门控模型的票一次取齐，成本与取一张相同，
于是一个周期只有一次出口活动、覆盖全部模型，空窗消失。

⚠️ **那条事实是这个开关的地基**：不成立时第二张起必然拿到 312（被长度黑名单挡掉）。换上游、换
出口之后要重新确认。

### 取票融合指纹（`fetch_fused_fingerprint`，默认开）

取票请求的 prompt 换成**第一份指纹挑战**，它的正文直接当第一份样本用——省掉一次往返（取票 ~1s、
单份挑战 27–31s）。批内每个成员都这么做，各自归因各自那张票。

**为什么这份样本能用来判定那张票**：turn-state 是 **sticky-routing token，钉住的正是那次请求被路由
到的目标**，且在 turn start 下发（读 codex 源码所得，见研究资料 `FACTS.md` 的协议事实一节）。所以
取票那次响应的正文，就是那张票所钉住的那个目标生成的——票是该次请求路由结果的载体，不是独立于它
的东西。这与「票是因不是果」不矛盾：那一条讲的是注入一张票会把**后续**请求钉过去。

**为什么必须留开关**，两处差异是推理而非实测：

1. **出口不同**：融合样本在**票据出口**上产生，而业务注入用票时走**流量出口**。「票是否编码了打票
   时的出口 IP」仍是未决问题（`FACTS.md` §5 Q1）——若编码，融合样本测到的是"打票出口上的档位"。
2. **无票态 vs 注入态**：融合样本是无票状态下的路由结果，注入后是按票路由。

叠加早停（生产实测多数验证一份样本就判定接受），融合样本往往是那张票的**唯一**证据。所以：

- **未达标时不据它判不合格**，只记一条 `inconclusive`（`fused: true`）。触发模型**丢弃这份样本、按
  常规路径重跑**（带票、走流量出口）——有请求在等，值得那几份长答案；非触发模型不重跑。
  混进后续样本是不行的：两类样本产生条件不同，混合归因会让概率失去意义，而「证据不完整不推翻既有
  结论」那条保护也是按同条件样本设计的。省不到就退回原路，不产出半污染的结论。
- 探测记录标 `fused` 且 `verify_egress` 记**票据出口**。不标的话事后分析会把两类混算，直接毁掉
  "哪个出口取到好票"这个判断。被丢弃的那份**留档但标 `discarded_reason` 并把 `counted_in_average`
  置假**——不置假的话，离线按它重算会把样本混回去，得出与线上不同的结论。
- 融合样本占 `part_index = 0`，常规挑战从 1 起。**不能共用 1**：融合被丢弃后 leader 会重跑一份同样是
  "第 1 份"的常规样本，同一个 verification 下出现两条 `part_index=1`，页面显示两个第 1 份、离线重算
  也分不清先后。
- **"取到票"与"融合样本可用"是两件事**。正文读失败（提前 EOF、SSE 错误）时票已经在响应头里拿到了，
  绝不能因此把整次取票判失败——那会连一张好票一起丢掉，而取票要花一整段出口静默。那时保留观测、记
  一条 `inconclusive`，并且 `fetch` 事件按**是否发起过融合**记 `fused`（额度已经花了），另记
  `fused_read_error`。装作没融合会让"额度花在哪了"这个问题答错。
- 新增迁移 `901`：`outcome` 的 CHECK 加 `inconclusive`，探测表加 `fused` / `discarded_reason`。
  **不改已应用的 `900`**——迁移框架按文件内容算 checksum。
- 取票期限在融合时改用挑战那一档（180s，因为要等整份答案生成），仍远小于 292 窗口的约 4 分钟寿命。

**成本**：融合不省额度（长答案本来也要在验票时生成），省的是一次往返；未达标重跑时多花一份长答案。
所以它的收益取决于早停命中率——生产实测 3/4。

⚠️ **上线后要看的一件事**：融合判定 verified 的票，在业务实际注入时有没有异常。`AfterUpstream` 那条
"上游重发票"的撤销率上升就是信号——那意味着上面两处差异中的某一条不成立。

### 批量取票的四条设计取舍

- **并发而不是串行**：全部取票请求同一瞬间发出，于是不存在"窗口在批次中途关闭"这回事，也就不
  需要批次预算与顺序安排。

  ⚠️ **事件流上它读起来像串行，那是时间戳语义造成的**：`fetch` 事件的时间是请求**完成**时刻
  （它同时就是出口活动 A，不能改成发起时刻），而融合取票要等整份挑战答案生成完——实测同一批里
  27s 与 43s 的一对很常见，两条事件于是差 16 秒落库。判据是 detail 里的 `duration_ms`：各自用
  完成时刻减去耗时，得到的发送时刻相等才是并发。少了这个字段，"批量取票是串行的"这个错误结论
  无法被证伪。

  `duration_ms` 的起点是**请求交给传输层那一刻**，不是"进入取票函数"：凭据准备（读 token 缓存、
  必要时同步刷新 OAuth、等刷新锁）在它之前，而批内共用一个账号——第一个成员触发刷新、其余等锁时
  实际发送本就错开，把那段算进耗时会让反推起点**凭空对齐**，恰好把要看的信号抹掉。一个字节都没
  发出去时不记这个字段（`fetch_skipped` 的两条路径都是如此）。
- **批内不发独立的验证请求**（原本叫"只取不验"，融合取票之后改了形态）：其余模型的票入库为候选
  （`source=fetch`，因此不受 `min_ticket_age` 限制）。开了融合时它们各自的取票响应就是一份指纹
  样本，**够判定就地落成 verified**，那张票立刻可用；不够则留作候选、**不重跑**——此刻没有请求在
  等它，不值得再花一份长答案。候选的验证仍留给该模型的下一个请求，且**在它进入刷新窗口时就异步
  验**（`inject_and_verify_candidate`），而不是等旧票过期再同步验；后者会让每个周期都有一个请求
  白等一次验证。关掉融合就回到原形态：批内一份额度都不烧，验证全留给各模型自己的第一个请求。
- **无条件补全所有门控模型，不只补缺票的**。只补缺票的会让各模型的周期长期错开，错开 30 分钟就
  意味着 30 分钟后要在出口上再取一次——而那低于静默门槛，必拿 312。无条件补全把各模型的到期时刻
  对齐，此后每周期一次批量即可覆盖全部。
- **非触发模型的失败只记事件、不进冷却、不影响返回值**。冷却是给失败的取票路径退避用的，而触发
  模型本周期已经证明这条路是通的，罚出口只会把下一个正常周期也往后推。**触发模型也不等它们**：
  等齐全部成员会把非触发模型的慢失败算进触发请求的等待时间（60s 取票失败 + 270s 验证就超过
  300s 的等待预算，而触发模型自己只需 271s）。槽位释放仍要等齐——出口上还有在途请求时别的账号
  不能开始取票。
- **批内的 fetch 候选不参与"本段无票期"的批量淘汰**（`SkipCandidatesFor` 只标 observed）。它们是
  一整段出口静默换来的，被旧 observed 候选的失败连带标掉等于白扔一次取票，而出口约 34 分钟内补
  不回来。正在验的那张由调用方单独标，否则会被无限重验。
- **取票请求有独立的连接池容量下限**（`kongFetchPoolFloor`）：账号隔离下 `MaxConnsPerHost` 等于
  `account.Concurrency`，取 1 且走 HTTP/1.1 时批内请求会在传输层被串行化——"并发所以不存在先后"
  这个前提就悄悄不成立了。只抬取票这条路径，业务连接池不动。

记账两处必须注意：**全批共用批前那一个静默值**（取票自己就是出口活动，第一发一落地就把"上次
活动"推到了现在，各算一次的话后面那些成员会记成 idle≈0——字面为真却把事实记错，它们骑的是同一
个窗口）；批内每次取票**照旧只记一条 `fetch` 事件**（"取了几次"是额度口径），detail 里带
`duration_ms`、`batch:{leader_model, index, total}` 与 `state_fingerprint`。后者用来回答一个开放问题：批内各模型
拿到的是不是同一张票——若恒为同一张，就等于实测出"票与模型无关"，跨模型复用（§5.3 的票身份现在是
`(账号, 模型)`）可以直接落地，这套批量取票还能再简化一层。

`accept_extra` 在
上游 config 结构里，键路径 `gateway.kong_codex_ticket.accept_extra`，因此也可用
`GATEWAY_KONG_CODEX_TICKET_ACCEPT_EXTRA` 设定（viper 的 env 绑定）。格式
`model:accepted[,accepted…][;model:…]`；**显式设成空串即没有任何额外接受关系**。

⚠️ 那个空串语义要靠一处显式覆盖兑现：viper 的 `AutomaticEnv` **忽略空环境变量**，只靠它的话
"设成空串"会被当成未设置、仍取默认值——而那个方向是放宽判据。覆盖在
`internal/config/kong_ticket_config.go`，与上游对 `SERVER_TRUSTED_PROXIES` 的处理同一范式。

⚠️ 白名单里指向**不在门控集合的模型**或**库外模型**的条目会被忽略并打 `slog.Warn`，**不让启动
失败**。那两类都是死配置，忽略只会让白名单比预期更小（方向是拒服而非放行降智），而拒绝启动会把
配置笔误升级成整个网关不可用。格式错误（缺冒号）仍然让启动失败。**没有热更新**——上游没有配置文件监视，config 只在启动时
读一次，改了要重启。

### 7.1 为什么 `refresh_before` 要小

**取票间隔 = TTL − `refresh_before`**，而这个间隔就是票据出口积累到的静默时长。静默是拿到 292
的**必要条件**（§1.1.4）——注意它不等于「astra 的充分条件」，292 桶内还分档，成因未定（§1.2.3）。
所以这个参数越小，可积累的静默越多、越有机会拿到好票：

```
refresh_before = 300s   → 间隔 3300s（55 分钟静默）
refresh_before = 1900s  → 间隔 1700s（28 分钟静默）← 踩在实测门槛下沿
```

**直觉会把提前量往大调**（怕空窗），而它挤掉的正是静默。这个取舍不是请求驱动独有的——定时器
方案里只要「票剩余不足即刷新」，提前量同样会压缩间隔（v026 实例把提前量设到 1900s 就把间隔压到
了 28 分钟）。区别只在请求驱动下没有一个独立的 `interval` 参数兜着，取舍暴露得更彻底。

由此还能否掉一个看似合理的想法：**「给预取失败留重试余量」是自我拆台**。静默是目前唯一已知
可控的杠杆（§1.2.3），而加大 `refresh_before` 恰好缩短它——用可能提高失败率换来的重试机会，
重试时多半还是坏票。

下界只由「一次预取要多久」决定：取票几秒 + 验证最多三份（三份都一次成功约 75 秒），300s 留了三倍
余量。**75 秒不是上界**——允许无效回答重试之后，真正的上界由重试次数与整体超时决定（§4.2）。

⚠️ **但预取失败或超时的代价远不止「那个请求多等一会」。** 预取一旦接触过票据出口就把静默重新
计时了，之后的同步路径会撞上 `ticket_fetch_min_idle` 这道门——此时的处境比长期闲置后的冷启动
更差，不是更好。具体算法见 §4.3 的时序口径；用默认值算出来是约 30 分钟拒服。

### 7.2 其余取值的依据

- **`ticket_fetch_min_idle` = 1800s**：实测「PH 出口空闲 30 分钟后会拿到 292」的直接对应值。
  正常路径上它根本不起作用（间隔 3300s 远超它），只在异常路径（预取失败后重试、冷启动紧接
  上一次取票）上当门。在那种时刻**宁可拒服，也不要浪费一次把静默清零的取票**——所以这个门
  宁高勿低。
- **`verify_fail_cooldown` = 1800s**，与 `ticket_fetch_min_idle` 取同值。**取同值不等于「失败后
  不额外加时」**——两者起算点不同（C 从失败判定时刻 `F`、M 从最后活动时刻 `A`），实际期限是
  `max(A+M, F+C)`，仍比单纯的静默门槛晚了 `F − A`（§4.3）。取同值的理由是：失败发生在远超已知
  门槛的静默之后，再多等一截**没有对应的理论收益**；但也不宜更小，否则它在多数分支里不起作用。
  保留可调，若数据显示「等更久确实更容易成功」再抬高。
- **`min_ticket_age` = 300s**：真正限制 observed 票验证频率的是 §4.2 的结构（只在无票时验、
  每次只验一张），不是它。它只负责在同一批票里偏向较早收到的那张，取值不敏感。
- **`observe_probe_interval` = 3600s**：与票 TTL 同尺度，每个票周期取一个采样点。成本每账号每天
  约 36k tokens 输出——那是"持续有样本、每次只需一份"的下限，需要三份时约三倍，不是固定值。

**账号级**：`codex_ticket_mode`、`proxy_id`、`ticket_egress` + `ticket_proxy_id`（见 §3）。

`state_len_denylist` 刻意做成"拒绝已知坏的"而非"要求特定长度"：上游改长度时，前者只在
出现新坏长度时需要人工介入，后者会让整个特性静默失效并触发拒绝服务。

### 7.3 值域与组合约束

默认组合 `300 / 1800 / 1800 / 300 / 3600 / [312]` 之间没有代数矛盾。但参数一旦可调，就不能再只
靠默认值推导——每个参数各有约束：

| 参数 | 约束 |
|---|---|
| `refresh_before` | 必须小于票的可用寿命；要真正避免续期空窗，还得覆盖「请求到达延迟 + 取票验证耗时」 |
| `ticket_fetch_min_idle` | 约束**所有**取票入口。若它大于可用续期间隔（`TTL − refresh_before`），预取会被这道门推迟——**推迟不等于空窗**，见下面的条件 |
| `verify_fail_cooldown` | 与静默门槛**共同**决定下次尝试时刻（§4.3 的 `max`）。作用域按 **`(账号, 票据出口)`**——它退避的是取票，而取票消耗的是出口静默（§1.1.4）；它**不阻止 observed 验证**，那条路径不碰票据出口 |
| `min_ticket_age` | 取值 `≥ TTL` 会让被动票永远得不到验证资格。选候选时还要看**剩余寿命**，不能只看存放了多久 |
| `observe_probe_interval` | 是相邻两次探测的**最小间隔**，不是定时周期（§2.3） |
| `state_len_denylist` | 只做廉价排除。不在表里**不等于**合格，仍须过指纹（§2.2） |

无空窗续期的条件要按**实际完成时刻**写，不能按"能不能立刻开始取票"写：

```text
无空窗 ⟺ Q + L ≤ E
  Q = 刷新窗口开启后，同时满足静默与冷却期限的首个业务触发时刻
  L = 该次取票到验证完成的耗时
  E = 当前票的过期时刻
```

`L ≤ refresh_before` 是其中一项必要条件。而 `ticket_fetch_min_idle ≤ TTL − refresh_before`
**只是"预取不被静默门槛推迟"的理想条件，不是无空窗的必要条件**——反例：`M = 3400`、`A = 0`、刷新
窗口在 3300 开启，取票要等到 3400 才允许开始，但 `L = 30` 时它在 3430 完成，早于 `E = 3600`，空窗
为 0。把这两件事混为一谈，会让参数提示和保存校验拒绝掉本来可用的配置。

**会让某条路径真正不可达的配置应当拒绝保存或明确提示**，而不是等运维从事件表里反推。

**哪个条件约束哪一步**，不要混在一起谈——混了就会出现"因为出口还没静默够所以连缓存里的票都不验"
这类错误实现：

| 条件 | 约束范围 |
|---|---|
| 账号可正常调度（§4.2） | 本功能主动发起的**全部**请求：取票、指纹验证、`observe` 探测 |
| 出口分离（§3.2）、票据代理有效 | 只约束**主动取票** |
| 静默门槛 `M`、冷却 `C`（§4.3） | 只约束**主动取票**——验证走流量出口，不消耗票据出口的静默 |
| 无票期内的候选限制（§4.2、§4.3） | 只约束 **observed 候选的重复验证** |

### 4.6 取票期间这次请求去哪

取票加验证要几十秒（实测取票 ~1s、单份挑战 27–31s）。让发起请求原地等满，在多账号环境下是浪费——
别的账号此刻可能就有票。

**票据层判一次、上层决定怎么等**，两层分工刻意分开：

1. **票据层**（`handoffWorthIt`）：查"别的账号在同一模型上有没有一张**按当前白名单仍然合格**的票"。
   - 有 → 起异步任务，报 `preparing`，不等。⚠️ 票据出口被**别的账号**占住、本账号这一轮压根起不了
     任务时报 `egress_busy` 而不是 `preparing`：后者的含义是"本账号的任务正在跑"，那是唯一值得先在
     同账号等一等的原因（见本节第 2 层）。
   - 没有 → 换号救不了，退回同步等（与此前行为一致）。
   - **必须按当前白名单重判**，不能只看 `status = verified`：那个状态只表示"按当时的白名单判过"，
     而白名单与阈值来自环境变量、随时可改。
   - **不确定时一律退回等待**（查询出错、没有别家、别家都不合格）。等一定能服务，而错误的交接代价
     远不止"多换一次号"——上层会把本账号加进失败列表，于是**它即将补上的那张票再也用不上**，请求
     最终被拒。那是无谓拒服。
   - ⚠️ 残余风险：这个查询看不到别的账号的 **mode 与调度资格**（那在调度层）。所以返回"值得交接"
     仍可能遇到那些账号都不可调度、failover 耗尽的局面。要彻底消掉它，票据层得把候选账号 id 交给
     上层、与本请求的可调度集合取交集——**尚未做**，见本节末尾的遗留。
   - **在途任务也走同一判定**：后到的请求不能直接进最长五分钟的同步等待，否则（一）无绑定的新请求
     明明能换号却原地等；（二）绑定请求首次拿到 `preparing`、重试一次就被这条路径吞掉，
     `pool_mode_retry_count` 再也控制不了等待。
2. **上层**（`KongTicketFailover`）：**所有票据拒服都可以 failover**，因为它们全是**账号级**条件
   ——静默与冷却按该账号的票据出口累积、出口配置是账号自己的、任务没拿到票也只是这个账号没拿到。
   真正全局的情况（所有账号都没票）由 failover 框架自然耗尽，不在这一层提前判定。

   ⚠️ **这里曾经只放行 `preparing`**，理由写的是"其余换号只是把同一个结论在每个账号上重演"。**那个
   推理是错的**，它把账号级条件当成了全局条件。生产实测（2026-09-20）：一个账号因 `window_closed`
   拒服期间，同分组另一账号已持有合格票，**64 个请求仍被直接拒掉、一个都没切过去**——那是无谓拒服，
   与放行降智同级。实例见 `../INCIDENT-20260920-ticket-deny-as-502.md`。

   ⚠️ `ensure_failed` 里有一种**账号级**情形要单独摘出来：调度到准入之间账号可能被删掉。那时
   `ensureTicket` 直接返回 `account_unready` 的拒服而不是错误——包成 `ensure_failed` 会带上
   `NextAccountStop`，于是"A 已不存在、B 持有合格票"时整个请求在 A 上终止，那是无谓拒服。其余读取
   失败仍是系统级（换一个账号同样读不到）。

   ⚠️ **拒服身份靠错误类型认，不靠 reason 字符串**。`*KongErrTicketDenied` 有十几个构造点，其中六种
   原因带自由文本（`ensure_failed: <err>`、`model_undeterminable: <reason>` 等），维护一张字符串清单
   反查必然漏项——漏一项就是把一次本地拒服计进账号健康度 EWMA（实测 `ensure_failed` 让 EWMA 从 0
   跳到 0.20）。所以转换时**保留原始错误**（多值 `Unwrap`），而 failover 错误自己的 `Reason` 落在
   `kong_ticket_denied:<分类>` 这个受控命名空间里，供只拿得到 `*UpstreamFailoverError` 的呈现层认。
   规范化同时挡住一条泄漏：`Reason` 会流进给客户端的文案，自由文本会把库连接串、代理地址一起带出去。

   **身份判定要认两种传参形态**：多值 `Unwrap` 只能由外向内查找，所以调用方一旦先 `errors.As` 把
   `*UpstreamFailoverError` 摘出来、再把那个指针单独传给调度上报（WS 换号就是这样），从它反查不到并列
   的 `*KongErrTicketDenied`。那时靠受控命名空间识别。只认类型会在这类消费点上静默漏掉。

   **换号有没有用另判一次**（`kongDenyIsAccountScoped`）：上面列的九种调度原因都是账号级的，换号有用；
   而 `model_undeterminable` / `frame_undeterminable` / `inject_failed` / `inject_unverified` /
   `ensure_failed` 这些是**请求级或系统级**的技术原因，每个账号上都会得出同一个结论，换号只是把它在整个
   账号池上重演一遍，还白占别人的槽位。它们**照样包成 failover 错误**（身份、调度豁免、503 呈现三件事
   在所有拒服原因上走同一条路），只是带 `NextAccountStop`——上层立刻进入耗尽呈现。
   这个分类正向列举、默认"不换号"：漏一项只是少拿一点收益，而反过来默认换号时漏判会让一个必然失败的
   请求走完整个账号池。

   同账号先不先等（`RetryableOnSameAccount`）另按**这个原因在几秒内是否可能自行好转**定：
   - **值得等**：`preparing`（本账号的任务正在跑，产物就是本账号要的票）、`other_model_task`（本账号
     的在途任务在跑别的模型；它可能是候选验证——走 `kongVerifyOnlySlot`、不碰票据出口——完成后本模型
     确实可能可取）。
   - **不值得等**：`window_closed`（要到某个时刻才满）、`egress_unusable` / `no_ticket_source`（配置
     问题，等不会变）、`task_no_ticket` / `wait_timeout`（任务已结束且没拿到票）、`egress_busy`。对这些
     先等本号是把重试次数花在一个确定不会变的状态上，还会延后真正能服务的那次换号。

   ⚠️ **`egress_busy` 曾被算作"值得等"**，理由是"别的任务占着，马上释放"。那个理由不成立：占着出口的
   是**另一个账号**的任务，它产出的票属于那个账号；更要紧的是那次取票会把这条**共享**出口的静默清零
   （`noteEgressUse`，§4.4），于是它一释放本账号立刻需要等满整个 `M`——状态从 `egress_busy` 变成
   `window_closed`，正落在"不值得等"那一类。

   值得等的那几种还要看**这个请求有没有绑定粘性会话**：绑定了先等（换号会让上下文缓存失效、粘性计费
   被强制），没绑定直接换。⚠️ **当前读不到绑定信息时按"可能绑定了"处理**——`WithPrefetchedStickySession`
   只有通用 Gateway 与 Gemini handler 会写，**OpenAI Responses/Messages 主路径不写**。两种猜错的后果
   不对称：该换却先等只多花几秒重试延时（`RetryableOnSameAccount` **不阻止换号**），该等却直接换会破坏
   会话。代价是"新请求立刻换号"这个优化暂时拿不到。

3. **呈现**（`kongWriteTicketDenyExhausted`）：failover 耗尽后——即**所有账号都没票**——返回
   **503 + `Retry-After`**，消息里带拒服分类与"最早可再来"的时刻。

   ⚠️ 不能落进通用上游错误映射（`502 / upstream_error / "Upstream request failed"`）。票据拒服**一个
   字节都没发给上游**，把它呈现成上游故障会同时带偏三处：排查方向指向上游与账号健康、供应商质量统计
   被记一笔（`error_owner=provider` 而 `upstream_status_code` 是 NULL，自相矛盾）、客户端按"网关故障"
   重试而不是按给定时刻等待。

   **挂载点必须是真实的耗尽 handler**（`handleFailoverExhausted` / `handleAnthropicFailoverExhausted`），
   不是通用兜底 `ensureForwardErrorResponse`。第 2 层把拒服包成了 failover 错误，它的终点就在耗尽
   handler；⚠️ 初版挂在通用兜底上，**单测全绿而生产照旧 502**——那条兜底只接"根本没走 failover"的错误。

   **识别绑定当前终止错误**，不读请求级的历史标记：A 因 `window_closed` 被拒、换到 B 后 B 真的 500，
   终止错误是 B 的，这时呈现必须是上游故障。反过来记就是把归因错误反着犯一遍。

   **等待时刻取已尝试账号中最早的那个**（`KongTicketDenyWait`），不是最后访问那个的：A 一分钟后可重试、
   B 三十分钟后可重试，按 B 给会让客户端白等二十九分钟。**任一被拒账号恢复时刻未知时干脆不给
   `Retry-After`**——未知不等于很久，拿别人的长等待去挡会把一个几十秒后就有票的池子搁置二十分钟。
   文案说"最早可再来"而**不说"保证那时恢复"**：那个时刻只表示静默/冷却期满，届时能否取到合格票仍取决
   于上游。

   503 而不是 502 是因为语义：502 是"上游返回了无效响应"，而这里没有上游交互；503 + `Retry-After`
   是"服务暂时不可用"的标准表达。`Retry-After` 给**秒数**不给日期——日期要求两端时钟一致，而它们可以
   差好几分钟。

**Live（realtime）是第五条通路，本期按拒绝处理**。它把模型放在 `session.model`，顶层没有 `model`
——按顶层判会得出"非门控"，于是受保护账号会在门控模型上发出一次**无票上送**，那是放行未受保障的输出。
创建端点因此按嵌套字段判定；判出门控模型就拒服（`live_unsupported`），因为这条通路承载不了票据：创建
之后是 sideband 原始双向转发，既没有逐轮注入点，也没有「上游是否接受了这张票」的证据。sideband 的
客户端帧另有一道守卫（`LiveFrameGuard`），挡住"先建非门控会话、再 `session.update` 切到门控模型"的
绕行。要支持受控 Live，需要先定下票据载体与交付证据——**需调用方定**。

这道拒绝的**作用域与别处一样是"受保护账号"**：`off` / `observe` / 未配置票据出口的账号照原样放行，
否则一条本期不做的保护会变成对全部 Live 用户的功能下线。守卫**每条连接解析一次**而不是每帧解析——
realtime 是音频帧流，逐帧读账号等于把一次会话变成对数据库的持续打击；装配时账号读失败或账号不存在
即**拒绝建立连接**（fail-closed），此刻无法判断账号是否受保护，放过去就可能是一次无票上送。
Live 的两个终点都不经过 failover 耗尽 handler，所以统一呈现要各接一次：创建落 503 + 分类，sideband
落 1008 + 分类，两处都写下"最终以票据拒服收场"的归因标记——否则前者说成 `502 / api_error`（把本地
策略决定说成上游挂了），后者说成 1011（让人去查网关自己）。

**还有第三条传输通路**：客户端 HTTP 进来、上游是 WebSocket（`openai_ws_forwarder_v2.go` 的
`PrepareWSMapPayload`）。它既不过 `doOpenAIUpstream` 也不过两条原生 WS 适配器，所以拒服要显式走 HTTP
那套统一转换——客户端是 HTTP，终点应当是 HTTP 的耗尽呈现（503），不是 WS 的关闭错误。裸返回会让
`errors.As(*UpstreamFailoverError)` 不命中、请求落进通用兜底的 502 且不换号。

**首输出守卫不得盖住票据判定**。守卫的 context 也套在准入上，所以"等本账号的取票任务"超过首输出期限
时它会先被触发。票据判定必须排在它前面，否则一次本地拒服会被改写成 `504 / first_output_timeout`：
票据身份丢失、错误记到上游头上、账号健康度被罚，而一个字节都没发给上游。换号在那里是安全的——
`startTime` 是每次 `Forward` 取的，下一个账号会拿到完整的首输出预算。

**准入被调用方的 context 中断算账号级 `wait_timeout`**，不是系统级 `ensure_failed`：这个账号这次没能
及时完成准入，换一个此刻有票的账号完全可能立刻服务。判据放在**准入的共同边界**（`kongEnsureDenial`），
不是某一个数据库调用上——这一路到处在读库（账号、当前票、候选、出口活动、冷却，任务完成后还要再读一
次票），守卫一取消，其中任何一处都会返回 context 错误，逐个补必然漏。不按 `ctx.Err()` 区分"是谁取消
的"——守卫用 `WithCancel` 取消，拿到的是 `Canceled`，与客户端断开没有区别；客户端真的走了那一路另有
判据（上层在 failover 之前先查 `failoverClientGone`）。

**六个自行处理传输错误的 `doOpenAIUpstream` 调用点**（embeddings、responses input_tokens、Anthropic
count_tokens、images、images_responses、live）在写 ops 传输错误之前先判票据拒服，交给统一转换。
⚠️ **不要按入口的模型名推断"受控模型到不了这里"**：出站载荷的顶层模型可以由别的机制决定——
images_responses 的驱动模型就来自 `SUB2API_IMAGES_MAIN_MODEL`，与入口的图片模型白名单无关。

**原生 WebSocket 不参与交接**（`EnsureTicketNoHandoff`）。那条路上的拒服落点是按策略关闭连接（1008），
没有 failover 可走——返回 `preparing` 等于把一条只需等几十秒的连接直接断掉，而改动前它是同步等的。

**不新增等待参数**：轮次与间隔复用账号已有的 `pool_mode_retry_count` 与 failover 框架的重试延时，
等待的自然终点是任务完成或重试次数用尽。

**它不是账号的故障**，所以 failover 错误带 `RequestScopedTransient`，并在调度上报处显式豁免
（`KongIsTicketDeniedFailover` / `KongIsTicketDenied`，两道都走 `errors.As` 认类型，覆盖全部
deny_reason，含带自由文本的技术原因）。计进错误率 EWMA 会让"票越缺、账号越被判坏"，与事实相反；临时
封禁更糟——那会把一个几十秒后就补上票的账号推出调度。

**并发**：`claimTask` 的 single-flight 不变——同一账号只有一个在途任务，后到的请求要么交接、要么等
它。票不是独占资源，TTL 内可反复注入，换过去的请求不会互抢。

**遗留**（都是"收益没拿满"，不是回归）：

1. 票据层只回一个布尔，没把**可接手的账号 id** 交给上层，所以上层无法与本请求的可调度集合取交集。
   别家账号全部不可调度时，failover 会耗尽而不是回来等。
2. OpenAI 主路径不写绑定事实到 context，于是"新请求立刻换号"退化成"先等几次重试再换"。
3. 原生 WS **只有首轮**参与 failover。首轮是这条路上唯一可换号的时点，三个条件同时成立：还没有任何
   字节写给客户端、relay 尚未启动（`RunEntry` 在首帧写出之后才调用）、首帧也还没写上游。所以首轮的
   账号级拒服包成 failover 错误交给上层换号（`wrapOpenAIWSFirstTurnKongTicketError`），客户端连接
   全程不断；WS 的同账号等待 helper 也放行票据拒服（原先只认 429 的 deadline，于是 `preparing` 只能
   靠换号，而换号在 WS 上要丢掉上游连接与它的上下文缓存）。耗尽时关 **1013** 并带分类与剩余秒数，
   不再是 1011「upstream websocket proxy failed」。

   **首轮的拒服一律统一包装**，换不换号由原因作用域决定（账号级真换，其余带 `NextAccountStop` 立刻
   进入耗尽关闭）。只包"能换号的那部分"等于给其余拒服开一条绕过统一呈现与归因的旁路。关闭码仍按作用域
   区分：账号级 1013（稍后再来），请求级/系统级 1008——对一个语义歧义的帧建议客户端稍后重试是错的。
   两条原生入口的**首帧歧义出口**同样走这条路。

   **两条原生入口都要接**：passthrough 在它自己的首帧注入处，ctx_pool / shared / dedicated 走
   `openai_ws_forwarder_ingress.go` 的 `parseClientPayload`（`turn == 1`），HTTP bridge 的首轮也落在
   同一个解析器上。只接一条等于另一条仍然在别的账号有票时直接断连。

   ⚠️ **bridge 每轮还有第二次准入**：它把 WS 轮次转成 HTTP 请求，于是 `doOpenAIUpstream` 里的
   `PrepareUpstream` 会再判一次。第一次通过不代表第二次还成立——两者之间票可能过期或被并发撤销。
   那一次失败同样要保住拒服身份：首轮走统一 failover，后续轮次按策略关闭（1008），**都不能落进
   "写一帧 502 upstream_error + 把错误字符串化"的通用路径**，那会同时丢掉类型、误罚账号健康度、
   并把本地拒服告知客户端为上游故障。

   **后续轮次仍然不交接**：会话状态活在那条上游连接里，换号等于把上下文丢掉，所以逐帧过滤器里的同一
   判定仍按策略关闭连接（1008）。原生 WS 也仍然不做票据层的**交接判定**（`EnsureTicketNoHandoff`）
   ——那条路上拿不到票时同步等任务出结果。

## 8. 风险与退出

| 风险 | 触发信号 | 应对 |
|---|---|---|
| **上游收紧校验**（校验 turn / 连接绑定） | 注入后响应**仍下发**新票，或请求开始报错 | `mode=off` 一键退回。这个开关必须始终可用 |
| 上游改票长度 | 出现既非已知合格也非 denylist 的长度 | denylist 机制只告警不拒服 |
| 票据出口失效 | 连续取票失败 | 事件表可见。票据出口是单点，失败即一段拒服，长度与边界见 §4.3 |
| 归因移植偏差 | 合格票被判为非 astra、成功率异常低 | §4.1 的 golden file 回归测试；§5.5 留存的线上序列持续扩充语料，也可回溯重判历史结论 |
| 冷启动阻塞触发客户端超时 | 首个请求约 30 秒无响应 | 首字节延迟的容忍度由调用方决定。实测触发超时就改成直接拒绝并给明确错误、由调用方重试，**不降级放行** |
| 账号风控 | 档位下降、授权失效 | 注入违反客户端契约的固有代价，**不可完全规避** |

**退出条件**：若上游变更使注入失效且无法在合理成本内恢复，**放弃该特性**而不是层层加码
绕过——继续对抗只会把风险推到账号上。

**看板归因**：票据拒服在 `ops_error_logs` 里落 `routing` / `platform` / `gateway`。它由响应本身决定
（502 `upstream_error` 会被分类成 `upstream` / `provider` / `upstream_http`），所以呈现改成 503 之后
归因自然就对了；另外按「最终返回的就是票据拒服」这个标记把 phase 从 `internal` 钉成 `routing`
——`internal` 指向"网关自己有 bug"，同样是误导。判据只认最终结果，不认"本请求期间有账号拒过服"：
后者在换号后遇到真实上游故障时照样在，据它归因就是把上游故障记成本地拒服。

排查这类记录的通用判据仍然有用：**`upstream_status_code` 为 NULL 即"压根没有上游响应"**。

**门控范围只覆盖会产出模型内容的端点**。`/v1/responses/input_tokens`（Responses 的 input_tokens 与
Anthropic 的 count_tokens 共用它）不产出模型输出，没有可被降智的东西——门控它只会在无票时白拒一次
计数请求，而且那条路上的拒服还拿不到本节的补救（它自己写 ops 传输错误，不走 failover 也不走 503
呈现）。跳过时仍返回一个 attempt，好让 `AfterUpstream` 继续收响应头里的免费票。embeddings / images /
realtime 的 `doOpenAIUpstream` 调用点同样绕开转换，但受门控的 codex 文本模型过不了它们各自的模型
白名单校验，到不了那里。

以上两条的实例见 `../INCIDENT-20260920-ticket-deny-as-502.md`。

守住的边界：`full` 拿不到合格票**仍然必须拒服**。这些改动都是关于怎么把拒服告诉调用方与看板、以及拒
之前有没有先试别的账号，不是该不该拒服。

## 9. 待定

- 默认值已按 §7.1 / §7.2 定下，但 `ticket_fetch_min_idle` 与 `verify_fail_cooldown` 仍站在少量
  手工探测上（§1.3.1），**要靠 §5.4 的事件数据校准**——「成功率 vs 静默时长」是最该先弄清的
  一条。`refresh_before` 不在此列：它由 TTL 与预取耗时决定，与静默门槛无关。
- 票缓存每 `(account, model)` 的保留条数。
- 升份数的置信阈值（初始取 0.9 —— astra/luna 的区分度 p 普遍 >0.94，落到 0.9 以下即视为
  采样不干净）。**是否要分档设阈值**待定：档 `"1"` 的 p=0.9 与档 `"3"` 的 p=0.9 背后证据量
  不同，严格些可以证据少时要求更高（如 0.95 / 0.90 / 0.85），但也可能是过度设计——先用单一
  阈值跑，看数据是否支持区分。
- **`full + none` 的产品语义**：现在定义为「依赖外部票源、不承诺自主恢复」（§3.1）。另一条路是
  保存时直接禁止该组合——更不容易误用，但也挡掉了"账号上另有非门控流量在养票"这种正当场景。
- **是否允许跨模型移用原始票**（§3.1）：允许则 `full + none` 有一条真正走得通的补给路径，代价是
  §5.2 的缓存键与候选选择都要按"原始票可跨模型、验证结论不可"来表达；不允许则该组合只剩手工养票
  一条路。两者都不影响"验证结论按 `(account, model)` 绑定"这条不变量。
- 票据页是否需要跨账号总览。当前设计用账号选择器（§6.2），账号数量上去之后可能需要一张列
  所有 `mode ≠ off` 账号的表——等真的不够用再加。
